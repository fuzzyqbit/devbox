---
# No handlers: the former "reload systemd" handler was notified only by the removed
# remote-desktop unit-install tasks (Phase 12 remote-desktop work). No surviving desktop
# task notifies a handler.
