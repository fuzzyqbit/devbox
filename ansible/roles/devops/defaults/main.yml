---
kubectl_version: "1.31.3"
helm_version: "3.16.3"
k9s_version: "0.32.7"
eksctl_version: "0.194.0"
istioctl_version: "1.24.2"

# mise (version manager) — folded in from the former standalone `mise` role.
# [VERIFIED: github.com/jdx/mise/releases — 2026-06-02]
# Operator-confirmed: official jdx org, release v2026.5.18, 49 assets present.
# SHA-256 sourced from upstream SHASUMS256.txt for mise-v2026.5.18-linux-x64.
mise_version: "2026.5.18"
mise_checksum_sha256: "cfac593469d028d7ae5fe36e37bd7c59118b5238e92d8a876209578464f24a84"
mise_install_dir: /usr/local/bin

# community.general collection baked alongside ansible-core — pinned to the same
# version the repo's own ansible/requirements.yml pins (keep the two in lockstep).
devops_ansible_community_general_version: "12.6.0"

dev_user: ec2-user
dev_home: /home/ec2-user
