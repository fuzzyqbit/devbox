---
phase: 06-gitlab-ci-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [run]
autonomous: true
requirements: [POL-01, POL-02]

must_haves:
  truths:
    - "./run help prints colored group headers and command names when NO_COLOR is unset and CI is unset"
    - "./run help prints plain uncolored text when NO_COLOR=1 is set"
    - "./run help prints plain uncolored text when CI=true is set"
    - "./run doctor reports pass/fail for each required tool"
    - "./run doctor works on bash 3.2 (does not hit the bash 4+ guard)"
    - "Existing error messages use the _error helper for consistent formatting"
  artifacts:
    - path: "run"
      provides: "Color output helpers, doctor command, bash 3.2-compatible dispatch"
      contains: "_info _warn _error cmd_doctor"
  key_links:
    - from: "run (color variables)"
      to: "run (_info, _warn, _error helpers)"
      via: "RED, GREEN, YELLOW, BOLD, RESET variables"
      pattern: "_error.*RED.*ERROR"
    - from: "run (doctor dispatch)"
      to: "run (bash 4+ guard)"
      via: "doctor dispatched BEFORE bash version check"
      pattern: "doctor.*cmd_doctor"
---

<objective>
Add colored status/error output and a `./run doctor` dependency checker to the run script.

Purpose: POL-01 gives operators visual feedback (colored [INFO]/[WARN]/ERROR prefixes) that auto-suppresses in CI and respects NO_COLOR. POL-02 gives operators a single command to validate their local toolchain after cloning.

Output: Updated `run` file with color helpers, doctor command, and restructured bash version guard.
</objective>

<execution_context>
@/Users/me/.claude/get-shit-done/workflows/execute-plan.md
@/Users/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gitlab-ci-polish/06-RESEARCH.md
@.planning/phases/05-run-script-core/05-01-SUMMARY.md

<interfaces>
<!-- Key structures from the run script that must be preserved -->

From run (line 7-9): Bash 4+ guard
```bash
if [[ "${BASH_VERSINFO[0]}" -lt 4 ]]; then
  echo "ERROR: Bash 4+ required. macOS: brew install bash" >&2
  exit 1
fi
```

From run (line 312-342): Main dispatcher
```bash
main() {
  local command="${1:-help}"
  shift || true
  case "$command" in
    help|--help|-h)      cmd_help ;;
    ...
    *)
      echo "ERROR: Unknown command '${command}'. Run './run help' for usage." >&2
      exit 1
      ;;
  esac
}
main "$@"
```

From run: Error output convention
```bash
echo "ERROR: ..." >&2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add color helpers, doctor command, and restructure bash version guard</name>
  <files>run</files>
  <read_first>
    - run (full file — current state, all functions, dispatcher, guards)
    - .planning/phases/06-gitlab-ci-polish/06-RESEARCH.md (Pattern 1: Color Output, Pattern 2: Doctor Command, Pitfall 1: Bash 3.2)
  </read_first>
  <action>
Modify the `run` script with these changes in order:

1. RESTRUCTURE BASH VERSION GUARD AND DOCTOR DISPATCH (Pitfall 1 from RESEARCH.md):
   - After the shebang and `set -euo pipefail`, BEFORE the current bash 4+ guard (line 7-10), add:
     a. The color variable setup block (NO_COLOR/CI detection). Use `[[ ]]` syntax which works in bash 3.2. Set RED, GREEN, YELLOW, BOLD, RESET to either ANSI escape sequences or empty strings based on `NO_COLOR` and `CI` env vars.
     b. The `_info`, `_warn`, `_error` helper functions. `_info` prints to stdout with `[INFO]` in green. `_warn` prints to stderr with `[WARN]` in yellow. `_error` prints to stderr with `ERROR:` in red. All use the color variables.
     c. The `_check_cmd` function (for doctor): takes cmd name, install hint, and optional min_major version. Uses `command -v` to check presence. If min_major is provided, extracts the version via `_get_version` and compares the major integer. Increments `pass`, `fail`, or `warn` counters using `set -e`-safe arithmetic: `pass=$((pass + 1))` — do NOT use `(( pass++ ))` which returns exit code 1 when pass=0 under `set -e`.
     d. The `_check_cmd_optional` function: same as `_check_cmd` but increments `warn` instead of `fail` when missing.
     e. The `_get_version` function: extracts version string from each tool. Use a case statement keyed on tool name with these patterns:
        - aws: `aws --version 2>&1 | head -1` then extract second field after `/`
        - packer: `packer --version 2>&1 | sed 's/.*v//' | head -1`
        - tofu: `tofu --version 2>&1 | sed 's/.*v//' | head -1`
        - ansible: `ansible --version 2>&1 | head -1 | sed 's/.*\[core //' | sed 's/\].*//'`
        - ansible-lint: `ansible-lint --version 2>&1 | head -1 | awk '{print $2}'`
        - shellcheck: `shellcheck --version 2>&1 | grep '^version:' | awk '{print $2}'`
        - gitleaks: `gitleaks version 2>&1 | sed 's/v//'`
        - pre-commit: `pre-commit --version 2>&1 | awk '{print $2}'`
        - Default: echo "unknown"
     f. The `cmd_doctor` function: initializes pass=0, fail=0, warn=0 counters. Calls `_check_cmd` for: aws (hint: "brew install awscli", min "2"), packer ("brew install packer", "1"), tofu ("brew install opentofu", "1"), ansible ("brew install ansible", ""), ansible-lint ("pip install ansible-lint", "26"), jq ("brew install jq", ""), shellcheck ("brew install shellcheck", "0"), gitleaks ("brew install gitleaks", "8"), pre-commit ("pip install pre-commit", "4"), session-manager-plugin ("brew install --cask session-manager-plugin", ""). Calls `_check_cmd_optional` for: checkov ("pip install checkov", ""). Also checks bash version: if BASH_VERSINFO[0] < 4, report as WARN (not fail) with hint "brew install bash". Prints summary line. Exits 1 if fail > 0.
     g. A mini-dispatcher that checks if `$1` is "doctor" and if so calls `cmd_doctor` and exits. This runs BEFORE the bash 4+ guard.
   - KEEP the existing bash 4+ guard after the doctor mini-dispatch. It still protects all other commands.

2. UPDATE EXISTING ERROR MESSAGES:
   - Replace all `echo "ERROR: ..."` lines in guard functions and command functions with `_error "..."` calls. The _error helper already prepends "ERROR:" in red, so remove the "ERROR: " prefix from the message strings.
   - Specifically update: `_require_devbox_user` (2 error messages), `_derive_tf_state_bucket` (1 error message), `cmd_devbox_port_forward` (1 error message), `cmd_secrets_show` (3 error messages), the unknown-command handler in main (1 error message).
   - The supplementary echo lines (e.g., "       Set per-invocation: ...") that follow error messages should continue to use plain `echo` to stderr (they are detail lines, not error headers).

3. ADD DOCTOR TO MAIN DISPATCHER:
   - Add `doctor) cmd_doctor ;;` to the case statement in the `main` function. This is a no-op for the doctor command itself (since it short-circuits before the bash guard), but keeps the help/dispatcher consistent.

4. UPDATE HELP TEXT:
   - Add a "Diagnostics" section to `cmd_help` output, listing: `doctor  Check required dependencies and report missing/version issues`

5. ALL doctor-related code (color variables, helper functions, _check_cmd, _get_version, cmd_doctor, and the mini-dispatch) MUST be written in bash 3.2-compatible syntax: no associative arrays, no `${var,,}` lowercase expansion, no `|&` pipe, no `readarray`/`mapfile`. Use `[[ ]]` and `$(...)` which are available in bash 3.2.

IMPORTANT: The `_info`, `_warn`, `_error` helpers and color variables are placed BEFORE the bash 4+ guard so they are available to both the doctor command (bash 3.2) and all other commands (bash 4+). All code above the bash 4+ guard must be bash 3.2-compatible.
  </action>
  <verify>
    <automated>shellcheck run && echo "shellcheck passed"</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck run` exits 0 (no errors or warnings beyond existing SC2046 disables)
    - `grep -c '_error' run` returns at least 8 (the helper definition + 7 callsites replacing echo ERROR)
    - `grep -c '_info\|_warn' run` returns at least 3 (the two helper definitions + at least one usage)
    - `grep -c 'cmd_doctor' run` returns at least 3 (function definition + mini-dispatch + case statement)
    - `grep -c 'NO_COLOR' run` returns at least 1 (the environment guard)
    - `grep -c 'CI.*true' run` returns at least 1 (the CI detection in color guard)
    - The string `doctor` appears in the help text output section
    - `grep -n 'cmd_doctor' run | head -1` shows a line number LESS than the line containing `BASH_VERSINFO` (doctor dispatch before bash guard)
    - `grep -c '_check_cmd' run` returns at least 11 (function definition + 10 tool checks)
    - `grep -c 'session-manager-plugin' run` returns at least 2 (existing port-forward check + doctor check)
  </acceptance_criteria>
  <done>
    - Color helpers (_info, _warn, _error) defined and used throughout the script
    - Color output suppressed when NO_COLOR is non-empty or CI=true
    - ./run doctor command checks all 10 required tools + 1 optional (checkov) + bash version
    - Doctor dispatch occurs before bash 4+ guard (bash 3.2 compatible)
    - All existing "ERROR: ..." messages converted to use _error helper
    - Help text includes doctor command
    - shellcheck passes clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation -> run script | Script trusts local tool binaries to report honest version strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | _get_version parsing | accept | Version strings come from locally-installed binaries the operator already trusts; no privilege escalation path |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. `shellcheck run` exits 0
2. `NO_COLOR=1 ./run help 2>&1` contains no ANSI escape sequences (`grep -P '\033' | wc -l` is 0)
3. `CI=true ./run help 2>&1` contains no ANSI escape sequences
4. `./run doctor` reports on bash, aws, packer, tofu, ansible, ansible-lint, jq, shellcheck, gitleaks, pre-commit, session-manager-plugin, checkov
5. Doctor mini-dispatch appears before bash 4+ guard in the file
</verification>

<success_criteria>
POL-01: Color output works locally and is suppressed with NO_COLOR=1 and CI=true
POL-02: ./run doctor checks all 10+1 dependencies in one pass, reports pass/fail/warn summary
</success_criteria>

<output>
Create `.planning/phases/06-gitlab-ci-polish/06-01-SUMMARY.md` when done
</output>
