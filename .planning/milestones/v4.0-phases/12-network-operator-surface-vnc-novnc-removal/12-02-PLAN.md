---
phase: 12-network-operator-surface-vnc-novnc-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - run
  - scripts/devbox-start.sh
  - scripts/devbox-status.sh
  - CLAUDE.md
autonomous: true
requirements: [RDP-10]
must_haves:
  truths:
    - "`./run devbox-port-forward 3389` tunnels :3389 (already supported by the generic port parser — no code change, only help text)"
    - "`./run secrets-show` labels the second password as the RDP login (ec2-user @ <host>:3389), not VNC/noVNC :6080"
    - "`./run start` and `./run status` advertise RDP :3389, not noVNC :6080"
    - "CLAUDE.md (on-disk operator copy) documents connecting with a native RDP client over SSM port-forward"
    - "No 6080 / noVNC reference remains in run, scripts/, or CLAUDE.md"
  artifacts:
    - path: "run"
      provides: "port-forward help + secrets-show label point at RDP :3389 (vnc-password SSM path unchanged)"
    - path: "scripts/devbox-start.sh"
      provides: "connection-info line advertises RDP :3389 instead of noVNC :6080"
    - path: "scripts/devbox-status.sh"
      provides: "connection-info + port-forward hint advertise RDP :3389 instead of noVNC :6080"
    - path: "CLAUDE.md"
      provides: "operator docs (§1/§2/§5/§7) describe RDP over SSM; ON-DISK ONLY — git-ignored, do NOT commit"
  key_links:
    - from: "run cmd_secrets_show"
      to: "/devbox/<user>/vnc-password SSM param"
      via: "aws ssm get-parameter (path unchanged; label only changes)"
      pattern: "/devbox/\\$\\{DEVBOX_USER\\}/vnc-password"
---

<objective>
Point the operator surface and docs at native RDP-over-SSM and scrub every `:6080`/noVNC
reference from `run`, the two delegated `scripts/`, and the on-disk CLAUDE.md. This is the
operator-surface half of RDP-10. The `cmd_devbox_port_forward` generic port parser already
accepts `3389` with NO code change (proven in research R1) — only help text, the
`secrets-show` label, and the two scripts behind `./run start`/`./run status` need editing.

Purpose: An operator runs `./run devbox-port-forward 3389` and connects a native RDP client
(mstsc / FreeRDP / Remmina) to `localhost:3389` as `ec2-user` using the `./run secrets-show`
password. The vnc-password SSM path is the RDP password (locked credential model) — KEEP the
path, relabel the human-facing text only.

Output: edited `run`, `scripts/devbox-start.sh`, `scripts/devbox-status.sh`, and the on-disk
`CLAUDE.md` (git-ignored — edited for the operator but NOT committed; any authoritative
committed note lives in code comments).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-network-operator-surface-vnc-novnc-removal/12-RESEARCH.md

<interfaces>
<!-- Exact existing references to edit. Executor uses these directly — no exploration. -->

run:391-395 — port-forward inline help comments (the "6080 -> noVNC" examples).
run:404 — _error spec example string ("e.g. 6080 or 8888:18888").
run:458-466 — secrets-show fetch of /devbox/<user>/vnc-password (KEEP the path; the error
  msg at :462 says "VNC password not found at /devbox/<user>/vnc-password").
run:469-470 — secrets-show printed labels (the "VNC / noVNC (https://<host>:6080)" line).
run:516-518 — devbox-port-forward help block ("e.g. ./run devbox-port-forward 8080 6080 8888:18888").
run:522 — secrets-show help description ("code-server and VNC passwords").
scripts/devbox-start.sh:70 — "noVNC:  https://${PRIVATE_IP}:6080 ...".
scripts/devbox-start.sh:72 — "Off-VPC? Use './run devbox-port-forward' for code-server...".
scripts/devbox-status.sh:54 — "noVNC (browser): https://${PRIVATE_IP}:6080 ...".
scripts/devbox-status.sh:59-61 — "Off-VPC operator? ... browse to https://localhost:8080 (and a second session for :6080)".
CLAUDE.md:9 — §1 "...code-server (browser VS Code) on :8080, noVNC on :6080,...".
CLAUDE.md:87 — §2 Step-2 heading "CIDR allowlist for code-server / noVNC".
CLAUDE.md:89 — §2 ":8080 (code-server) and :6080 (noVNC) are restricted...".
CLAUDE.md:118-126 — §5 daily-flow steps 5-6 (devbox-port-forward + browser :8080 line + secrets-show comment).
CLAUDE.md §7 — Troubleshooting (firewalld-blocking entry references the workaround).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel run — port-forward help (add 3389/drop 6080), secrets-show RDP label, help descriptions (vnc-password path KEPT)</name>
  <read_first>
    - run lines 379-435 (cmd_devbox_port_forward — confirm the generic parser at 400-409 accepts any numeric PORT; NO logic change), 441-473 (cmd_secrets_show), 514-523 (help block)
    - .planning/phases/12-network-operator-surface-vnc-novnc-removal/12-RESEARCH.md (inventory rows R1-R6 + "Post-removal secrets-show output should print exactly two lines")
  </read_first>
  <action>
    Make ONLY label/help/comment edits in run — NO functional logic change to
    cmd_devbox_port_forward (the parser already forwards 3389):
    (1) Port-forward inline help comments (lines 392-395): change the "6080 -> noVNC" example
    to "3389 -> RDP (native RDP client)"; update the "8080 6080" combined example to
    "8080 3389".
    (2) The _error spec example (line 404): change "e.g. 6080 or 8888:18888" to
    "e.g. 3389 or 8888:18888".
    (3) cmd_secrets_show: KEEP the SSM fetch path `/devbox/${DEVBOX_USER}/vnc-password`
    unchanged (lines 458-460) — it is the RDP/PAM login password. Reword the error message at
    line 462 from "VNC password not found at ..." to "RDP login password not found at ..."
    (path unchanged). Change the printed label at line 470 from
    "VNC / noVNC  (https://<host>:6080) password:" to
    "RDP login (ec2-user @ <host>:3389) password:". Leave line 469 (the code-server label)
    intact.
    (4) Help block: line 518 example "8080 6080 8888:18888" -> "8080 3389 8888:18888";
    line 522 "code-server and VNC passwords" -> "code-server and RDP login passwords".
    Do NOT change DEVBOX_FORWARD_DEFAULT_PORT (line 190) — it stays 8080 (code-server is the
    sensible default; 3389 is an explicit arg).
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox && ! grep -Eqi '6080|novnc' run && grep -q '/devbox/${DEVBOX_USER}/vnc-password' run && grep -q '3389' run && grep -q 'code-server (https://<host>:8080) password' run && shellcheck run && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -Eqi '6080|novnc' run` (no noVNC/6080 residue)
    - `grep -q 'RDP login' run` and `grep -q '3389' run` (RDP relabel applied)
    - SURVIVING-FEATURE CHECK: `grep -q '/devbox/${DEVBOX_USER}/vnc-password' run` (RDP-password SSM fetch path RETAINED — not renamed/removed)
    - SURVIVING-FEATURE CHECK: `grep -q 'code-server (https://<host>:8080) password' run` (code-server secrets-show label intact)
    - SURVIVING-FEATURE CHECK: `grep -q 'DEVBOX_FORWARD_DEFAULT_PORT' run` (default-port var untouched)
    - `shellcheck run` passes (no new warnings introduced)
  </acceptance_criteria>
  <done>run prints "RDP login (ec2-user @ <host>:3389) password"; port-forward help shows 3389; vnc-password fetch path unchanged; code-server surfaces intact; shellcheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Relabel scripts/devbox-start.sh + scripts/devbox-status.sh — RDP :3389 connection info</name>
  <read_first>
    - scripts/devbox-start.sh (esp. the connection-info block 65-72)
    - scripts/devbox-status.sh (esp. the connection-info block 46-65)
    - .planning/phases/12-network-operator-surface-vnc-novnc-removal/12-RESEARCH.md (inventory rows R7-R9 + Pitfall 4 — these are SEPARATE files behind ./run start / ./run status)
  </read_first>
  <action>
    In scripts/devbox-start.sh: replace the "noVNC: https://${PRIVATE_IP}:6080 ..." line
    (line 70) with an RDP line, e.g.
    "RDP desktop:         ${PRIVATE_IP}:3389  (native RDP client; reachable from VPC; requires your CIDR in allowed_web_cidrs)".
    Update the off-VPC hint (line 72) to mention RDP too, e.g.
    "Off-VPC? Use './run devbox-port-forward' for code-server (:8080) or './run devbox-port-forward 3389' for RDP, or check var.allowed_web_cidrs."
    Leave the code-server line (69) and shell/SSM lines intact.

    In scripts/devbox-status.sh: replace the "noVNC (browser): https://${PRIVATE_IP}:6080 ..."
    line (line 54) with an RDP line, e.g.
    "RDP desktop:           ${PRIVATE_IP}:3389  (native RDP client; reachable from VPC; requires your CIDR in allowed_web_cidrs)".
    Update the off-VPC port-forward hint (lines 60-61): keep the browse-to-localhost:8080 for
    code-server but change "(and a second session for :6080)" to "(and './run
    devbox-port-forward 3389' → an RDP client on localhost:3389)". Leave the code-server,
    JupyterLab, shell/SSM, and allowed_web_cidrs lines intact.
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox && ! grep -Eqi '6080|novnc' scripts/devbox-start.sh scripts/devbox-status.sh && grep -q '3389' scripts/devbox-start.sh && grep -q '3389' scripts/devbox-status.sh && grep -q ':8080' scripts/devbox-status.sh && shellcheck scripts/devbox-start.sh scripts/devbox-status.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -Eqi '6080|novnc' scripts/devbox-start.sh scripts/devbox-status.sh` (no noVNC/6080 residue in either script)
    - `grep -q '3389' scripts/devbox-start.sh` and `grep -q '3389' scripts/devbox-status.sh` (RDP advertised in both)
    - SURVIVING-FEATURE CHECK: `grep -q ':8080' scripts/devbox-status.sh` and `grep -q 'JupyterLab' scripts/devbox-status.sh` (code-server + Jupyter surfaces intact)
    - SURVIVING-FEATURE CHECK: `grep -q ':8080' scripts/devbox-start.sh` (code-server line intact in start)
    - `shellcheck scripts/devbox-start.sh scripts/devbox-status.sh` passes
  </acceptance_criteria>
  <done>Both scripts advertise RDP :3389 in their connection info; no 6080/noVNC residue; code-server + Jupyter + SSM lines intact; shellcheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md (§1/§2/§5/§7) — RDP over SSM; ON-DISK ONLY, do NOT commit</name>
  <read_first>
    - CLAUDE.md (§1 line 9, §2 lines 87-89, §5 lines 118-126, §7 Troubleshooting incl. the firewalld-blocking entry)
    - .planning/phases/12-network-operator-surface-vnc-novnc-removal/12-RESEARCH.md (inventory rows C1-C7 + "Claude's Discretion → CLAUDE.md daily-flow RDP step wording")
    - .planning/STATE.md (v3.2 decision: "CLAUDE.md is git-untracked; deviation docs that must go in CLAUDE.md are written to the operator's local copy")
  </read_first>
  <action>
    IMPORTANT: CLAUDE.md is git-IGNORED (commit effde0f). Edit the on-disk copy for the
    operator, but this file MUST NOT be committed (the plan-02 git commit step lists only run +
    the two scripts — see <output>). Do NOT add CLAUDE.md to any commit.

    Edits:
    (1) §1 (line 9): change "...code-server (browser VS Code) on :8080, noVNC on :6080,..."
    to "...code-server (browser VS Code) on :8080, an RDP desktop on :3389,...".
    (2) §2 Step-2 heading (line 87): "CIDR allowlist for code-server / noVNC" ->
    "CIDR allowlist for code-server / RDP".
    (3) §2 (line 89): ":8080 (code-server) and :6080 (noVNC) are restricted..." ->
    ":8080 (code-server) and :3389 (RDP) are restricted...".
    (4) §5 daily-flow (lines 118-126): add an RDP connection step after the existing
    devbox-port-forward line, e.g. "./run devbox-port-forward 3389   # tunnel RDP :3389, then
    connect a native RDP client (mstsc / FreeRDP / Remmina) to localhost:3389 as ec2-user".
    The §5 secrets-show comment at line 126 references
    "/devbox/${DEVBOX_USER}/{code-server,vnc}-password" — the path is unchanged so this stays
    correct; optionally annotate the vnc-password param as "(RDP login password)".
    (5) §7 Troubleshooting: ADD an RDP-connect entry, e.g. "RDP client can't connect → confirm
    your CIDR is in var.allowed_web_cidrs and the SG :3389 ingress applied, or use
    `./run devbox-port-forward 3389` and point your client at localhost:3389". Review the
    firewalld-blocking entry — leave it (the workaround playbook still exists; only its comment
    mentioning :6080 is edited in plan 12-03).
    Do NOT touch §8 invariants (no invariant references VNC/noVNC; the CIS X-server deviation
    is unaffected).
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox && ! grep -Eqi '6080|novnc' CLAUDE.md && grep -q '3389' CLAUDE.md && grep -q ':8080' CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -Eqi '6080|novnc' CLAUDE.md` (no noVNC/6080 residue)
    - `grep -q '3389' CLAUDE.md` and `grep -qi 'RDP' CLAUDE.md` (RDP documented)
    - `grep -q 'devbox-port-forward 3389' CLAUDE.md` (the native-RDP-over-SSM step present — RDP-10 doc requirement)
    - SURVIVING-FEATURE CHECK: `grep -q ':8080' CLAUDE.md` (code-server docs intact)
    - SURVIVING-FEATURE CHECK: `grep -q 'hardening' CLAUDE.md` (§8 invariants section intact)
    - GIT CHECK: after the plan's commit, `git ls-files CLAUDE.md` returns EMPTY (file remains untracked — never committed). The SUMMARY must state CLAUDE.md was edited on-disk only.
  </acceptance_criteria>
  <done>CLAUDE.md §1/§2/§5/§7 describe RDP :3389 over SSM port-forward with a native client; no 6080/noVNC residue; code-server + §8 intact; file remains git-untracked (not committed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → SSM data channel | `./run devbox-port-forward 3389` tunnels RDP over the authenticated SSM channel; bypasses the SG. No code path change here, only docs/labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Info-disclosure | secrets-show printed RDP password | accept | Pre-existing behavior: secrets-show prints the SSM SecureString to the operator's terminal on explicit request. This plan only changes the label text, not the disclosure surface. The param is KMS-encrypted at rest and fetched with --with-decryption under the operator's own AWS creds. |
| T-12-05 | Tampering | run / scripts edits | mitigate | shellcheck gate on run + both scripts after edits; no logic change to the port-forward parser (R1: generic parser already supports 3389), so no new injection surface. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (shell/doc edits only). No legitimacy checkpoint required. |
</threat_model>

<verification>
After all three tasks:
```bash
# RDP-10 operator-surface half: 3389 advertised, 6080/noVNC gone, vnc-password path kept, shellcheck clean.
! grep -Eqi '6080|novnc' run scripts/devbox-start.sh scripts/devbox-status.sh CLAUDE.md && echo "no 6080/noVNC residue OK"
grep -q '/devbox/${DEVBOX_USER}/vnc-password' run && echo "RDP-password fetch path kept OK"
grep -q 'devbox-port-forward 3389' CLAUDE.md && echo "RDP-over-SSM documented OK"
shellcheck run scripts/devbox-start.sh scripts/devbox-status.sh && echo "shellcheck OK"
git ls-files CLAUDE.md | grep -q . && echo "FAIL: CLAUDE.md is tracked" || echo "CLAUDE.md untracked OK"
```
</verification>

<success_criteria>
- `./run devbox-port-forward 3389` works (generic parser, no code change) and help advertises it (RDP-10)
- secrets-show labels the RDP login (ec2-user @ <host>:3389); vnc-password SSM fetch path retained
- start/status scripts advertise RDP :3389, not noVNC :6080
- CLAUDE.md §1/§2/§5/§7 document native RDP client over SSM port-forward (RDP-10 doc)
- Zero 6080/noVNC reference in run, scripts/, CLAUDE.md; shellcheck clean; CLAUDE.md not committed
</success_criteria>

<output>
Create `.planning/phases/12-network-operator-surface-vnc-novnc-removal/12-02-SUMMARY.md` when done.
Git commit MUST include only `run scripts/devbox-start.sh scripts/devbox-status.sh` (and the
SUMMARY) — do NOT stage/commit CLAUDE.md (git-ignored; edited on-disk for the operator only).
</output>
