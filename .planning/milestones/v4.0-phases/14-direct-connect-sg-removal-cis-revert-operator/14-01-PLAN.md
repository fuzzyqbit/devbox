---
phase: 14-direct-connect-sg-removal-cis-revert-operator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - terraform/main.tf
  - terraform/variables.tf
  - terraform/outputs.tf
autonomous: true
requirements: [DCV-06]
must_haves:
  truths:
    - "The SG admits :8443 TCP from var.allowed_web_cidrs (DCV web/WebSocket, direct connect)"
    - "The SG admits :8443 UDP from var.allowed_web_cidrs (DCV QUIC, direct connect)"
    - "The SG no longer admits :3389 (xrdp ingress dropped)"
    - "The :8080 code-server TCP ingress, :22 absence, IMDSv2-only, and egress-all are unchanged"
    - "terraform validate passes and the dcv_endpoint output describes direct :8443 connect"
  artifacts:
    - path: "terraform/main.tf"
      provides: ":8443 TCP + :8443 UDP ingress blocks gated on var.allowed_web_cidrs; no :3389"
      contains: "from_port   = 8443"
    - path: "terraform/outputs.tf"
      provides: "dcv_endpoint output (replaces rdp_endpoint), :8443 direct connect, no port-forward mention"
      contains: "dcv_endpoint"
    - path: "terraform/variables.tf"
      provides: "RDP→DCV description edits on allowed_web_cidrs / associate_public_ip / allow_open_ingress"
  key_links:
    - from: "terraform/main.tf :8443 ingress blocks"
      to: "var.allowed_web_cidrs"
      via: "cidr_blocks reference (mirrors the :8080 block)"
      pattern: "from_port   = 8443[\\s\\S]*cidr_blocks = var.allowed_web_cidrs"
---

<objective>
Flip the Terraform security group from the obsolete xrdp RDP `:3389` ingress to **direct-connect Amazon DCV `:8443` (TCP and UDP/QUIC)**, both gated on `var.allowed_web_cidrs`. Relabel the SG/variable/output descriptions from RDP→DCV and rename the `rdp_endpoint` output to `dcv_endpoint`. This is the additive half of Phase 14 — it makes the network perimeter DCV-correct while the xrdp Ansible files still exist (those are removed in Wave 2).

Purpose: DCV-06 — the network access boundary for v4.0 is direct connect (no SSM tunnel), so the SG must open `:8443` TCP **and** UDP (QUIC enabled), and drop the `:3389` surface. Per REQUIREMENTS.md DCV-06, both protocols are required; this SUPERSEDES the milestone-draft ARCHITECTURE.md "TCP-only, no UDP" line (which assumed the SSM-port-forward posture). Follow REQUIREMENTS.md.
Output: edited `terraform/main.tf`, `terraform/variables.tf`, `terraform/outputs.tf`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-RESEARCH.md

<interfaces>
<!-- Current SG (terraform/main.tf:106-142): :8080 TCP KEEP, :3389 TCP DELETE, egress-all KEEP, :22 absent KEEP. -->
<!-- The :8080 block (main.tf:111-117) is the template to mirror for the two :8443 blocks. -->
<!-- outputs.tf:21-24 = rdp_endpoint (rename→dcv_endpoint); outputs.tf:6-9 private_ip desc; outputs.tf:41-44 ssm_vnc_password_param desc (KEEP value path). -->
<!-- variables.tf:54 associate_public_ip desc; :66 allowed_web_cidrs desc; :82 allow_open_ingress desc. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SG — add :8443 TCP+UDP ingress, drop :3389, fix header comment</name>
  <files>terraform/main.tf</files>
  <action>In `resource "aws_security_group" "devbox"` (main.tf:106-142): (1) DELETE the entire `:3389` ingress block (main.tf:119-125, description "RDP (xrdp/TLS) restricted to operator CIDR allowlist"). (2) ADD, immediately after the existing `:8080` ingress block, TWO new ingress blocks mirroring the `:8080` block's structure exactly — first a TCP block (`description = "Amazon DCV (HTTPS/WebSocket) restricted to operator CIDR allowlist — direct connect"`, `from_port = 8443`, `to_port = 8443`, `protocol = "tcp"`, `cidr_blocks = var.allowed_web_cidrs`), then a UDP block (`description = "Amazon DCV QUIC (UDP) restricted to operator CIDR allowlist — direct connect, no SSM tunnel"`, `from_port = 8443`, `to_port = 8443`, `protocol = "udp"`, `cidr_blocks = var.allowed_web_cidrs`). UDP is required for QUIC per DCV-06 — do NOT omit it (the older ARCHITECTURE.md TCP-only guidance assumed SSM tunneling and is superseded). (3) EDIT the SG comment header at main.tf:102: `Web ports (:8080, :3389)` → `Web ports (:8080 code-server, :8443 DCV TCP+UDP)`. KEEP the `:8080` ingress, the egress-all block, the `:22` absence comment, and `create_before_destroy` untouched. No IMDSv2/metadata/IAM/S3-endpoint changes (out of scope — those are Phase-B/license infra per the roadmap).</action>
  <verify>
    <automated>cd terraform && grep -q 'protocol    = "udp"' main.tf && grep -A5 'from_port   = 8443' main.tf | grep -q 'var.allowed_web_cidrs' && [ "$(grep -cE 'protocol    = "(tcp|udp)"' main.tf)" -ge 3 ] && ! grep -q '3389' main.tf && echo PASS</automated>
  </verify>
  <done>main.tf has a :8443 TCP block AND a :8443 UDP block, both with `cidr_blocks = var.allowed_web_cidrs`; `protocol = "(tcp|udp)"` count ≥3 (8080 tcp, 8443 tcp, 8443 udp); no `3389` anywhere in main.tf; :8080 + egress unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: outputs + variables — rename rdp_endpoint→dcv_endpoint, relabel RDP→DCV descriptions, validate</name>
  <files>terraform/outputs.tf, terraform/variables.tf</files>
  <action>In outputs.tf: (1) RENAME the `rdp_endpoint` output (outputs.tf:21-24) to `dcv_endpoint`; set its value to `"https://${aws_instance.devbox.private_ip}:8443 — Amazon DCV web client (browser) or native DCV client, within the allowed CIDR"` and description to `"Amazon DCV endpoint (TLS :8443, TCP+UDP/QUIC) — direct connect within var.allowed_web_cidrs; browser web client or native DCV client"`. DROP any `./run devbox-port-forward 3389` mention (DCV is direct connect, no port-forward). (2) EDIT `private_ip` output description (outputs.tf:7): `code-server/RDP` → `code-server/DCV`. (3) EDIT `ssm_vnc_password_param` output description (outputs.tf:42): `RDP/desktop login password` → `DCV login password (the ec2-user PAM password)`; KEEP the output key name `ssm_vnc_password_param` and its `/devbox/${var.devbox_user}/vnc-password` value path UNCHANGED (renaming the param orphans bootstrap/secrets-show — Anti-Pattern B). In variables.tf: EDIT three descriptions only (no logic/validation changes): `associate_public_ip` (variables.tf:54) `code-server/RDP`→`code-server/DCV`; `allowed_web_cidrs` (variables.tf:66) `code-server (:8080) and RDP (:3389)`→`code-server (:8080) and DCV (:8443 TCP+UDP)`; `allow_open_ingress` (variables.tf:82) `no inbound :8080 / :3389`→`no inbound :8080 / :8443`.</action>
  <verify>
    <automated>cd terraform && grep -q 'output "dcv_endpoint"' outputs.tf && ! grep -q 'rdp_endpoint' outputs.tf && grep -q '/devbox/${var.devbox_user}/vnc-password' outputs.tf && ! grep -qiE 'RDP|3389' outputs.tf variables.tf && tofu validate 2>/dev/null || terraform validate</automated>
  </verify>
  <done>`output "dcv_endpoint"` exists; no `rdp_endpoint`/`RDP`/`3389` text remains in outputs.tf or variables.tf; the `vnc-password` SSM path is preserved; `tofu validate` (or `terraform validate`) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator client → SG → instance :8443 | Untrusted network ingress crosses the SG to the DCV server (TLS + PAM auth terminate inside the instance) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure / Elevation | :8443 TCP+UDP ingress | mitigate | Both blocks gate on `var.allowed_web_cidrs` (default RFC1918 10.0.0.0/8); empty-list refusal validation on the var is unchanged; perimeter is the CIDR allowlist + in-instance TLS/PAM |
| T-14-02 | Tampering / Spoofing | dropped :3389 RDP surface | mitigate | Net attack-surface REDUCTION — the entire RDP/xorgxrdp ingress path is removed; one fewer listening service exposed |
| T-14-03 | Spoofing | direct UDP (QUIC) path | accept | QUIC datagrams gated by the same CIDR allowlist; DCV terminates TLS/PAM; UDP is required by DCV-06 for the direct-connect posture; no SSM tunnel to intercept |
</threat_model>

<verification>
- `tofu validate` (fallback `terraform validate`) passes.
- `grep -A5 'from_port   = 8443' terraform/main.tf | grep -q 'var.allowed_web_cidrs'` → both :8443 blocks gated.
- `! grep -qE 'RDP|3389' terraform/main.tf terraform/outputs.tf terraform/variables.tf` → no RDP/:3389 residue in terraform.
- `grep -q 'output "dcv_endpoint"' terraform/outputs.tf` and `! grep -q 'rdp_endpoint' terraform/outputs.tf`.
- `vnc-password` SSM path preserved in outputs.tf (surviving-credential check).
</verification>

<success_criteria>
SG opens :8443 TCP **and** UDP gated on var.allowed_web_cidrs; :3389 dropped; :8080/egress/:22-absence unchanged; `dcv_endpoint` output describes direct :8443 connect with no port-forward; `vnc-password` path kept; `tofu validate` green.
</success_criteria>

<output>
Create `.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-01-SUMMARY.md` when done.
</output>
