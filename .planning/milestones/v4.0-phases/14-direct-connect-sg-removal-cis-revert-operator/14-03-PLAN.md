---
phase: 14-direct-connect-sg-removal-cis-revert-operator
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - ansible/roles/xrdp
  - ansible/playbook.yml
  - ansible/layer_config.yml
  - ansible/test-xrdp.yml
  - ansible/roles/hardening/defaults/main.yml
autonomous: true
requirements: [DCV-07, DCV-08]
must_haves:
  truths:
    - "The xrdp role directory, its playbook wiring, its layer toggle, test-xrdp.yml, and the vendored xorg.conf no longer exist"
    - "The CIS 2.2.1 override (amzn2023cis_rule_2_2_1: false) is gone — the rule reverts to its vendored default (true)"
    - "The post-hardening X-server guard is RETARGETED to /usr/bin/Xdcv (NOT deleted), gated on layers.dcv and layers.desktop"
    - "The dcv role block, the secrets/desktop roles, and hardening-stays-LAST are all intact and undisturbed"
    - "ansible-lint passes and the playbook still parses"
  artifacts:
    - path: "ansible/playbook.yml"
      provides: "no xrdp role/guard; post_tasks guard stats /usr/bin/Xdcv; hardening last"
      contains: "/usr/bin/Xdcv"
    - path: "ansible/roles/hardening/defaults/main.yml"
      provides: "no amzn2023cis_rule_2_2_1 override (reverted to default)"
  key_links:
    - from: "ansible/playbook.yml post_tasks guard"
      to: "/usr/bin/Xdcv"
      via: "stat path + assert (retargeted from /usr/libexec/Xorg)"
      pattern: "path: /usr/bin/Xdcv"
    - from: "ansible/playbook.yml roles list (tail)"
      to: "hardening"
      via: "last `- role:` line (invariant)"
      pattern: "- role: hardening"
---

<objective>
The DESTRUCTIVE / irreversible-cleanup wave: remove the obsolete `xrdp`/`xorgxrdp` role and all its playbook wiring (DCV-07), revert the CIS 2.2.1 X-server exception (DCV-08), and **RETARGET** (not delete) the post-hardening X-server regression guard from `/usr/libexec/Xorg` to `/usr/bin/Xdcv` so a wrong CIS revert becomes a LOUD bake failure instead of a silent blank desktop. Sequenced after Wave 1 so the operator surface + SG are already DCV-correct before the irreversible delete.

Purpose: DCV-07 tears out the xrdp stopgap now that the Phase-13 `dcv` role fills its playbook slot. DCV-08 closes the single accepted Level-2 CIS deviation — DCV virtual sessions render into `Xdcv` (DCV's own bundled X server, `/usr/bin/Xdcv` from `nice-xdcv`), NOT the system `/usr/libexec/Xorg` that xrdp's xorgxrdp backend needed. The revert is committed THIS phase; its runtime SAFETY is the Phase-15 live UAT (DCV-11). The retargeted guard is the bake-time safety net (research §1 VERDICT). The Phase-13 `dcv` role ships its OWN `45-allow-colord.rules` (confirmed — Assumption A3 holds), so deleting the whole xrdp dir is safe (Pitfall D resolved).
Output: `ansible/roles/xrdp/` deleted (via `git rm -r`), edited `ansible/playbook.yml`, edited `ansible/layer_config.yml`, `ansible/test-xrdp.yml` deleted, edited `ansible/roles/hardening/defaults/main.yml`.

CARRIED TO PHASE 15 (note only — do NOT implement here): the independent proof that the CIS revert is safe is one command on a baked box — `repoquery --requires nice-xdcv | grep -i xorg-x11-server-common` (and `rpm -qR nice-xdcv`) — plus the live render/AVC/FIPS/license/QUIC checks (DCV-11). At bake time, the retargeted `/usr/bin/Xdcv` stat IS the proof-by-survival; the documented fallback if it fires is to re-add `amzn2023cis_rule_2_2_1: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-RESEARCH.md

<interfaces>
<!-- playbook.yml:59-63 = `- role: xrdp` block + when + comment (DELETE). playbook.yml:65-69 = dcv block (KEEP untouched). playbook.yml:71-72 = hardening role, MUST stay last. -->
<!-- playbook.yml:80-96 = post_tasks W1 guard: stat /usr/libexec/Xorg (:82) + assert (:86-96), both `when: (layers.xrdp...) and (layers.desktop...)`. RETARGET stat→/usr/bin/Xdcv, when→layers.dcv and layers.desktop, reword fail_msg. DO NOT delete. -->
<!-- layer_config.yml:21-22 = `# xrdp ...` comment + `xrdp: true` (DELETE the 2 lines). layer_config.yml:23-24 = dcv comment + `dcv: true` (KEEP). -->
<!-- hardening/defaults/main.yml:28-37 = CIS 2.2.1 comment block + `amzn2023cis_rule_2_2_1: false` (DELETE the whole block so the rule reverts to vendored default true). -->
<!-- ansible/test-xrdp.yml = whole file (git rm). ansible/roles/xrdp/ = whole dir incl files/xorg.conf, files/45-allow-colord.{pkla,rules} (git rm -r — dcv role has its own colord rules). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the xrdp role, its playbook wiring, layer toggle, and test-xrdp.yml (DCV-07)</name>
  <files>ansible/roles/xrdp, ansible/playbook.yml, ansible/layer_config.yml, ansible/test-xrdp.yml</files>
  <action>(1) `git rm -r ansible/roles/xrdp` (removes all 12 files incl. the vendored `files/xorg.conf` (XDummy — xrdp-only, DCV virtual uses Xdcv) and `files/45-allow-colord.{pkla,rules}` — the dcv role ships its own colord rule at ansible/roles/dcv/files/45-allow-colord.rules, confirmed, so this is not over-removal). (2) `git rm ansible/test-xrdp.yml`. (3) In `ansible/playbook.yml`: DELETE the `- role: xrdp` block (playbook.yml:59-63 — the role line, its `when:`, and the 3-line comment). DO NOT touch the `- role: dcv` block (65-69) or `- role: hardening` (71-72) — removing the xrdp block leaves `desktop → dcv → hardening`, so hardening stays last. (4) In `ansible/layer_config.yml`: DELETE the two lines at 21-22 (the `# xrdp gates...` comment and `xrdp: true`); KEEP the `dcv:` comment + `dcv: true` (23-24). Do NOT touch the post_tasks guard in this task — it is retargeted in Task 2 (not deleted).</action>
  <verify>
    <automated>! test -d ansible/roles/xrdp && ! test -f ansible/test-xrdp.yml && ! grep -q 'role: xrdp' ansible/playbook.yml && ! grep -q 'xrdp' ansible/layer_config.yml && grep -q 'role: dcv' ansible/playbook.yml && grep -q 'dcv: true' ansible/layer_config.yml && [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'hardening')" -eq 1 ] && echo PASS</automated>
  </verify>
  <done>`ansible/roles/xrdp/` and `ansible/test-xrdp.yml` are gone; no `role: xrdp` in playbook.yml; no `xrdp` in layer_config.yml; the `dcv` role + `dcv: true` toggle survive; hardening is the LAST `- role:` line (invariant=1).</done>
</task>

<task type="auto">
  <name>Task 2: Revert CIS 2.2.1 + RETARGET the post-hardening guard to /usr/bin/Xdcv (DCV-08)</name>
  <files>ansible/roles/hardening/defaults/main.yml, ansible/playbook.yml</files>
  <action>(1) In `ansible/roles/hardening/defaults/main.yml`: DELETE the entire CIS 2.2.1 block at lines 28-37 — the `# --- X server (CIS 2.2.1)...` comment block AND the `amzn2023cis_rule_2_2_1: false` line — so the rule reverts to the vendored AMAZON2023-CIS default (`true`, which removes xorg-x11-server-common). KEEP all the other `amzn2023cis_rule_3_4_*` / journal / logrotate / AIDE overrides untouched. (2) In `ansible/playbook.yml` post_tasks, RETARGET the W1 guard (DO NOT DELETE — research §1 / Pitfall A): in the stat task (playbook.yml:80-84) change `path: /usr/libexec/Xorg` → `path: /usr/bin/Xdcv` and its `when:` from `(layers.xrdp | default(false)) and (layers.desktop | default(false))` → `(layers.dcv | default(false)) and (layers.desktop | default(false))`; in the assert task (playbook.yml:86-96) change its `when:` to the same `layers.dcv and layers.desktop` gate and reword the `fail_msg` to: "/usr/bin/Xdcv was DELETED after the dcv role configured it — the DCV X server is gone post-hardening. Most likely cause: CIS 2.2.1 (now re-enabled) removed xorg-x11-server-common, and Xdcv or the GNOME-on-Xorg virtual session depended on it. If this fires, re-add amzn2023cis_rule_2_2_1: false to ansible/roles/hardening/defaults/main.yml and confirm `rpm -qR nice-xdcv` / `repoquery --requires nice-xdcv` at the Phase-15 UAT." Also update the explanatory comment block above the stat task (playbook.yml:75-79) from the xrdp/Xorg framing to the dcv/Xdcv framing. KEEP the assert structure (stat → register → assert on `.stat.exists`).</action>
  <verify>
    <automated>! grep -q 'amzn2023cis_rule_2_2_1' ansible/roles/hardening/defaults/main.yml && grep -q 'amzn2023cis_rule_3_4_1_1' ansible/roles/hardening/defaults/main.yml && grep -q 'path: /usr/bin/Xdcv' ansible/playbook.yml && ! grep -q '/usr/libexec/Xorg' ansible/playbook.yml && grep -q 'layers.dcv' ansible/playbook.yml && (command -v ansible-lint >/dev/null && ansible-lint ansible/playbook.yml || true) && echo PASS</automated>
  </verify>
  <done>No `amzn2023cis_rule_2_2_1` in hardening/defaults (other CIS overrides intact); playbook.yml post-hardening guard stats `/usr/bin/Xdcv` (no `/usr/libexec/Xorg` residue) gated on `layers.dcv`; the assert + fail_msg are retargeted, not deleted; ansible-lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CIS hardening role (runs last) → installed packages | The hardening role can delete packages other roles installed; the post_tasks guard observes the end state after hardening |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering / surface reduction | xrdp/xorgxrdp role + system Xorg dependency | mitigate | Removing the role eliminates the xorgxrdp + system-Xorg attack surface; reverting CIS 2.2.1 re-enables the X-server purge (tightens the baseline back to CIS default) — net posture TIGHTENING |
| T-14-08 | Denial of Service | CIS 2.2.1 revert silently deleting Xdcv prerequisites | mitigate | Retargeted post-hardening guard stats `/usr/bin/Xdcv` and fails the bake LOUDLY with the exact remediation if a transitive xorg-x11-server-common dependency breaks Xdcv (converts silent breakage → self-documenting bake failure); residual proof at Phase-15 UAT (`repoquery --requires nice-xdcv`) |
| T-14-09 | Repudiation | hardening-stays-last invariant | mitigate | Removing the xrdp block leaves desktop→dcv→hardening; grep-gate #5 (re-asserted in Plan 04) confirms hardening is the last `- role:` line |
</threat_model>

<verification>
- `! test -d ansible/roles/xrdp` ; `! test -f ansible/test-xrdp.yml` ; `! grep -q 'role: xrdp' ansible/playbook.yml` ; `! grep -q 'xrdp' ansible/layer_config.yml`.
- Surviving-feature checks: `grep -q 'role: dcv' ansible/playbook.yml` ; `grep -q 'dcv: true' ansible/layer_config.yml` ; `test -f ansible/roles/dcv/files/45-allow-colord.rules` (dcv colord intact) ; secrets/desktop roles untouched.
- `! grep -q 'amzn2023cis_rule_2_2_1' ansible/roles/hardening/defaults/main.yml` AND `grep -q 'amzn2023cis_rule_3_4_1_1' ...` (only 2.2.1 reverted).
- Guard retargeted (not deleted): `grep -q 'path: /usr/bin/Xdcv' ansible/playbook.yml` AND `! grep -q '/usr/libexec/Xorg' ansible/playbook.yml`.
- Invariant: hardening is the last `- role:` line (=1).
- `ansible-lint ansible/playbook.yml` clean.
</verification>

<success_criteria>
xrdp role/wiring/toggle/test-xrdp/xorg.conf removed; CIS 2.2.1 override reverted to vendored default; post-hardening guard RETARGETED to /usr/bin/Xdcv (not deleted) gated on layers.dcv+desktop with an updated fail_msg; dcv/secrets/desktop roles + dcv colord rule intact; hardening last; ansible-lint clean. Phase-15 carries (repoquery nice-xdcv + live checks) noted only.
</success_criteria>

<output>
Create `.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-03-SUMMARY.md` when done.
</output>
