---
phase: 14-direct-connect-sg-removal-cis-revert-operator
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEVELOPER-LIFECYCLE.md
  - docs/HOWTO-ACCESS-CODE-SERVER-RDP.md
  - docs/HOWTO-ACCESS-CODE-SERVER-DCV.md
autonomous: true
requirements: [DCV-10]
must_haves:
  truths:
    - "docs/DEVELOPER-LIFECYCLE.md describes the GNOME desktop as Amazon DCV on https://<host>:8443 direct connect, with NO ./run devbox-port-forward 3389 / RDP-client guidance"
    - "docs/HOWTO-ACCESS-CODE-SERVER-RDP.md no longer exists; docs/HOWTO-ACCESS-CODE-SERVER-DCV.md exists with a DCV title/body and is reachable via git history (git mv preserves it)"
    - "The :8080 code-server + SSM-shell + JupyterLab-:8888 content is retained unchanged; only the RDP/:3389 desktop path is rewritten to DCV :8443 direct"
    - "No tracked link to the old HOWTO-ACCESS-CODE-SERVER-RDP.md filename survives anywhere outside .planning/"
    - "grep over docs/ finds no xrdp / :3389 / 'port-forward 3389' tokens"
  artifacts:
    - path: "docs/DEVELOPER-LIFECYCLE.md"
      provides: "DCV :8443 direct-connect desktop section + cheat-sheet, code-server/SSM/Jupyter retained"
      contains: ":8443"
    - path: "docs/HOWTO-ACCESS-CODE-SERVER-DCV.md"
      provides: "renamed-from-RDP access guide, DCV :8443 direct connect title+body"
      contains: "DCV"
  key_links:
    - from: "docs/DEVELOPER-LIFECYCLE.md desktop section"
      to: "https://<private-ip>:8443"
      via: "direct-connect DCV web/native client guidance (no port-forward)"
      pattern: ":8443"
    - from: "tracked repo (cheat sheets / index / README)"
      to: "docs/HOWTO-ACCESS-CODE-SERVER-DCV.md"
      via: "renamed-filename references updated"
      pattern: "HOWTO-ACCESS-CODE-SERVER-DCV"
---

<objective>
Close BLOCKER 1: the `docs/` operator guides were uncovered by the original Phase-14 plan set, leaving the phase goal "zero RDP/:3389 residue" unachievable. `docs/DEVELOPER-LIFECYCLE.md` and `docs/HOWTO-ACCESS-CODE-SERVER-RDP.md` are tracked and full of RDP/:3389/`devbox-port-forward 3389` content. This Wave-1 plan rewrites both to the v4.0 DCV **direct-connect** posture (browser web client or native DCV client at `https://<host>:8443`, within the allowed CIDR — NO SSM port-forward for the desktop), renames the HOWTO file via `git mv` (preserving history), and updates any tracked link to the old filename.

Purpose: DCV-10 mandates a direct-connect operator surface; the docs are part of that surface. NOTE — 14-RESEARCH §5 asserts "no docs/ dir" — that claim is FALSE (these two files are tracked); this plan corrects the gap that the false claim caused. The DCV desktop is direct TCP+UDP in-CIDR (no `./run devbox-port-forward 3389`); the `:8080` code-server SSM-port-forward path, the SSM shell, and the JupyterLab `:8888` flow are KEPT unchanged — only the RDP/:3389 desktop path is rewritten.
Output: rewritten `docs/DEVELOPER-LIFECYCLE.md`; `git mv docs/HOWTO-ACCESS-CODE-SERVER-RDP.md docs/HOWTO-ACCESS-CODE-SERVER-DCV.md` with a rewritten title+body; updated references to the old filename.

NOTE — this plan owns `docs/` exclusively; it has no `files_modified` overlap with 14-01 (terraform), 14-02 (run/scripts/ansible-secrets), or 14-03 (ansible/playbook+xrdp+hardening), so it runs in Wave 1 in parallel with them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-RESEARCH.md

<interfaces>
<!-- docs/DEVELOPER-LIFECYCLE.md:91-104 = "### Graphical desktop (RDP on :3389)" section (rewrite→DCV :8443 direct, drop the port-forward 3389 block); :133-134 = the "same SSM pattern as the RDP :3389 forward" aside inside the Jupyter section (reword, KEEP the :8888 manual forward); :146-148 = "code-server and the RDP desktop" passwords para; :186 cheat-sheet "RDP desktop off-VPC | ./run devbox-port-forward 3389" row (rewrite→DCV direct, drop port-forward). KEEP §0-2, the code-server :8080 + SSM-shell + Jupyter :8888 content. -->
<!-- docs/HOWTO-ACCESS-CODE-SERVER-RDP.md = whole file (git mv → DCV; rewrite title :1, the RDP bullet :5-8, the :3389 ingress mentions :17, the RDP-client prereq :48-52, Step1 :58-67, Step2 OptionA :81-94 + OptionB RDP block :108-118, Step3 :126-127, troubleshooting :137-138, quick-ref :151-153). KEEP the code-server :8080 + SSM + on/off-VPC framing; the desktop path becomes DCV :8443 DIRECT (drop Option-B-for-desktop; DCV is in-CIDR direct, not tunneled). -->
<!-- The vnc-password SSM credential is KEPT (it is the DCV PAM password now); secrets-show still prints it. Relabel the prose RDP→DCV; do NOT rename the param. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/DEVELOPER-LIFECYCLE.md RDP/:3389 → DCV :8443 direct connect</name>
  <files>docs/DEVELOPER-LIFECYCLE.md</files>
  <action>Rewrite every RDP/:3389/desktop reference to Amazon DCV direct connect, KEEPING all code-server/:8080, SSM-shell, and JupyterLab/:8888 content unchanged. Specifically: (1) The "### Graphical desktop (RDP on :3389)" section (lines ~91-104) becomes "### Graphical desktop (Amazon DCV on :8443)": the GNOME desktop is served over Amazon DCV on `:8443` (TLS); connect with the DCV browser web client (`https://<private-ip>:8443`) or a native DCV client, logging in as `ec2-user` with the desktop password from `./run secrets-show`. Your source IP must be inside `var.allowed_web_cidrs`. DELETE the off-VPC "tunnel it over SSM / `./run devbox-port-forward 3389`" subsection entirely — DCV is direct TCP+UDP in-CIDR, there is no port-forward step for the desktop (state this explicitly so readers don't look for one). (2) In the JupyterLab section (lines ~133-134), reword the parenthetical "the same SSM pattern as the RDP `:3389` forward above" to "the same SSM pattern as the code-server `:8080` forward above" — KEEP the `:8888` manual forward instructions intact. (3) The passwords paragraph (lines ~146-148): "code-server and the RDP desktop each have..." → "code-server and the Amazon DCV desktop each have a per-operator password stored in SSM Parameter Store (SecureString). The DCV desktop logs in as `ec2-user` with the desktop password" — KEEP the `./run secrets-show` instruction and the "parameter not found" note. (4) Cheat-sheet row (line ~186) "| RDP desktop off-VPC | `./run devbox-port-forward 3389` |" → "| DCV desktop (in-CIDR) | `https://<private-ip>:8443` (browser or native DCV client) |". Do NOT introduce any `make`-target token (CLAUDE.md §8 grep-gate).</action>
  <verify>
    <automated>! grep -niE 'xrdp|:?3389|port-forward 3389|RDP desktop|native RDP' docs/DEVELOPER-LIFECYCLE.md && grep -q ':8443' docs/DEVELOPER-LIFECYCLE.md && grep -q ':8080' docs/DEVELOPER-LIFECYCLE.md && grep -q 'devbox-port-forward' docs/DEVELOPER-LIFECYCLE.md && grep -q '8888' docs/DEVELOPER-LIFECYCLE.md && echo PASS</automated>
  </verify>
  <done>No `xrdp`/`:3389`/`port-forward 3389`/`RDP desktop`/`native RDP` in DEVELOPER-LIFECYCLE.md; the desktop section is DCV :8443 direct with no port-forward step; the code-server `:8080` port-forward, SSM shell, and Jupyter `:8888` flows are intact.</done>
</task>

<task type="auto">
  <name>Task 2: git mv the HOWTO to *-DCV.md, rewrite title+body to DCV :8443 direct, update refs</name>
  <files>docs/HOWTO-ACCESS-CODE-SERVER-RDP.md, docs/HOWTO-ACCESS-CODE-SERVER-DCV.md</files>
  <action>(1) `git mv docs/HOWTO-ACCESS-CODE-SERVER-RDP.md docs/HOWTO-ACCESS-CODE-SERVER-DCV.md` (preserves history). (2) Rewrite the moved file: title (line 1) "How To: Access code-server and the RDP desktop" → "How To: Access code-server and the Amazon DCV desktop"; the intro RDP bullet (lines ~5-8) → "**Amazon DCV desktop** — the GNOME graphical desktop over Amazon DCV, on port `:8443` (TLS), reached with the DCV browser web client or a native DCV client"; the `:3389` ingress mention (line ~17) → `:8443`; the "native RDP client" prerequisite (lines ~48-52) → "a DCV client (optional): the DCV browser web client needs only a browser; for the native client install Amazon DCV Client (macOS/Windows/Linux from the AWS DCV downloads page)"; Step 1 (lines ~58-67) "the RDP desktop password" → "the DCV desktop password", "logs you in ... in your RDP client" → "logs you in as `ec2-user` ... in your DCV client (or the browser login)"; Step 2 Option A (lines ~81-94) "`<private-ip>:3389` # RDP desktop (native RDP client)" → "`https://<private-ip>:8443` # Amazon DCV desktop (browser or native DCV client)" and point the prose at `:8443`; **DELETE the Option B "RDP desktop (`:3389`)" port-forward block (lines ~108-118)** — DCV is direct in-CIDR, NOT tunneled (keep the code-server `:8080` Option-B port-forward block); Step 3 (lines ~126-127) "RDP desktop → your RDP client" → "Amazon DCV desktop → DCV web/native client → username `ec2-user`, the desktop password from `./run secrets-show`"; troubleshooting rows (lines ~137-138) replace the `:3389`/RDP symptoms with DCV `:8443` ones (in-CIDR direct — confirm your IP is in `var.allowed_web_cidrs` and the :8443 TCP+UDP ingress is applied); quick-ref rows (lines ~151-153) drop the "Forward RDP :3389" row and rewrite "RDP desktop (on VPC)" → "DCV desktop (in-CIDR) | `https://<private-ip>:8443` (DCV client / browser, user `ec2-user`)". KEEP all code-server `:8080` + SSM + on/off-VPC framing for code-server. (3) `grep -rn 'HOWTO-ACCESS-CODE-SERVER-RDP'` over the tracked repo EXCLUDING `.planning/`; update every surviving reference (e.g. a README index or cheat sheet) to the new `HOWTO-ACCESS-CODE-SERVER-DCV.md` filename. (At plan-time this grep returns no matches outside `.planning/`, but the executor MUST re-run it post-`git mv` and fix any hit.)</action>
  <verify>
    <automated>test ! -f docs/HOWTO-ACCESS-CODE-SERVER-RDP.md && test -f docs/HOWTO-ACCESS-CODE-SERVER-DCV.md && ! grep -niE 'xrdp|:?3389|port-forward 3389|native RDP|RDP desktop|RDP client' docs/HOWTO-ACCESS-CODE-SERVER-DCV.md && grep -q ':8443' docs/HOWTO-ACCESS-CODE-SERVER-DCV.md && grep -q ':8080' docs/HOWTO-ACCESS-CODE-SERVER-DCV.md && test -z "$(git grep -nI 'HOWTO-ACCESS-CODE-SERVER-RDP' -- ':!.planning/**')" && echo PASS</automated>
  </verify>
  <done>The RDP-named HOWTO is gone and replaced by `HOWTO-ACCESS-CODE-SERVER-DCV.md` (via `git mv`); its title+body are DCV :8443 direct connect with no `:3389`/RDP/port-forward-desktop content; the code-server :8080 content survives; no tracked link to the old filename remains outside `.planning/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator docs → operator action | Stale RDP/:3389/port-forward guidance would send operators to a dead port or an unnecessary tunnel; the docs are the human-facing perimeter contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Tampering / Repudiation | residual RDP/:3389/port-forward desktop guidance in docs/ | mitigate | Rewrite both docs to DCV :8443 direct connect; drop the port-forward-desktop step so operators reach the CIDR-gated :8443 perimeter directly (consistent with the SG in Plan 01); the Plan-04 repo-wide gate (incl. docs/) re-confirms zero residue |
| T-14-13 | Repudiation | broken link to the renamed HOWTO file | accept→mitigate | `git mv` preserves history; a post-mv `git grep` over the tracked tree (excl. .planning) updates any surviving reference to the old filename |
</threat_model>

<verification>
- `! grep -rniE 'xrdp|:?3389|port-forward 3389' docs/` → empty (the kept code-server/:8080 + SSM + Jupyter content is allowed; only RDP/:3389 residue is forbidden).
- `test -f docs/HOWTO-ACCESS-CODE-SERVER-DCV.md` AND `test ! -f docs/HOWTO-ACCESS-CODE-SERVER-RDP.md`.
- `git grep -nI 'HOWTO-ACCESS-CODE-SERVER-RDP' -- ':!.planning/**'` → empty (no tracked link to the old name).
- Both docs still reference `:8080` and `secrets-show` (code-server + credential path retained).
</verification>

<success_criteria>
docs/ carries the DCV :8443 direct-connect operator surface: DEVELOPER-LIFECYCLE.md's desktop section + cheat sheet rewritten, the HOWTO `git mv`'d to *-DCV.md and rewritten, no RDP/:3389/port-forward-desktop residue, no broken link to the old filename, and the code-server/:8080/SSM/Jupyter content untouched — making the Phase-14 "zero residue" goal achievable across docs/.
</success_criteria>

<output>
Create `.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-05-SUMMARY.md` when done.
</output>
</content>
