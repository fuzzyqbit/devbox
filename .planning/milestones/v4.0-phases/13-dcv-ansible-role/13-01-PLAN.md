---
phase: 13-dcv-ansible-role
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/roles/dcv/defaults/main.yml
  - ansible/roles/dcv/handlers/main.yml
  - ansible/roles/dcv/tasks/main.yml
  - ansible/roles/dcv/templates/dcv.conf.j2
  - ansible/roles/dcv/templates/dcv-virtual-session.service.j2
  - ansible/roles/dcv/files/45-allow-colord.rules
  - ansible/roles/dcv/files/dcv.pam
autonomous: true
requirements: [DCV-01, DCV-02, DCV-03, DCV-05]
must_haves:
  truths:
    - "The bake installs nice-dcv-server, nice-xdcv, nice-dcv-web-viewer (no GPU pkgs) from the pinned CloudFront tarball, GPG-signature-verified (disable_gpg_check:false, no --nogpgcheck)"
    - "/etc/dcv/dcv.conf sets authentication=\"system\", web-port=8443, enable-quic-frontend=true; never authentication=\"none\""
    - "A FIPS-safe self-signed cert exists at /etc/dcv/dcv.pem + /etc/dcv/dcv.key, owner dcv:dcv, mode 0600, carrying subjectAltName=DNS:devbox"
    - "A oneshot dcv-virtual-session.service runs `dcv create-session --type virtual --owner ec2-user` after dcvserver (the only auto-virtual mechanism; create-session=true is NOT set in dcv.conf)"
    - "dcvserver and dcv-virtual-session are enabled systemd services"
    - "DCV install paths are SELinux-relabelled (restorecon) at bake while permissive, before hardening flips enforcing"
  artifacts:
    - path: "ansible/roles/dcv/tasks/main.yml"
      provides: "Install + config + cert + SELinux + session-unit + enable for the dcv role"
      min_lines: 90
      contains: "rpm_key"
    - path: "ansible/roles/dcv/defaults/main.yml"
      provides: "Version pins (split server/xdcv build), web port, deferred sha256, runtime deps"
      contains: "dcv_build_xdcv"
    - path: "ansible/roles/dcv/templates/dcv.conf.j2"
      provides: "DCV config: auth=system, web-port, QUIC on"
      contains: "enable-quic-frontend=true"
    - path: "ansible/roles/dcv/templates/dcv-virtual-session.service.j2"
      provides: "Oneshot virtual-session systemd unit"
      contains: "create-session"
    - path: "ansible/roles/dcv/handlers/main.yml"
      provides: "reload systemd handler"
      contains: "daemon_reload"
    - path: "ansible/roles/dcv/files/45-allow-colord.rules"
      provides: "polkit colord allow rule (GNOME-over-remote hang prevention)"
      contains: "color-manager"
    - path: "ansible/roles/dcv/files/dcv.pam"
      provides: "/etc/pam.d/dcv delegating to password-auth"
      contains: "password-auth"
  key_links:
    - from: "ansible/roles/dcv/tasks/main.yml"
      to: "https://d1uj6qtbmh3dt5.cloudfront.net/NICE-GPG-KEY"
      via: "rpm_key import before dnf install"
      pattern: "rpm_key"
    - from: "ansible/roles/dcv/tasks/main.yml"
      to: "/etc/dcv/dcv.pem + /etc/dcv/dcv.key"
      via: "openssl req -x509 -newkey rsa:2048 -sha256 -addext SAN, then chown dcv:dcv 0600"
      pattern: "rsa:2048"
    - from: "ansible/roles/dcv/templates/dcv-virtual-session.service.j2"
      to: "/usr/bin/dcv create-session --type virtual"
      via: "oneshot ExecStart"
      pattern: "create-session"
---

<objective>
Create the `dcv` Ansible role: install the non-GPU Amazon DCV server package set (airgap-compliant),
template `dcv.conf` (PAM auth, TLS, QUIC on, :8443), deliver a FIPS-safe TLS cert at DCV's
hardcoded path, ship the colord polkit rule + PAM delegate, install + enable the oneshot
virtual-session unit, and SELinux-relabel the install while still permissive.

This is a **port-plus-deltas**, not a greenfield build. ~80% is recovered verbatim from the prior
role at git `51c5f1f`; the new 20% (FIPS cert, SELinux relabel, colord, PAM, QUIC-on) is ported
from the shipped `xrdp` role. Do NOT invent new logic — port the two proven bodies of code.

Purpose: deliver everything the DCV server needs to survive the CIS-hardened (SELinux enforcing +
FIPS) baseline, ready for the playbook wiring + bake assert in plan 13-02.
Output: a complete `ansible/roles/dcv/` (defaults, handlers, 2 templates, 2 files, tasks core).

NOTE: the playbook wiring (`- role: dcv` before hardening + the `dcv: true` layer toggle) and the
RDP-13-grade bake assert are plan 13-02 (wave 2) — NOT this plan. This plan stops after the
SELinux relabel + cleanup tasks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-dcv-ansible-role/13-RESEARCH.md

# The port base (recover with git show — the dcv role is NOT on disk, reverted at d3bd9a0):
#   git show 51c5f1f:ansible/roles/dcv/tasks/main.yml
#   git show 51c5f1f:ansible/roles/dcv/defaults/main.yml
#   git show 51c5f1f:ansible/roles/dcv/handlers/main.yml
#   git show 51c5f1f:ansible/roles/dcv/templates/dcv.conf.j2
#   git show 51c5f1f:ansible/roles/dcv/templates/dcv-virtual-session.service.j2
# USE 51c5f1f ONLY — 67faeb3/8538ef3/1d2f32e have a PII-mangled GPG URL (<MRCLEAN:ENTROPY:001>).

# The delta recipes to port (cert / SELinux / colord / PAM):
@ansible/roles/xrdp/tasks/main.yml
@ansible/roles/xrdp/files/45-allow-colord.rules
@ansible/roles/xrdp/files/xrdp-sesman.pam
@ansible/roles/xrdp/defaults/main.yml

# The GNOME the virtual session renders (already installed by the desktop role — do NOT re-add):
@ansible/roles/desktop/tasks/main.yml

# The secrets role that sets the PAM password (the assert in Task 1 gates on its var):
@ansible/roles/secrets/tasks/generate.yml
</context>

<read_first>
- `git show 51c5f1f:ansible/roles/dcv/` (the full prior role — your port base; recover all 5 files)
- `ansible/roles/xrdp/tasks/main.yml` lines 199-224 (cert-gen + perms recipe), 314-328 (colord),
  293-299 (PAM copy), 453-481 (semanage fcontext + restorecon pattern)
- `ansible/roles/xrdp/files/45-allow-colord.rules` (copy verbatim into the dcv role)
- `ansible/roles/xrdp/files/xrdp-sesman.pam` (model the dcv.pam on it)
- `ansible/roles/desktop/tasks/main.yml` (confirms @Desktop + gnome-session + mesa-dri-drivers
  already installed — the dcv role consumes, never re-installs, these)
- 13-RESEARCH.md sections: "Exact dcv.conf for v4.0", "FIPS-Safe Cert Decision", "SELinux Relabel
  Approach", "Recovered Prior-Role Inventory (KEEP/CHANGE)", Pitfalls A–G
</read_first>

<interfaces>
<!-- Exact identifiers the executor must use verbatim. No codebase exploration needed. -->

# Airgap install (DCV-01) — CloudFront, GPG, deferred sha256:
GPG key URL (literal — guard against PII-scrubber re-mangling, Pitfall D):
  https://d1uj6qtbmh3dt5.cloudfront.net/NICE-GPG-KEY
Tarball URL template:
  https://d1uj6qtbmh3dt5.cloudfront.net/{{ dcv_version }}/Servers/nice-dcv-{{ dcv_version }}-{{ dcv_build_server }}-amzn2023-x86_64.tgz
RPM globs (find module — the glob handles the build-number skew):
  nice-dcv-server-*.rpm   nice-xdcv-*.rpm   nice-dcv-web-viewer-*.rpm

# defaults/main.yml pins (CHANGE from prior: split dcv_build → server/xdcv, add runtime deps):
  dcv_version: "2025.0"
  dcv_build_server: "20103"   # nice-dcv-server / nice-dcv-web-viewer build
  dcv_build_xdcv: "688"       # nice-xdcv build — DIFFERENT number (Pitfall E); only used in a comment, glob installs it
  dcv_web_port: 8443
  dcv_tarball_sha256: ""      # deferred-pin (CLAUDE.md §9 / A3); GPG signature is the integrity gate until filled
  dcv_runtime_deps:
    - policycoreutils-python-utils   # provides semanage (AL2023 core; NOT a DCV dep — must add)
  dev_user: ec2-user
  dev_home: /home/ec2-user

# dcv.conf keys (DCV-02) — every key verified against the AWS config-param reference:
  [security] authentication="system"      # NEVER "none"; default is already system, set explicit for the grep gate
  [connectivity] web-port={{ dcv_web_port }}
  [connectivity] enable-quic-frontend=true   # QUIC ON — v4.0 direct-connect (Pitfall C: do NOT set false)
  # NO certificate/certificate-key key exists in dcv.conf (Pitfall A) — cert is path-based, delivered as files.
  # DO NOT set [session-management] create-session=true (Pitfall B: that is CONSOLE-only).

# FIPS cert (DCV-05) — DCV reads these HARDCODED names/path; cert is files, not config:
  /etc/dcv/dcv.pem   (cert)  owner dcv:dcv  mode 0600   <- NOT 0644 like xrdp's cert
  /etc/dcv/dcv.key   (key)   owner dcv:dcv  mode 0600
  openssl recipe (port xrdp lines 204-213, change paths/owner):
    openssl req -x509 -nodes -newkey rsa:2048 -sha256 \
      -keyout /etc/dcv/dcv.key -out /etc/dcv/dcv.pem \
      -days 3650 -subj '/CN=devbox' -addext "subjectAltName=DNS:devbox"
  The `dcv` system user is auto-created by the nice-dcv-server RPM (chown runs AFTER install).

# Oneshot virtual-session unit (DCV-03) — port verbatim from 51c5f1f, the ONLY auto-virtual path:
  ExecStart=/usr/bin/dcv create-session --owner {{ dev_user }} --type virtual {{ dev_user }}-session
  ExecStop=/usr/bin/dcv close-session {{ dev_user }}-session
  Type=oneshot, RemainAfterExit=true, After=dcvserver.service, Requires=dcvserver.service

# SELinux relabel (DCV-05) — port the xrdp two-step pattern; restorecon over DCV install paths:
  restorecon -RvF /usr/bin/dcv* /usr/lib64/dcv* /etc/dcv /var/lib/dcv /var/run/dcv
  (changed_when:false, failed_when:false — tolerate paths absent on a given build / pre-first-start)
  Do NOT invent a custom fcontext type speculatively (DCV lands in distro paths with sane labels,
  unlike xrdp's /usr/local/sbin). audit2allow scoped module is a Phase-15 escalation if AVCs appear.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port the prior dcv role base (defaults, handlers, 2 templates, install + config + enable + cleanup)</name>
  <files>ansible/roles/dcv/defaults/main.yml, ansible/roles/dcv/handlers/main.yml, ansible/roles/dcv/templates/dcv.conf.j2, ansible/roles/dcv/templates/dcv-virtual-session.service.j2, ansible/roles/dcv/tasks/main.yml</files>
  <action>
    Recover the prior role with `git show 51c5f1f:ansible/roles/dcv/<file>` for all 5 files and port them, applying these deltas:

    defaults/main.yml — port the prior file, then CHANGE: split the single `dcv_build: "20103"` into
    `dcv_build_server: "20103"` and `dcv_build_xdcv: "688"` (the xdcv build differs — Pitfall E), and
    ADD `dcv_runtime_deps: [policycoreutils-python-utils]`. Keep `dcv_version`, `dcv_web_port: 8443`,
    `dcv_tarball_sha256: ""` (deferred-pin), `dev_user`, `dev_home`. Use exact values from <interfaces>.

    handlers/main.yml — port verbatim (`reload systemd` → daemon_reload).

    templates/dcv.conf.j2 — EXPAND the prior minimal template to the exact keys in <interfaces>:
    `[security] authentication="system"`; `[connectivity] web-port={{ dcv_web_port }}` AND
    `enable-quic-frontend=true` (the QUIC delta per DCV-02). Add a comment that there is NO cert key in
    dcv.conf (cert is path-based) and that create-session is deliberately NOT set (console-only). Do NOT
    add any certificate=/certificate-key= line and do NOT add [session-management] create-session=true.

    templates/dcv-virtual-session.service.j2 — port verbatim (the oneshot create-session --type virtual
    unit per <interfaces>; this is the ONLY auto-virtual mechanism — Pitfall B).

    tasks/main.yml — port the prior task list as the spine, in this order:
      1. PAM-password assert — port the prior `desktop_vnc_password is defined` + `| length > 0` assert
         with `no_log: true`. DROP the `!= "changeme"` clause (the no-changeme hook regex matches that
         literal in code files; `is defined`+`length>0` covers the intent without the literal — see A4).
      2. rpm_key NICE-GPG-KEY import (literal URL from <interfaces> — verify it survives into the
         committed file; the PII-scrubber mangled it once, Pitfall D). state: present.
      3. get_url tarball — port the dual-task pattern (checksum when `dcv_tarball_sha256 | length > 0`,
         no-checksum otherwise), using the URL template in <interfaces> with `dcv_build_server`.
      4. create /tmp/dcv-extract + unarchive --strip-components=1 (port verbatim).
      5. find the 3 RPMs by glob (nice-dcv-server-*, nice-xdcv-*, nice-dcv-web-viewer-*) — port verbatim.
      6. dnf install the 3 found RPMs, `disable_gpg_check: false` (NEVER --nogpgcheck) — port verbatim.
      7. dnf install `dcv_runtime_deps` (policycoreutils-python-utils) from AL2023 core — NEW task.
      8. template dcv.conf → /etc/dcv/dcv.conf mode 0644, notify reload systemd.
      9. template dcv-virtual-session.service → /etc/systemd/system/, notify reload systemd.
     10. enable dcvserver (systemd enabled:true daemon_reload:true) — port verbatim.
     11. enable dcv-virtual-session — port verbatim.
     (Cert, colord, PAM, SELinux relabel, cleanup are Task 2; the bake assert is plan 13-02.)
    Do NOT wire anything into ansible/playbook.yml or layer_config.yml here (that is plan 13-02).
  </action>
  <verify>
    <automated>ansible-playbook --syntax-check ansible/playbook.yml -i localhost, 2>&1 | tail -5; python3 -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('ansible/roles/dcv/**/*.yml',recursive=True)]; print('YAML OK')"</automated>
  </verify>
  <done>
    All 5 ported files exist; defaults has dcv_build_server + dcv_build_xdcv + dcv_runtime_deps;
    dcv.conf.j2 has authentication="system" + web-port + enable-quic-frontend=true and NO certificate key;
    the oneshot unit has create-session --type virtual; tasks/main.yml imports the GPG key (literal
    CloudFront URL, no placeholder), get_urls the pinned tarball, installs the 3 RPMs + runtime deps,
    templates both files, enables both services; the PAM assert has no "changeme" literal; syntax-check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the FIPS cert, colord polkit rule, PAM delegate, and SELinux relabel (the xrdp deltas)</name>
  <files>ansible/roles/dcv/files/45-allow-colord.rules, ansible/roles/dcv/files/dcv.pam, ansible/roles/dcv/tasks/main.yml</files>
  <action>
    Append the delta tasks to tasks/main.yml (ported from the xrdp role) and create the two files/.
    All tasks run AFTER the RPM install (so the `dcv` user + /etc/dcv exist) and BEFORE the bake assert
    (plan 13-02 appends after this). Order within this task:

    FILES first:
    - files/45-allow-colord.rules — copy VERBATIM from ansible/roles/xrdp/files/45-allow-colord.rules
      (the polkit JS .rules form; .pkla is ignored on AL2023 polkit 121+). Reword the header comment to
      "GNOME-over-DCV" if desired; the rule body is identical.
    - files/dcv.pam — model on ansible/roles/xrdp/files/xrdp-sesman.pam: delegate auth/account/session/
      password to `password-auth` (so CIS faillock/pwquality apply), include `pam_loginuid.so` +
      `pam_lastlog.so quiet`. This is /etc/pam.d/dcv (the default pam-service-name for authentication=system).

    TASKS appended to tasks/main.yml (after the enable tasks from Task 1):
    1. Generate the FIPS cert (DCV-05) — port xrdp lines 204-213 changing paths: openssl recipe from
       <interfaces> → /etc/dcv/dcv.key + /etc/dcv/dcv.pem, args.creates: /etc/dcv/dcv.pem (idempotent).
    2. Set cert/key ownership+perms — file module loop: dcv.pem AND dcv.key both owner dcv, group dcv,
       mode "0600" (NOT 0644 — AWS-mandated; Pitfall A: wrong perms → DCV ignores the cert).
    3. Ensure /etc/polkit-1/rules.d exists (directory 0755), then copy 45-allow-colord.rules →
       /etc/polkit-1/rules.d/45-allow-colord.rules mode 0644.
    4. Copy dcv.pam → /etc/pam.d/dcv mode 0644.
    5. SELinux relabel — port the xrdp restorecon pattern (NOT the speculative fcontext): a single
       restorecon command over the DCV install paths from <interfaces>, changed_when:false,
       failed_when:false. Do NOT add a custom semanage fcontext type — DCV's distro-path RPM gets sane
       default labels (research A6); a scoped audit2allow module is a Phase-15 escalation only if the
       live UAT shows AVCs. Never setenforce 0.
    6. Cleanup — port the prior /tmp cleanup (remove /tmp/nice-dcv.tgz + /tmp/dcv-extract).
    Ensure the cert/perms tasks run AFTER the dnf install of nice-dcv-server (the `dcv` user exists only
    post-install) — they are after Task 1's install in file order, which satisfies this.
  </action>
  <verify>
    <automated>ansible-playbook --syntax-check ansible/playbook.yml -i localhost, 2>&1 | tail -5; grep -q "rsa:2048" ansible/roles/dcv/tasks/main.yml && grep -q "subjectAltName=DNS:devbox" ansible/roles/dcv/tasks/main.yml && grep -q 'mode: "0600"' ansible/roles/dcv/tasks/main.yml && grep -q "restorecon" ansible/roles/dcv/tasks/main.yml && grep -q "password-auth" ansible/roles/dcv/files/dcv.pam && grep -q "color-manager" ansible/roles/dcv/files/45-allow-colord.rules && echo "DELTA CHECKS PASS"; ! git grep -nIE "changeme" -- ansible/roles/dcv 2>/dev/null && echo "NO CHANGEME"; ! grep -nE "enable-quic-frontend=false|authentication=.none.|--nogpgcheck|certificate=" ansible/roles/dcv/tasks/main.yml ansible/roles/dcv/templates/dcv.conf.j2 && echo "NO ANTIPATTERNS"</automated>
  </verify>
  <done>
    files/45-allow-colord.rules and files/dcv.pam exist (PAM delegates to password-auth); tasks/main.yml
    generates the rsa:2048/-sha256/SAN cert to /etc/dcv/dcv.{pem,key}, chowns dcv:dcv 0600, installs the
    colord rule + /etc/pam.d/dcv, and restorecons the DCV install paths; no "changeme" literal anywhere in
    the role; no enable-quic-frontend=false, no authentication="none", no --nogpgcheck, no certificate= key;
    syntax-check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser/native client → dcvserver :8443 (TCP web + UDP QUIC) | Untrusted network input crosses here; gated by var.allowed_web_cidrs (the SG is Phase 14, not this role) |
| dcvserver → PAM (/etc/pam.d/dcv → password-auth) | Credential validation against the ec2-user password (CIS faillock/pwquality stack) |
| CloudFront tarball + GPG key → bake host | Supply-chain boundary: vendor artifact entering the AMI |
| dcv role artifacts → SELinux enforcing (after hardening reboot) | Confinement boundary; relabel set at bake (permissive), enforced at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | DCV TLS cert on :8443 | mitigate | Self-signed RSA-2048/sha256/SAN cert at /etc/dcv/dcv.{pem,key} dcv:dcv 0600; FIPS-strict handshake rejects CN-only — SAN bake-asserted in 13-02. Self-signed trust model accepted for single-operator box (custom CA is v4.x deferred). |
| T-13-02 | Tampering | nice-dcv RPM tarball (CloudFront) | mitigate | NICE-GPG-KEY rpm_key import before install + disable_gpg_check:false; pinned version+build URL (no floating latest); dcv_tarball_sha256 deferred-pin (GPG signature is the gate until filled). Never --nogpgcheck. |
| T-13-03 | Elevation of Privilege | DCV authentication | mitigate | authentication="system" (PAM) only; bake-asserted (13-02) the conf is never authentication="none"; /etc/pam.d/dcv delegates to password-auth so CIS faillock/pwquality apply. |
| T-13-04 | Information Disclosure | DCV private key | mitigate | /etc/dcv/dcv.key owner dcv:dcv mode 0600 (over-broad perms → key disclosure); perms bake-asserted (13-02). |
| T-13-05 | Information Disclosure | DCV stream (TLS off) | mitigate | TLS on by default; the role never disables TLS; the FIPS cert is the only handshake cert (no auto-cert fallback because we ship one at the fixed path). |
| T-13-06 | Denial of Service | SELinux AVC silently kills the confined session | mitigate | restorecon over DCV install paths at bake (permissive); never setenforce 0. Residual: AVC-clean under enforcing is only provable at the Phase-15 live UAT (audit2allow scoped module is the documented escalation, not a Phase-13 gate). |
| T-13-07 | Spoofing/EoP | direct-connect :8443 TCP+UDP network exposure | accept (this phase) | The CIDR-gated SG ingress is Phase 14 (out of scope here); this role only opens the listener. Accepted for Phase 13: the listener is harmless until the SG opens it; CIDR allowlist + PAM are the perimeter (Phase 14). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | No npm/pip/cargo packages in this role. DCV is a GPG-signed vendor RPM tarball (covered by T-13-02); policycoreutils-python-utils is AL2023-core dnf (distro GPG). No [ASSUMED]/[SUS] registry packages → no slopcheck checkpoint required (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- Role directory complete: defaults, handlers, 2 templates, 2 files, tasks all present.
- `ansible-playbook --syntax-check ansible/playbook.yml -i localhost,` passes (role is on disk but not
  yet wired — syntax-check still validates the role files via galaxy path / explicit include is 13-02).
- Airgap: rpm_key import present with the literal CloudFront URL (no <MRCLEAN…> placeholder);
  disable_gpg_check:false; no --nogpgcheck anywhere.
- dcv.conf.j2: authentication="system", web-port, enable-quic-frontend=true; no certificate= key;
  no create-session=true.
- Cert: rsa:2048 + -sha256 + subjectAltName=DNS:devbox → /etc/dcv/dcv.{pem,key}, owner dcv:dcv, mode 0600.
- Oneshot unit: `dcv create-session --type virtual` (not create-session=true in conf).
- restorecon over DCV install paths present.
- No "changeme" literal in the role (no-changeme hook clean).
</verification>

<success_criteria>
The `ansible/roles/dcv/` role is complete and self-consistent: it installs the non-GPU DCV package set
airgap-compliant (DCV-01), configures dcv.conf with PAM auth + TLS + QUIC-on + :8443 (DCV-02), ships the
oneshot virtual-session unit (DCV-03), delivers the FIPS-safe cert at DCV's hardcoded path with the
mandated owner/mode + SAN and SELinux-relabels the install (DCV-05). The role does NOT yet wire into the
playbook or assert the bake — those are plan 13-02. Syntax-check passes; no anti-patterns
(QUIC-off / auth-none / --nogpgcheck / certificate= key / changeme literal) present.
</success_criteria>

<output>
Create `.planning/phases/13-dcv-ansible-role/13-01-SUMMARY.md` when done.
</output>
