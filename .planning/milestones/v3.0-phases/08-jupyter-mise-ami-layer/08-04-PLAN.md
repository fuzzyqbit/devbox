---
phase: 08-jupyter-mise-ami-layer
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - ansible/playbook.yml
  - ansible/layer_config.yml
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
autonomous: true
requirements: [JUP-08, MISE-03]
user_setup: []

must_haves:
  truths:
    - "The jupyter and mise roles are wired into the playbook before hardening, and the secrets role's when: condition now fires for the jupyter layer"
    - "hardening remains the LAST role in ansible/playbook.yml"
    - "layer_config.yml toggles jupyter and mise on, ordered before hardening"
    - "A grep-gate (pre-commit + CI) ratchets the hardening-last invariant and the no-committed-.mise.toml invariant"
    - "Honors CONTEXT.md decision D-07 (no committed .mise.toml — enforced by a grep-gate; existing language layers untouched)"
  artifacts:
    - path: "ansible/playbook.yml"
      provides: "jupyter + mise role entries before hardening; secrets when: includes layers.jupyter"
      contains: "role: jupyter"
    - path: "ansible/layer_config.yml"
      provides: "jupyter + mise layer toggles"
      contains: "jupyter:"
    - path: ".pre-commit-config.yaml"
      provides: "hardening-last + no-.mise.toml grep gates"
      contains: "hardening"
  key_links:
    - from: "ansible/playbook.yml"
      to: "ansible/roles/jupyter, ansible/roles/mise"
      via: "role entries gated on layers.jupyter / layers.mise, placed before hardening"
      pattern: "role: (jupyter|mise)"
    - from: ".pre-commit-config.yaml"
      to: "ansible/playbook.yml"
      via: "grep-gate asserting hardening is the last role"
      pattern: "hardening"
---

<objective>
Wire the two new roles into the build and ratchet the phase invariants. Add `jupyter` and `mise` to `ansible/playbook.yml` (jupyter + mise BEFORE `hardening`, which stays last — JUP-08), extend the `secrets` role's `when:` to include `layers.jupyter`, add both toggles to `layer_config.yml`, and add a grep-gate (mirrored in pre-commit and CI) that mechanically enforces "hardening is the last role" and "no `.mise.toml` is committed" (MISE-03).

Purpose: Activates the Phase 8 work in the bake pipeline while making the hardening-last and no-.mise.toml invariants self-enforcing for all future changes.
Output: edited `playbook.yml`, `layer_config.yml`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-jupyter-mise-ami-layer/08-RESEARCH.md
@.planning/phases/08-jupyter-mise-ami-layer/08-PATTERNS.md

<interfaces>
<!-- All four files are extended in place (self-analog). -->
Current playbook tail (ansible/playbook.yml): ... secrets → vscode → desktop → hardening (last).
Current secrets when::
  when: (layers.vscode | default(false)) or (layers.desktop | default(false))
Required new order: secrets (when extended) → vscode → desktop → jupyter → mise → hardening (LAST).

Existing grep-gate hygiene (from .pre-commit-config.yaml / ci.yml grep-gates):
  - Gates are chained `... && \` bash one-liners under `bash -euo pipefail -c`.
  - Comment-filter form `grep -vE '^[[:space:]]*#' ... | grep -c token` is used to avoid
    self-invalidating comment matches. Reuse that hygiene.
  - CLAUDE.md §8 lists hardening-last + no-changeme + SHA-pin + most_recent as enforced invariants.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire jupyter + mise roles into playbook.yml and layer_config.yml</name>
  <files>ansible/playbook.yml, ansible/layer_config.yml</files>
  <read_first>
    - ansible/playbook.yml (full role list; confirm hardening is currently last)
    - ansible/layer_config.yml (layer toggle list)
    - .planning/phases/08-jupyter-mise-ami-layer/08-PATTERNS.md (§"ansible/playbook.yml", §"ansible/layer_config.yml")
    - .planning/phases/08-jupyter-mise-ami-layer/08-RESEARCH.md (§"Pattern 6")
  </read_first>
  <action>
    playbook.yml:
    1. Extend the `secrets` role `when:` to a folded form including `(layers.jupyter | default(false))` alongside the existing vscode/desktop conditions.
    2. Insert two role entries AFTER `desktop` and BEFORE `hardening`: `- role: jupyter` gated `when: layers.jupyter | default(false)`, then `- role: mise` gated `when: layers.mise | default(false)`.
    3. Leave `hardening` as the final role; add/keep an inline comment on it noting "MUST remain last — invariant (JUP-08 / CLAUDE.md §8)". Do not move it.
    layer_config.yml: append `jupyter: true` and `mise: true` after `desktop: true` and before `hardening: true`, matching playbook ordering.
  </action>
  <verify>
    <automated>grep -q 'role: jupyter' ansible/playbook.yml && grep -q 'role: mise' ansible/playbook.yml && [ "$(grep -nE 'role: (jupyter|mise|hardening)' ansible/playbook.yml | tail -1 | grep -c hardening)" -eq 1 ] && grep -q 'layers.jupyter' ansible/playbook.yml && grep -qE '^[[:space:]]*jupyter: true' ansible/layer_config.yml && grep -qE '^[[:space:]]*mise: true' ansible/layer_config.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `jupyter` and `mise` role entries appear in playbook.yml, both before the `hardening` entry.
    - `hardening` is the LAST `- role:` line in playbook.yml (the tail check passes).
    - The `secrets` role `when:` now references `layers.jupyter`.
    - layer_config.yml has `jupyter: true` and `mise: true`, both before `hardening: true`.
  </acceptance_criteria>
  <done>Roles are wired before hardening (which stays last) and layer toggles are on; verify prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Add hardening-last + no-.mise.toml grep gates (pre-commit + CI)</name>
  <files>.pre-commit-config.yaml, .github/workflows/ci.yml</files>
  <read_first>
    - .pre-commit-config.yaml (the `grep-gates` local hook, entry block + comment-filter hygiene)
    - .github/workflows/ci.yml (the `grep-gates` job, numbered invariant list)
    - CLAUDE.md (§8 invariants — hardening last; §9 known follow-ups)
    - .planning/phases/08-jupyter-mise-ami-layer/08-RESEARCH.md (§"Validation Architecture", §"Wave 0 Gaps")
  </read_first>
  <action>
    Append two new numbered invariants to BOTH grep-gate locations (the `.pre-commit-config.yaml` `grep-gates` hook entry and the `.github/workflows/ci.yml` `grep-gates` job), keeping the existing `... && \` chaining / `bash -euo pipefail` style and comment-filter hygiene:
    1. Hardening-last (JUP-08): assert the last `- role:` line in `ansible/playbook.yml` is `hardening`. Use a comment-filtered extraction, e.g. `[ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ]` (or the `&&`-chained equivalent that fails the gate when hardening is not last). Do not use a bare `grep -c ... == 0` on an unfiltered file.
    2. No committed `.mise.toml` (MISE-03): `! find . -name '.mise.toml' -not -path './.git/*' | grep -q .` (fail if any `.mise.toml` is tracked).
    Add a short comment above each new gate citing JUP-08 and MISE-03 respectively. Keep the two locations byte-consistent in intent (CI is authoritative; pre-commit mirrors it).
  </action>
  <verify>
    <automated>grep -q 'hardening' .pre-commit-config.yaml && grep -q '.mise.toml' .pre-commit-config.yaml && grep -q 'hardening' .github/workflows/ci.yml && grep -q '.mise.toml' .github/workflows/ci.yml && pre-commit run grep-gates --all-files && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both `.pre-commit-config.yaml` and `.github/workflows/ci.yml` grep-gate blocks contain a hardening-last assertion and a no-`.mise.toml` assertion.
    - The hardening-last gate uses a comment-filtered / `tail -1` extraction (not a bare unfiltered `== 0`).
    - `pre-commit run grep-gates --all-files` PASSES against the current tree (hardening is last after Plan-04 Task 1; no `.mise.toml` exists).
    - Sanity: temporarily appending a dummy role after hardening makes the gate FAIL (executor may verify locally then revert), proving the ratchet bites.
  </acceptance_criteria>
  <done>Both gate locations enforce hardening-last and no-.mise.toml; the gate passes on the current tree; verify prints OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → CI / pre-commit | Future edits could re-order roles (re-opening hardening) or commit a `.mise.toml`; the gates are the enforcement boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | A role inserted after `hardening` re-opening the locked-down posture | mitigate | grep-gate (pre-commit + CI) asserts `hardening` is the last `- role:` line in playbook.yml; comment-filtered extraction avoids self-invalidation (JUP-08 / CLAUDE.md §8). |
| T-08-13 | Tampering | A committed `.mise.toml` silently changing baked tool versions | mitigate | grep-gate fails if any tracked `.mise.toml` exists (MISE-03). |
| T-08-14 | Repudiation | Local edits diverging from CI enforcement | accept | pre-commit gate mirrors the CI gate; CI is authoritative per CLAUDE.md §8 — operators run all three pre-commit install stages. |
</threat_model>

<verification>
- `grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1` ends in `hardening` (JUP-08).
- `! find . -name '.mise.toml' -not -path './.git/*' | grep -q .` passes (MISE-03).
- `pre-commit run grep-gates --all-files` is green.
- `pre-commit run ansible-lint --hook-stage pre-push` is clean across the new roles + playbook (run if hook env warmed).
</verification>

<success_criteria>
- `jupyter` + `mise` roles are active in the bake (toggles on, gated, placed before hardening).
- `hardening` remains the last role; the grep-gate enforces it for all future changes.
- A committed `.mise.toml` would fail CI.
</success_criteria>

<output>
Create `.planning/phases/08-jupyter-mise-ami-layer/08-04-SUMMARY.md` when done.
</output>
