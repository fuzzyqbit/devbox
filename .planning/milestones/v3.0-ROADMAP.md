# Roadmap: devbox — v3.0 (Archived)

## Milestone

**v3.0 — Jupyter + mise** — SHIPPED 2026-06-02 — Phases 8-9

Add JupyterLab and the `mise` version manager to the baked devbox AMI. Shipped as a
**loopback-only, on-demand** JupyterLab (pivoted mid-milestone away from the original
password-protected `0.0.0.0` HTTPS systemd service) plus a checksum-pinned `mise` binary
with system-wide shell activation.

## Phases

- [x] **Phase 8: Jupyter + mise AMI Layer** (4/4 plans) — loopback `/opt/jupyter` venv + checksum-pinned mise
- [x] **Phase 9: Jupyter Operator Surface + Docs** (1/1 plan) — `./run status` surfacing + DEVELOPER-LIFECYCLE docs

## Phase Details

### Phase 8: Jupyter + mise AMI Layer
**Goal**: The baked AMI ships JupyterLab in an isolated `/opt/jupyter` venv, launchable on demand bound to loopback (`127.0.0.1:8888`) via `./run jupyter` over SSM — no systemd service, no password, no TLS (SSM/IAM is the auth boundary) — and ships the mise binary ready for ec2-user
**Depends on**: Phase 7 (v2.0 complete; `./run` is the operator surface; `secrets` role and `hardening` role patterns established)
**Requirements**: JUP-01, JUP-08, MISE-01, MISE-02, MISE-03  (JUP-02/JUP-03/JUP-04 superseded — see amendment)
**Success Criteria** (what must be TRUE):
  1. A freshly baked AMI has an isolated `/opt/jupyter` venv with pinned JupyterLab + a registered `python3` kernel; `/opt/jupyter/bin/jupyter --version` succeeds
  2. JupyterLab is launched on demand via `./run jupyter` and binds `127.0.0.1` only — NOT a systemd service, NOT reachable on any non-loopback interface
  3. No Jupyter password is generated/published and no TLS cert is baked: no `/devbox/${devbox_user}/jupyter-password` SSM param, no jupyter listener on `0.0.0.0`
  4. Running `mise --version` as ec2-user in a new login shell succeeds; no `.mise.toml` is committed; Python/Go/Rust/Java/Node Ansible layers unmodified
  5. `hardening` remains the last role in `ansible/playbook.yml` (grep-gate passes; CI green)
**Plans**: 4 plans
- [x] 08-01-PLAN.md — mise: checksum-pinned binary + /etc/profile.d activation (Wave 1) — later folded into the `devops` role (commit ce777a3)
- [x] 08-02-PLAN.md — jupyter role: /opt/jupyter venv + kernel (loopback on-demand; systemd/TLS/password removed by amendment) (Wave 1) — later folded into the `devtools` role (commit 12d8dc5)
- [x] 08-03-PLAN.md — secrets role: jupyter-password gen/publish reverted by amendment (code-server/VNC unchanged) (Wave 2)
- [x] 08-04-PLAN.md — playbook/layer_config wiring + hardening-last & no-.mise.toml grep gates (Wave 3)

> **Design amendment (2026-06-02):** Jupyter pivoted from a password-protected `0.0.0.0`
> HTTPS systemd service to loopback-only on-demand (`./run jupyter`). Supersedes JUP-02
> (systemd service), JUP-03 (SSM password), JUP-04 (boot-time password injection) — a
> loopback listener reached only via SSM/IAM needs no app-layer password. Knock-on: the
> original Phase 9 (SG `:8888` ingress, `secrets-show` password) was obsoleted and re-scoped.
> Eliminated code-review finding CR-01 (auth-floor bypass) by removing the network listener.

> **Post-execution role consolidation (2026-06-02):** the two standalone roles created
> during Phase 8 were folded into existing roles — `mise` → `devops` (commit ce777a3,
> alongside kubectl/helm/k9s/eksctl/istioctl) and `jupyter` → `devtools` (commit 12d8dc5,
> alongside the other developer tooling). Requirement outcomes unchanged (JUP-01,
> MISE-01/02/03); mise now rides the `devops` layer and JupyterLab the `devtools` layer;
> `hardening` stays last.

---

### Phase 9: Jupyter Operator Surface + Docs
**Goal**: Operators can discover and use the loopback Jupyter flow through `./run` and the docs — no security-group changes, no password (the Phase 8 loopback pivot removed those needs)
**Depends on**: Phase 8
**Requirements**: JUP-07 (amended). JUP-05 and JUP-06 superseded.
**Success Criteria** (what must be TRUE):
  1. `./run status` indicates JupyterLab is available on demand via `./run jupyter` (loopback, 127.0.0.1:8888)
  2. `docs/DEVELOPER-LIFECYCLE.md` documents the `./run jupyter` + manual `:8888` SSM port-forward access flow
  3. No security-group rule added for `:8888` and no Jupyter password introduced — Phase 8 loopback + SSM/IAM model preserved
**Plans**: 1 plan
- [x] 09-01-PLAN.md — surface JupyterLab (on-demand/loopback) in `./run status`; document the `./run jupyter` + manual `:8888` SSM port-forward flow in DEVELOPER-LIFECYCLE.md (Wave 1)

> **Re-scope note (2026-06-02):** Original Phase 9 (`:8888` SG ingress + `secrets-show`
> password) was obsoleted by Phase 8's loopback pivot. Trimmed to operator-surface + docs.

## Milestone Summary

**Key decisions:**
- Loopback-only, on-demand JupyterLab (`./run jupyter` → `127.0.0.1:8888` over SSM) instead of a network-exposed password-protected systemd service. SSM/IAM is the auth boundary.
- `ipykernel` pinned to 6.29.5 (not 7.2.0) — AL2023 system python3 is 3.9; 7.2.0 needs ≥3.10.
- `mise` binary checksum-pinned to the official jdx release (operator-verified SHA-256), folded into the `devops` role post-execution.
- New grep-gate invariants (hardening-last, no-`.mise.toml`) mirrored across pre-commit + CI.

**Known deferred items at close:** 3 (see STATE.md Deferred Items) — 2 Phase-8 bake-time UAT/verification checks (Jupyter venv + `mise --version`, require a live AMI bake) and 1 completed-but-unarchived v2.0 quick-task.

**Tech debt:** WR-05 — `ansible/roles/secrets/templates/devbox-secrets-bootstrap.sh.j2` is outside CI's `shellcheck scripts/*.sh` glob.
