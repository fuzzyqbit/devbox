---
phase: quick-260611-jq2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/roles/dcv/defaults/main.yml
  - ansible/roles/dcv/tasks/main.yml
  - ansible/roles/dcv/handlers/main.yml
  - ansible/roles/dcv/templates/dcv.conf.j2
  - ansible/roles/dcv/templates/dcv-virtual-session.service.j2
  - ansible/playbook.yml
  - ansible/layer_config.yml
autonomous: true
requirements: [QUICK-260611-jq2]

must_haves:
  truths:
    - "ansible-playbook --syntax-check passes with the new dcv role wired in"
    - "dcv role installs Amazon DCV server from the pinned AWS tarball (not AL2023 repos)"
    - "DCV reuses the dev_user PAM password set by the desktop role — no new secret, no new SSM parameter"
    - "A headless virtual DCV session auto-starts at boot via systemd"
    - "dcv role runs after desktop and before hardening; hardening remains the last role"
    - "No `changeme` literal introduced; existing VNC/noVNC stack untouched"
  artifacts:
    - path: "ansible/roles/dcv/defaults/main.yml"
      provides: "Pinned dcv_version/dcv_build, dcv_web_port, optional dcv_tarball_sha256, dev_user/dev_home"
    - path: "ansible/roles/dcv/tasks/main.yml"
      provides: "GPG key import, tarball download+verify, RPM install, config + session unit, cleanup"
    - path: "ansible/roles/dcv/handlers/main.yml"
      provides: "reload systemd handler"
      contains: "daemon_reload"
    - path: "ansible/roles/dcv/templates/dcv.conf.j2"
      provides: "/etc/dcv/dcv.conf — web-port + system (PAM) auth"
    - path: "ansible/roles/dcv/templates/dcv-virtual-session.service.j2"
      provides: "systemd unit running dcv create-session --type virtual"
  key_links:
    - from: "ansible/playbook.yml"
      to: "dcv role"
      via: "role entry gated on layers.dcv and layers.desktop, placed before hardening"
      pattern: "role: dcv"
    - from: "ansible/roles/dcv/tasks/main.yml"
      to: "desktop_vnc_password"
      via: "assert (PAM password already set by desktop role; no rotation here)"
      pattern: "desktop_vnc_password"
---

<objective>
Create an Ansible role `dcv` that installs and configures Amazon DCV server on the
AL2023 devbox AMI, mirroring the existing `desktop` role conventions exactly. DCV
coexists with the current TigerVNC/noVNC stack (do not remove or modify it).

Purpose: Add a high-performance remote-desktop protocol (DCV) alongside VNC for the
operator's GNOME session, reusing the existing PAM-based auth so no new secret is
introduced.

Output: A complete `ansible/roles/dcv/` role plus two-line wiring edits to
`ansible/playbook.yml` and `ansible/layer_config.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@ansible/playbook.yml
@ansible/layer_config.yml
@ansible/roles/desktop/tasks/main.yml
@ansible/roles/desktop/defaults/main.yml
@ansible/roles/desktop/handlers/main.yml
@ansible/roles/desktop/templates/vncserver.service.j2
@ansible/roles/desktop/templates/novnc.service.j2

<conventions>
Mirror the `desktop` role verbatim for style:
- Role layout: defaults/main.yml, tasks/main.yml, handlers/main.yml, templates/*.j2
- Tasks open with an `assert` of `desktop_vnc_password` (defined, length > 0, != "changeme",
  `no_log: true`) — the same assert block desktop uses. DCV depends on the desktop role
  having already set the dev_user PAM password; this role does NOT set or rotate it.
- Use `dnf`, `get_url`, `unarchive`, `rpm_key`, `template`, `systemd`, `file` modules
  (no shell where a module exists).
- Template systemd units to /etc/systemd/system/, `notify: reload systemd`, then enable
  with `systemd: { name, enabled: true, daemon_reload: true }`.
- `dev_user: ec2-user`, `dev_home: /home/ec2-user` in defaults (matches desktop role).
- Clean up the downloaded tarball + extract dir at the end (noVNC/ffmpeg cleanup pattern).
- The `handlers/main.yml` is the single `reload systemd` handler (copy desktop's verbatim).
</conventions>

<locked_decisions>
1. Amazon DCV is NOT in AL2023 repos. Install from AWS tarball:
   `https://d1uj6qtbmh3dt5.cloudfront.net/{{ dcv_version }}/Servers/nice-dcv-{{ dcv_version }}-{{ dcv_build }}-amzn2023-x86_64.tgz`
   (known-good: dcv_version=2025.0, dcv_build=20103). Import the NICE GPG key via
   `rpm_key` BEFORE installing. From the extracted tarball install exactly these RPMs:
   `nice-dcv-server`, `nice-xdcv`, `nice-dcv-web-viewer`. Do NOT install nice-dcv-gl/gltest.
2. Pin `dcv_version` + `dcv_build` in defaults/main.yml (REP posture). Add optional
   `dcv_tarball_sha256: ""` — when non-empty, pass it to `get_url`'s `checksum: "sha256:{{ dcv_tarball_sha256 }}"`.
   Leave empty with a comment to pin later (matches the Packer SSM `:NN` deferred-pin
   pattern in CLAUDE.md §9).
3. Auth = DCV "system" (PAM). The desktop role already set the ec2-user PAM password from
   `desktop_vnc_password`. DCV reuses that — NO new password, NO new SSM parameter. Assert
   `desktop_vnc_password` is defined.
4. Headless VIRTUAL session (not console). systemd unit
   `dcv-virtual-session.service` running `dcv create-session --owner {{ dev_user }} --type virtual`,
   `After=`/`Requires=dcvserver.service`. Auto-start enabled.
5. Config template `/etc/dcv/dcv.conf` — web-port `{{ dcv_web_port }}` (default 8443),
   authentication = system.
6. playbook.yml: add `- role: dcv` gated
   `when: (layers.dcv | default(false)) and (layers.desktop | default(false))`, placed
   AFTER the desktop role and BEFORE hardening (hardening MUST stay last). Add `dcv: true`
   to layer_config.yml.
7. Firewall: do NOT touch firewall config. firewalld-docker-fix.yml already makes firewalld
   permissive. Access is via SSM port-forward of :8443; SG change is out of scope.
8. Do NOT remove or modify the VNC/noVNC stack — DCV coexists.
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dcv role files (defaults, handlers, templates)</name>
  <files>ansible/roles/dcv/defaults/main.yml, ansible/roles/dcv/handlers/main.yml, ansible/roles/dcv/templates/dcv.conf.j2, ansible/roles/dcv/templates/dcv-virtual-session.service.j2</files>
  <action>
    Create the role scaffold (everything except tasks/main.yml).

    defaults/main.yml — define (per D-02, D-05): `dcv_version: "2025.0"`, `dcv_build: "20103"`,
    `dcv_web_port: 8443`, `dcv_tarball_sha256: ""` (with a comment: leave empty until pinned,
    same deferred-pin posture as the Packer SSM `:NN` follow-up in CLAUDE.md §9), and
    `dev_user: ec2-user` / `dev_home: /home/ec2-user` (mirror desktop/defaults/main.yml).

    handlers/main.yml — copy desktop/handlers/main.yml verbatim: a single `reload systemd`
    handler that runs `systemd: { daemon_reload: true }`.

    templates/dcv.conf.j2 — write /etc/dcv/dcv.conf in DCV's INI format. Include a
    `[security]` section with `authentication="system"` (PAM, per D-03/D-05) and a
    `[connectivity]` section with `web-port={{ dcv_web_port }}`. Keep it minimal; only set
    web-port and authentication.

    templates/dcv-virtual-session.service.j2 — systemd unit (mirror novnc.service.j2 style)
    that auto-creates the headless virtual session (D-04). [Unit]: Description, plus
    `After=dcvserver.service` and `Requires=dcvserver.service`. [Service]: `Type=oneshot`
    with `RemainAfterExit=true`; `ExecStart=/usr/bin/dcv create-session --owner {{ dev_user }} --type virtual {{ dev_user }}-session`;
    `ExecStop=/usr/bin/dcv close-session {{ dev_user }}-session`. [Install]:
    `WantedBy=multi-user.target`. Use {{ dev_user }} from defaults — do not hardcode ec2-user.
  </action>
  <verify>
    <automated>test -f ansible/roles/dcv/defaults/main.yml && test -f ansible/roles/dcv/handlers/main.yml && test -f ansible/roles/dcv/templates/dcv.conf.j2 && test -f ansible/roles/dcv/templates/dcv-virtual-session.service.j2 && grep -q 'dcv_web_port' ansible/roles/dcv/defaults/main.yml && grep -q 'authentication' ansible/roles/dcv/templates/dcv.conf.j2 && grep -q 'create-session' ansible/roles/dcv/templates/dcv-virtual-session.service.j2 && ! grep -rq 'changeme' ansible/roles/dcv/</automated>
  </verify>
  <done>
    All four files exist; defaults declare pinned version/build + dcv_web_port +
    empty dcv_tarball_sha256; dcv.conf.j2 sets system auth and templated web-port; the
    session unit runs `dcv create-session --type virtual`; no `changeme` literal anywhere
    in the role.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dcv tasks/main.yml (GPG, download, install, config, session, cleanup)</name>
  <files>ansible/roles/dcv/tasks/main.yml</files>
  <action>
    Write the task list mirroring desktop/tasks/main.yml conventions. In order:

    1. Assert block (copy desktop's): `desktop_vnc_password is defined`, `| length > 0`,
       `!= "changeme"`, `no_log: true`, fail_msg pointing at secrets role ordering (D-03 —
       DCV reuses the PAM password the desktop role already set; this role neither sets nor
       rotates it).
    2. Import the NICE GPG key via `ansible.builtin.rpm_key` from
       `https://d1uj6qtbmh3dt5.cloudfront.net/NICE-GPG-KEY` (state: present) BEFORE any
       install (D-01).
    3. `get_url` the DCV tarball to /tmp/nice-dcv.tgz from
       `https://d1uj6qtbmh3dt5.cloudfront.net/{{ dcv_version }}/Servers/nice-dcv-{{ dcv_version }}-{{ dcv_build }}-amzn2023-x86_64.tgz`,
       mode 0644. Add `checksum: "sha256:{{ dcv_tarball_sha256 }}"` ONLY when
       `dcv_tarball_sha256 | length > 0` (use a conditional/when or a templated checksum that
       is omitted when empty — D-02). Document the deferred pin in a comment.
    4. Create /tmp/dcv-extract dir, `unarchive` the tgz into it with `remote_src: true` and
       `--strip-components=1` so the RPMs land at the top level.
    5. Install the three RPMs by absolute path with `dnf` (state: present, disable_gpg_check
       not needed — key imported): `nice-dcv-server`, `nice-xdcv`, `nice-dcv-web-viewer`.
       Use a `find` for each RPM glob (e.g. `nice-dcv-server-*.rpm`) then install the matched
       paths, OR install the extract dir glob in one dnf call listing the three filename
       patterns. Do NOT install nice-dcv-gl/gltest (D-01).
    6. `template` dcv.conf.j2 -> /etc/dcv/dcv.conf, mode 0644, `notify: reload systemd`.
    7. `template` dcv-virtual-session.service.j2 -> /etc/systemd/system/dcv-virtual-session.service,
       mode 0644, `notify: reload systemd`.
    8. Enable dcvserver via `systemd: { name: dcvserver, enabled: true, daemon_reload: true }`
       (the nice-dcv-server RPM installs the dcvserver unit).
    9. Enable the session unit: `systemd: { name: dcv-virtual-session, enabled: true, daemon_reload: true }`.
    10. Clean up: `file: state=absent` on /tmp/nice-dcv.tgz and /tmp/dcv-extract (loop —
        noVNC/ffmpeg cleanup pattern).

    Do not enable/start any service synchronously beyond `enabled: true` (Packer bake;
    services start at boot). Do not touch firewall (D-07). Do not modify the VNC/noVNC
    stack (D-08).
  </action>
  <verify>
    <automated>test -f ansible/roles/dcv/tasks/main.yml && grep -q 'desktop_vnc_password' ansible/roles/dcv/tasks/main.yml && grep -q 'rpm_key' ansible/roles/dcv/tasks/main.yml && grep -q 'nice-dcv-server' ansible/roles/dcv/tasks/main.yml && grep -q 'nice-xdcv' ansible/roles/dcv/tasks/main.yml && grep -q 'nice-dcv-web-viewer' ansible/roles/dcv/tasks/main.yml && ! grep -q 'nice-dcv-gl' ansible/roles/dcv/tasks/main.yml && grep -q 'dcvserver' ansible/roles/dcv/tasks/main.yml && ! grep -q 'changeme' ansible/roles/dcv/tasks/main.yml && ansible-lint ansible/roles/dcv/ 2>/dev/null; echo "lint exit: $?"</automated>
  </verify>
  <done>
    tasks/main.yml asserts desktop_vnc_password, imports the GPG key before install,
    downloads + (optionally checksums) + extracts the pinned tarball, installs exactly the
    three required RPMs (no gl/gltest), templates dcv.conf + session unit, enables both
    services, and cleans up temp files. No `changeme`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the role into playbook.yml and layer_config.yml; syntax-check</name>
  <files>ansible/playbook.yml, ansible/layer_config.yml</files>
  <action>
    Edit two files (D-06):

    1. ansible/playbook.yml — insert the dcv role entry AFTER the `desktop` role block and
       BEFORE the `hardening` role block (hardening MUST remain the last role — CLAUDE.md §8
       invariant, enforced by the CI grep gate that takes the LAST `- role:` line and asserts
       it is hardening). Entry:
         `- role: dcv`
         `  when: (layers.dcv | default(false)) and (layers.desktop | default(false))`
       Match the existing two-space list indentation of the other role blocks.

    2. ansible/layer_config.yml — add `dcv: true` to the `layers:` map. Place it right after
       `desktop: true` (logical grouping; the desktop dependency is real).

    Then run a syntax check to prove the wiring is valid YAML/Ansible and that hardening is
    still last.
  </action>
  <verify>
    <automated>cd ansible && ansible-playbook --syntax-check playbook.yml && grep -q 'role: dcv' playbook.yml && grep -q 'dcv: true' layer_config.yml && [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ] && ! grep -rq 'changeme' roles/dcv/ playbook.yml layer_config.yml</automated>
  </verify>
  <done>
    `ansible-playbook --syntax-check` passes; `role: dcv` present in playbook.yml gated on
    layers.dcv AND layers.desktop, positioned before hardening; `dcv: true` present in
    layer_config.yml; the last `- role:` line is still hardening (CI invariant intact);
    no `changeme` introduced.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run from repo root):
- `cd ansible && ansible-playbook --syntax-check playbook.yml` passes.
- Role layout complete: `ansible/roles/dcv/{defaults,tasks,handlers}/main.yml` + the two
  template files exist.
- CI invariants hold: last `- role:` in playbook.yml is hardening; no `changeme` literal in
  any new/edited file.
- Existing VNC/noVNC files unchanged (no diff under ansible/roles/desktop/).
- (Optional, requires AWS creds + bake — out of scope for syntax-only verification) Pin
  `dcv_tarball_sha256` after the first bake resolves the live tarball hash.
</verification>

<success_criteria>
- The `dcv` role installs Amazon DCV from the pinned AWS tarball with the three required
  RPMs, imports the NICE GPG key first, configures system (PAM) auth on web-port 8443, and
  auto-starts a headless virtual session.
- DCV reuses the desktop role's dev_user PAM password — no new secret, no new SSM parameter.
- The role is wired into the playbook gated on `layers.dcv and layers.desktop`, runs after
  desktop and before hardening, and `dcv: true` is added to layer_config.yml.
- `ansible-playbook --syntax-check` passes; all CI grep gates remain green (hardening last,
  no `changeme`); the VNC/noVNC stack is untouched.
</success_criteria>

<output>
Create `.planning/quick/260611-jq2-create-ansible-role-installing-and-confi/260611-jq2-SUMMARY.md` when done.
</output>
