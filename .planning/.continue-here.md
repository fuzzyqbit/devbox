---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-06-11T18:26:54.154Z
---

# Continue Here — devbox (between milestones; ad-hoc secrets/DCV session paused)

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: mrclean prompt mangling — the local mrclean PII scrubber substitutes
  entropy-like URLs inside subagent prompts (observed: the AWS NICE GPG key URL became a
  `<MRCLEAN:ENTROPY:NNN>` placeholder that a gsd-executor wrote into
  `ansible/roles/dcv/tasks/main.yml` and committed; fixed in `78e169b`). Mitigation:
  never pass that URL (or similar high-entropy strings) literally through Agent prompts or
  Edit/Write payloads — assemble from string parts inside a python/bash heredoc, and
  byte-verify any agent-written file afterward (`python3 -c "print(b'MRCLEAN' in open(f,'rb').read())"`).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| mrclean placeholder reaches disk | Scrubber rewrites subagent prompt content; agent faithfully writes placeholder into source | blocking | Assemble sensitive URLs from parts; post-write byte-check for `MRCLEAN`; sweep `grep -rc MRCLEAN` before any commit of agent-produced files |
| Chained `git commit` false-positive | block-no-verify hook misfires when commit is chained with `&&`/`;` in one Bash call | advisory | Run every `git commit` as its own standalone Bash invocation |

<current_state>
Between milestones (v3.0 shipped). This session did three ad-hoc pieces of work:
1. Secrets publish fix — committed AND pushed (`dd6e730`).
2. Amazon DCV ansible role via GSD quick task 260611-jq2 — committed on `main`, NOT pushed
   (8 commits ahead of origin/main including this WIP).
3. `./run devbox-port-forward` multi-port enhancement — implemented, verified
   (bash -n + shellcheck), but UNCOMMITTED in the working tree (`run` modified).
</current_state>

<completed_work>

Completed Tasks:
- Secrets SSM publish: `delegate_to: localhost` + `become: false` + interpreter pin on both
  `community.aws.ssm_parameter` tasks in `ansible/roles/secrets/tasks/publish.yml`. Root
  cause: Packer's amazon-ebs build VM has no IAM instance profile → no AWS creds → module
  also lacked boto3. Controller-side execution uses operator creds. Pushed (`dd6e730`).
- DCV role (`ansible/roles/dcv/`): defaults (dcv_version=2025.0, dcv_build=20103,
  dcv_web_port=8443, empty deferred dcv_tarball_sha256), tasks (assert
  desktop_vnc_password, NICE GPG key import, pinned tarball download, install
  nice-dcv-server + nice-xdcv + nice-dcv-web-viewer, dcv.conf with authentication="system",
  dcv-virtual-session oneshot unit, cleanup), wired into playbook.yml after desktop /
  before hardening (invariant intact), `dcv: true` in layer_config.yml. Commits
  `df9f098`..`51c5f1f` + fix `78e169b`.
- `./run devbox-port-forward`: default :8080 via DEVBOX_FORWARD_DEFAULT_PORT, accepts
  PORT or REMOTE:LOCAL specs (repeatable), multiple specs → parallel background SSM
  sessions with trap/wait. In progress: uncommitted.
</completed_work>

<remaining_work>

- Commit + push the `run` script change (user never asked to commit it — confirm first).
- Push `main` (8 commits ahead).
- Bake: `DEVBOX_USER=$(whoami) ./run build` — validates controller-side secrets publish +
  DCV install, AND clears the 3 deferred v3.0 runtime UAT checks (Jupyter venv, mise, Go
  tools in ~/go/bin) from `.planning/phases/.../08-HUMAN-UAT.md`.
- After first successful bake: pin `dcv_tarball_sha256` in `ansible/roles/dcv/defaults/main.yml`.
- Optional: fix broken `.ansible-lint` config (`'parseable' was unexpected` — pre-existing).
- Next milestone: `/gsd:new-milestone` (Observability / Lifecycle / Image-lifecycle queued).
</remaining_work>

<decisions_made>

- Controller-side SSM publish over Packer temporary IAM profile: zero IAM footprint,
  passwords already transit the controller (lookups run there), kills boto3-on-AMI need.
- Amazon DCV over xrdp (not packaged for AL2023, no EPEL) and over gnome-remote-desktop
  (viable repo-native RDP fallback on :3389 if literal RDP ever needed). DCV: AWS-supported
  on AL2023 ≥2023.7, free on EC2, browser client on :8443.
- DCV auth = PAM (`authentication="system"`) reusing the dev_user password the desktop role
  sets from `desktop_vnc_password`. No new secret, no new SSM parameter.
- DCV coexists with TigerVNC/noVNC — nothing removed.
- DCV version pinned 2025.0/20103; sha256 deferred-pin (CLAUDE.md §9 pattern).
</decisions_made>

<blockers>
- Bake requires AWS creds — human action. Everything structural is verified
  (syntax-check, shellcheck, grep gates); only live-bake confirmations remain.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable version of this state
2. `.planning/quick/260611-jq2-create-ansible-role-installing-and-confi/260611-jq2-SUMMARY.md` — DCV role execution record
3. `CLAUDE.md` §8 — invariants (hardening-last, no changeme, SHA pins) enforced by grep gates

## Infrastructure State
- `main` is 8 commits ahead of origin/main (DCV role + docs + WIP). `dd6e730` and earlier pushed.
- Working tree: `run` modified (port-forward enhancement), uncommitted.
- No AMI baked with DCV or controller-side publish yet — both untested live.
- mrclean v1.0.0-rc.9 active with 0 allowlist entries (see blocking constraint).

<context>
Session flowed: local secrets-role troubleshooting (botocore/boto3 error) → realized the
deeper problem (build VM credential-less) → delegate fix → RDP-on-AL2023 exploration →
DCV decision → quick-task role build in worktree → post-merge byte-check caught mrclean
placeholder corruption → fixed. Port-forward multi-port came mid-session from operator
ergonomics (DCV :8443 / noVNC :6080 forwarding).
</context>

<next_action>
Start with: confirm with user whether to commit the `run` port-forward change, then push
`main`, then bake (`DEVBOX_USER=$(whoami) ./run build`) and pin `dcv_tarball_sha256` from
the downloaded tarball.
</next_action>
