---
phase: 03-reproducibility-version-pinning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packer/devimage.pkr.hcl
  - packer/variables.pkr.hcl
  - Makefile
  - terragrunt.hcl
autonomous: true
requirements: [REP-04, REP-05]
tags: [reproducibility, version-pinning, packer, ami-handoff, manifest, makefile, ssm-parameter-store]

must_haves:
  truths:
    - "`grep -E 'most_recent\\s*=\\s*true' packer/` returns no matches (REP-04 anti-pattern eliminated)"
    - "packer/devimage.pkr.hcl resolves source AMI via a Packer `amazon-parameterstore` data source with a literal `:NN` version suffix on the SSM parameter name (Pattern 4)"
    - "packer/devimage.pkr.hcl contains a `post-processor \"manifest\"` block writing `packer/packer-manifest.json` (Pattern 5)"
    - "Makefile has a `packer-bake` target that (a) deletes any stale manifest, (b) runs `packer build`, (c) parses the manifest with `jq` extracting the AMI ID, (d) writes `users/${DEVBOX_USER}.auto.tfvars` containing `ami_id = \"ami-...\"` (REP-05 mechanism)"
    - "terragrunt.hcl no longer contains a hand-copied `ami_id = \"ami-XXXX\"` literal in `inputs` (REP-05 hand-copy eliminated)"
    - "`packer validate packer/` exits 0 against the post-edit config"
    - "Running `make packer-bake DEVBOX_USER=test --dry-run` (or equivalent) prints the expected jq+printf command chain without executing it (smoke-only — actual build requires AWS credentials and is out-of-scope for this task)"
  artifacts:
    - path: "packer/devimage.pkr.hcl"
      provides: "Pinned SSM-parameter source AMI + manifest post-processor"
      contains: "amazon-parameterstore"
      contains_also: "post-processor \"manifest\""
      contains_not: "most_recent"
    - path: "packer/variables.pkr.hcl"
      provides: "(possibly unchanged) — only modified if a new variable is needed for the SSM parameter name"
    - path: "Makefile"
      provides: "`packer-bake` target wrapping build + manifest parse + tfvars write"
      contains: "packer-bake:"
      contains_also: "packer-manifest.json"
      contains_also_2: "users/$(DEVBOX_USER).auto.tfvars"
    - path: "terragrunt.hcl"
      provides: "ami_id no longer in inputs block; comment explains the auto.tfvars handoff"
      contains_not_regex: "^\\s*ami_id\\s*=\\s*\"ami-"
  key_links:
    - from: "packer/devimage.pkr.hcl (amazon-parameterstore data source)"
      to: "AWS SSM Parameter Store"
      via: "data.amazon-parameterstore.al2023_minimal.value resolved at packer build time against /aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64:NN"
      pattern: "amazon-parameterstore.*al2023"
    - from: "packer/devimage.pkr.hcl (manifest post-processor)"
      to: "packer/packer-manifest.json"
      via: "post-processor \"manifest\" { output = \"${path.root}/packer-manifest.json\" }"
      pattern: "post-processor \"manifest\""
    - from: "Makefile (packer-bake target)"
      to: "users/${DEVBOX_USER}.auto.tfvars"
      via: "jq '.builds | sort_by(.build_time) | .[-1].artifact_id | split(\":\") | .[1]' on packer-manifest.json, then printf into auto.tfvars"
      pattern: "users/\\$\\(DEVBOX_USER\\)\\.auto\\.tfvars"
    - from: "users/${DEVBOX_USER}.auto.tfvars"
      to: "var.ami_id in terraform/variables.tf"
      via: "Terraform's *.auto.tfvars auto-load when terragrunt runs `tofu plan` in .terragrunt-cache/.../terraform/"
      pattern: "ami_id = \"ami-"

---

<objective>
Eliminate the two remaining floating-input sites in the bake → provision pipeline: the Packer source AMI filter (currently `most_recent = true` against a glob) and the hand-copied AMI ID in `terragrunt.hcl`.

Purpose: Closes REP-04 (pin Packer source AMI via SSM Parameter Store version pin — Pattern 4 from 03-RESEARCH.md:256-290) and REP-05 (automate the Packer-output → Terraform-input handoff via manifest post-processor + Makefile + auto.tfvars — Pattern 5 from 03-RESEARCH.md:292-346). After this plan, `make packer-bake` + `make tg-apply` requires zero hand-copying.

Output:
- `packer/devimage.pkr.hcl`: `source_ami_filter { most_recent = true }` block REPLACED with `data "amazon-parameterstore" "al2023_minimal"` + `source_ami = data.amazon-parameterstore.al2023_minimal.value`; `post-processor "manifest"` added inside the `build {}` block.
- `Makefile`: new `.PHONY: packer-bake` target that wraps `packer build`, parses `packer-manifest.json` with `jq`, and writes `users/${DEVBOX_USER}.auto.tfvars`. `clean` target updated to also remove the manifest. `help` updated to list `packer-bake`.
- `terragrunt.hcl`: `ami_id = "ami-0b7cfe2135f319a55"` line in `inputs` REMOVED; comment block explains the auto.tfvars handoff.
- `packer/variables.pkr.hcl`: optionally add a `var.source_ami_ssm_parameter` variable IF the executor decides to parameterize the SSM path (Claude's discretion — recommendation: hardcode for the first cut, parameterize in Phase 4 if needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-reproducibility-version-pinning/03-RESEARCH.md

@packer/devimage.pkr.hcl
@packer/variables.pkr.hcl
@Makefile
@terragrunt.hcl
@terraform/variables.tf

<interfaces>
<!-- Existing contracts the executor must respect — do NOT re-explore the codebase. -->

From packer/devimage.pkr.hcl (lines 19-55) — the source block that must change:
```hcl
source "amazon-ebs" "al2023" {
  ami_name      = local.ami_name
  instance_type = var.instance_type
  region        = var.aws_region

  vpc_id    = var.vpc_id != "" ? var.vpc_id : null
  subnet_id = var.subnet_id != "" ? var.subnet_id : null

  source_ami_filter {                       # ← REPLACE this whole block
    filters = {                              #
      name                = "al2023-ami-minimal-*-x86_64"
      root-device-type    = "ebs"
      virtualization-type = "hvm"
    }
    most_recent = true                       # ← This is the anti-pattern REP-04 closes
    owners      = ["amazon"]
  }

  ssh_username = "ec2-user"
  # ... rest unchanged ...
}
```

From packer/devimage.pkr.hcl (lines 57-75) — the build block where the manifest post-processor goes:
```hcl
build {
  sources = ["source.amazon-ebs.al2023"]

  provisioner "ansible" {
    # ... unchanged ...
  }
  # ← ADD post-processor "manifest" { ... } HERE
}
```

From terraform/variables.tf (lines 1-4) — `var.ami_id` is REQUIRED (no default), so a missing tfvars file is a loud failure (per 03-RESEARCH.md:304):
```hcl
variable "ami_id" {
  type        = string
  description = "AMI ID of the devimage to launch"
}
```

From terragrunt.hcl (line 45) — the hand-copy that must go:
```hcl
inputs = {
  devbox_user = local.user
  ami_id      = "ami-0b7cfe2135f319a55"   # ← REMOVE this line
  # ... rest unchanged ...
}
```

From .gitignore (line 32, after Plan 03-01 lands) — confirms `*.auto.tfvars` is gitignored:
```
# Phase 2: operator CIDR allowlist (written by `make devbox-allowlist-me`).
*.auto.tfvars
allowlist.auto.tfvars
```

From Makefile (lines 1-2, 42-50) — the `.PHONY` declaration and the existing `init`/`build` targets that `packer-bake` will compose:
```makefile
.PHONY: help validate build fmt clean init start stop status devbox-ssm devbox-port-forward devbox-allowlist-me secrets-show tf-init tf-plan tf-apply tf-destroy tg-init tg-reinit tg-plan tg-apply tg-auto-apply tg-destroy tg-auto-destroy

DEVBOX_USER ?= $(shell whoami)
# ...
init:
	cd packer && packer init .

validate: init
	cd packer && packer validate .
	DEVBOX_USER=$(DEVBOX_USER) terragrunt validate

build: init
	cd packer && packer build .
```
</interfaces>

<research_pointers>
<!-- All citations live in 03-RESEARCH.md; consult these line ranges. -->
- 03-RESEARCH.md:256-290 — Pattern 4: SSM Parameter Store version-pinned source AMI; the `:NN` suffix is what makes it deterministic
- 03-RESEARCH.md:289-291 — Backup if SSM versioning is unreliable: hardcoded AL2023 name (less elegant, bulletproof)
- 03-RESEARCH.md:292-346 — Pattern 5: manifest-to-tfvars handoff (the recommended REP-05 mechanism)
- 03-RESEARCH.md:325-334 — Example E: complete Makefile target body to use as the template
- 03-RESEARCH.md:336-345 — Where the AMI ID change lands in terragrunt.hcl (REMOVE from inputs, add explanatory comment)
- 03-RESEARCH.md:422-432 — Pitfall 4: stale manifest across builds — MUST `rm -f packer-manifest.json` before build, OR use `sort_by(.build_time)` in jq
- 03-RESEARCH.md:411-420 — Pitfall 3: pinned source AMI deprecation (operator maintenance burden — document in SUMMARY)
- 03-RESEARCH.md:577-614 — Example F: verification commands per requirement
- 03-RESEARCH.md:691-700 — Phase 4 CI hook needs that downstream signals
</research_pointers>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Packer source AMI to SSM-versioned parameter + add manifest post-processor</name>
  <files>
    packer/devimage.pkr.hcl
  </files>
  <action>
    Two surgical edits to `packer/devimage.pkr.hcl`. No new variables introduced (hardcode the SSM parameter path in the data source — Claude's discretion per Open Question #3-adjacent; parameterizing is a Phase 4 follow-up if needed, per KISS in 03-RESEARCH.md:682).

    Step 1 — Resolve the current SSM parameter version BEFORE editing the HCL. The literal `:NN` suffix in the data source name is what makes this a version pin (Pattern 4 at 03-RESEARCH.md:262-265). The executor MUST resolve the live version, NOT copy the placeholder `:42` from Example D verbatim (assumption A3 at 03-RESEARCH.md:636 — research uses `:42` as a placeholder).

    Run:
      aws ssm get-parameter-history \
        --name /aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64 \
        --region us-east-1 \
        --query 'Parameters[-1].{Version: Version, LastModifiedDate: LastModifiedDate, Value: Value}' \
        --output table

    The most recent row's `Version` field is what gets baked into the parameter name. Record both the version number and the resolved AMI ID for the SUMMARY.

    Failure modes:
      - `aws ssm` errors with `ParameterNotFound` → wrong path. AL2023 minimal kernel-default x86_64 path is the canonical one per 03-RESEARCH.md:262. Double-check region.
      - `aws ssm` errors with credentials → operator must `aws sso login` (or equivalent) BEFORE running the executor; this is an environment prerequisite, not a plan defect.
      - History returns only the most-recent value, no history → IAM policy lacks `ssm:GetParameterHistory`; fall back to `aws ssm get-parameter` which returns the current Version field. Note in SUMMARY.

    Step 2 — Edit `packer/devimage.pkr.hcl`.

    REPLACE the `source_ami_filter { ... }` block (lines 27-35) with a top-level `data` block (above the `source` block) AND replace `source_ami_filter` with `source_ami = data.amazon-parameterstore.al2023_minimal.value`. Final shape of the file (showing context):

      packer {
        required_plugins {
          amazon = {
            version = ">= 1.3.0"
            source  = "github.com/hashicorp/amazon"
          }
          ansible = {
            version = ">= 1.1.0"
            source  = "github.com/hashicorp/ansible"
          }
        }
      }

      locals {
        timestamp = formatdate("YYYYMMDDhhmmss", timestamp())
        ami_name  = "${var.ami_name_prefix}-al2023-${local.timestamp}"
      }

      # REP-04: pin the source AMI to a specific SSM Parameter Store *parameter version*
      # (the trailing :NN). Bumping the pin is an intentional reviewable diff in this file.
      # To resolve a newer version:
      #   aws ssm get-parameter-history --name /aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64 --region us-east-1
      # See 03-RESEARCH.md Pattern 4 (line 256-290).
      data "amazon-parameterstore" "al2023_minimal" {
        name   = "/aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64:<RESOLVED-VERSION>"
        region = var.aws_region
      }

      source "amazon-ebs" "al2023" {
        ami_name      = local.ami_name
        instance_type = var.instance_type
        region        = var.aws_region

        vpc_id    = var.vpc_id != "" ? var.vpc_id : null
        subnet_id = var.subnet_id != "" ? var.subnet_id : null

        source_ami    = data.amazon-parameterstore.al2023_minimal.value
        ssh_username  = "ec2-user"

        launch_block_device_mappings {
          # ... unchanged ...
        }

        tags = merge(
          # ... unchanged ...
        )
      }

      build {
        sources = ["source.amazon-ebs.al2023"]

        provisioner "ansible" {
          # ... unchanged ...
        }

        # REP-05: emit a manifest after each build. `make packer-bake` parses this
        # to write users/${DEVBOX_USER}.auto.tfvars (which Terraform auto-loads as
        # var.ami_id). See 03-RESEARCH.md Pattern 5 (line 292-346) + Example E
        # (line 537-556).
        post-processor "manifest" {
          output     = "${path.root}/packer-manifest.json"
          strip_path = true
          custom_data = {
            devbox_user = var.devbox_user
            base_ami_id = data.amazon-parameterstore.al2023_minimal.value
          }
        }
      }

    REPLACE `<RESOLVED-VERSION>` with the integer Version field surfaced by the `aws ssm get-parameter-history` call from Step 1 (e.g. `42` or `7` or whatever the live value is). Do NOT leave the literal `<RESOLVED-VERSION>` placeholder — that will fail `packer validate`.

    KEEP these unchanged (lines you do NOT touch in this file):
      - `packer { required_plugins { ... } }` block (lines 1-12)
      - `locals { ... }` (lines 14-17)
      - The `ami_name`, `instance_type`, `region`, `vpc_id`, `subnet_id`, `ssh_username` settings (move `source_ami` into where `source_ami_filter` was)
      - `launch_block_device_mappings` (lines 39-44)
      - `tags = merge(...)` (lines 46-54)
      - `provisioner "ansible"` (lines 60-74)

    Step 3 — Sanity-check the edit:

      cd packer/
      packer init .                 # may be required if amazon-parameterstore data source needs a newer plugin minor
      packer fmt .                  # normalize indentation
      packer validate .             # MUST exit 0; any error here means the SSM path is wrong or the data source syntax slipped

    Step 4 — Optional sanity: the `amazon-parameterstore` data source was added to the Amazon plugin in 1.2.4 (per https://github.com/hashicorp/packer-plugin-amazon/releases). The existing pin `version = ">= 1.3.0"` (line 4) is well above that floor, so no plugin pin bump is needed. If `packer init` warns about an unknown data source, raise the pin to `>= 1.3.0` (already at this floor — no change needed).

    Step 5 — Do NOT run `packer build` in this task. Running the build requires AWS credentials, costs ~$0.50, and produces a non-trivial artifact (an AMI) that this plan does not need to produce — Task 3 verifies the Makefile target shape, not a real bake.
  </action>
  <verify>
    <automated>
      bash -lc '
        set -e
        cd /Users/me/Documents/code/devbox
        # REP-04: no `most_recent = true` anywhere in packer/
        if grep -rE "most_recent[[:space:]]*=[[:space:]]*true" packer/; then
          echo "FAIL: most_recent = true still present"; exit 1
        fi
        # REP-04 positive: SSM parameter data source present with a :NN version pin
        grep -E "amazon-parameterstore" packer/devimage.pkr.hcl >/dev/null || { echo "FAIL: amazon-parameterstore data source missing"; exit 1; }
        grep -E "/aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64:[0-9]+" packer/devimage.pkr.hcl >/dev/null || { echo "FAIL: SSM parameter name missing the :NN version suffix"; exit 1; }
        grep -E "source_ami[[:space:]]*=[[:space:]]*data\\.amazon-parameterstore\\.al2023_minimal\\.value" packer/devimage.pkr.hcl >/dev/null || { echo "FAIL: source_ami not wired to data source"; exit 1; }
        # REP-05 positive: manifest post-processor present
        grep -E "post-processor[[:space:]]+\"manifest\"" packer/devimage.pkr.hcl >/dev/null || { echo "FAIL: manifest post-processor missing"; exit 1; }
        grep -E "packer-manifest\\.json" packer/devimage.pkr.hcl >/dev/null || { echo "FAIL: manifest output path missing"; exit 1; }
        # packer validate must pass (requires packer init)
        cd packer
        packer init . >/dev/null
        packer validate . || { echo "FAIL: packer validate errored"; exit 1; }
        echo "PASS: SSM pin + manifest post-processor configured; packer validate succeeds"
      '
    </automated>
  </verify>
  <done>
    `packer/devimage.pkr.hcl` no longer contains `most_recent = true`; contains an `amazon-parameterstore` data source pinned to `/aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64:<integer>`; `source_ami` references that data source's `.value`; a `post-processor "manifest"` block is present in the `build {}` body writing `${path.root}/packer-manifest.json`; `packer validate` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire AMI handoff — gitignore, packer-manifest, Makefile target, terragrunt.hcl</name>
  <files>
    .gitignore,
    Makefile,
    terragrunt.hcl
  </files>
  <action>
    This task creates the operator-facing `make packer-bake` target and removes the hand-copied AMI ID from `terragrunt.hcl`. No real builds are run here.

    Step 1 — Verify `.gitignore` from Plan 03-01 (Wave 1) preserved the `*.auto.tfvars` rule:

      grep -E "^\*\.auto\.tfvars$" .gitignore || { echo "ERROR: *.auto.tfvars rule missing — Plan 03-01 should have preserved it"; exit 1; }

    Step 2 — Add `packer-manifest.json` to `.gitignore`. The file is per-build artifact, regenerated by every `packer build`, and contains build-time metadata (not secret, but ephemeral and should not be committed). Append to the `# Packer` section of `.gitignore` (top of file, around lines 1-3):

      Current `.gitignore` lines 1-3:
        # Packer
        packer/packer_cache/
        *.pkr.hcl.lock

      Updated `.gitignore` lines 1-4:
        # Packer
        packer/packer_cache/
        packer/packer-manifest.json
        *.pkr.hcl.lock

    Also defensively add `users/*.auto.tfvars` after the existing `*.auto.tfvars` rule (the bare rule already matches, but the explicit form is more discoverable). After line 32 (`*.auto.tfvars`), append:

        users/*.auto.tfvars

    Step 3 — Edit `Makefile`. Three changes:

    Change 3a — Update the `.PHONY` line at line 1 to add `packer-bake`:

      OLD:
        .PHONY: help validate build fmt clean init start stop status devbox-ssm devbox-port-forward devbox-allowlist-me secrets-show tf-init tf-plan tf-apply tf-destroy tg-init tg-reinit tg-plan tg-apply tg-auto-apply tg-destroy tg-auto-destroy

      NEW:
        .PHONY: help validate build fmt clean init start stop status devbox-ssm devbox-port-forward devbox-allowlist-me secrets-show tf-init tf-plan tf-apply tf-destroy tg-init tg-reinit tg-plan tg-apply tg-auto-apply tg-destroy tg-auto-destroy packer-bake

    Change 3b — Update the `help` target to list `packer-bake`. After the existing `build` line in the help block (line 11):

      OLD (help target around lines 9-12):
        @echo "AMI"
        @echo "  init         Install Packer plugins"
        @echo "  validate     Validate Packer and Terraform configs"
        @echo "  build        Build the devimage AMI with Packer"
        @echo "  fmt          Format Packer, Terraform, and Terragrunt files"

      NEW:
        @echo "AMI"
        @echo "  init         Install Packer plugins"
        @echo "  validate     Validate Packer and Terraform configs"
        @echo "  build        Build the devimage AMI with Packer (legacy — prefer packer-bake)"
        @echo "  packer-bake  Build AMI + write users/$$(DEVBOX_USER).auto.tfvars (handoff for tg-apply)"
        @echo "  fmt          Format Packer, Terraform, and Terragrunt files"

    Change 3c — Add the `packer-bake` target. Insert after the existing `build:` target block (after line 50). Use this exact body (adapted from Example E at 03-RESEARCH.md:545-556 with the manifest pre-deletion guard from Pitfall 4 at 03-RESEARCH.md:422-432):

      # --- AMI bake + handoff to Terraform (REP-04, REP-05) ---
      # Replaces the legacy `make build` for the day-to-day flow.
      # 1) Removes any stale manifest (Pitfall 4 in 03-RESEARCH.md).
      # 2) Runs `packer build`, which emits packer/packer-manifest.json.
      # 3) Extracts the AMI ID from the most-recent build entry (sort_by(.build_time)).
      # 4) Writes users/$(DEVBOX_USER).auto.tfvars — auto-loaded by Terraform via
      #    terragrunt's source = "./terraform" symlink. The file is gitignored.
      # Operator flow:  make packer-bake DEVBOX_USER=$$(whoami)  &&  make tg-apply
      packer-bake: init
      	@rm -f packer/packer-manifest.json
      	cd packer && packer build .
      	@AMI_ID=$$(jq -r '\''.builds | sort_by(.build_time) | .[-1].artifact_id | split(":") | .[1]'\'' packer/packer-manifest.json); \
      	  [ -n "$$AMI_ID" ] && [ "$$AMI_ID" != "null" ] || { echo "ERROR: AMI ID missing in packer/packer-manifest.json" >&2; exit 1; }; \
      	  mkdir -p users; \
      	  printf '\''# Generated by `make packer-bake` — do not edit, do not commit (gitignored).\nami_id = "%s"\n'\'' "$$AMI_ID" > users/$(DEVBOX_USER).auto.tfvars; \
      	  echo "Wrote ami_id=$$AMI_ID to users/$(DEVBOX_USER).auto.tfvars"; \
      	  echo "Next: make tg-apply DEVBOX_USER=$(DEVBOX_USER)"

    Tab/indent note: Makefile recipe lines MUST be TAB-indented (the Makefile uses literal tabs throughout — see existing `init:` body at line 43). The `\` line continuations and the embedded `'\''`-quoted jq expression must be preserved exactly.

    Change 3d — Update the `clean` target (current lines 163-165) to also remove the manifest:

      OLD:
        clean:
        	rm -rf packer/packer_cache
        	rm -rf terraform/.terraform terraform/.terraform.lock.hcl

      NEW:
        clean:
        	rm -rf packer/packer_cache
        	rm -f packer/packer-manifest.json
        	rm -rf terraform/.terraform
        	rm -f users/*.auto.tfvars

      CRITICAL: do NOT delete `terraform/.terraform.lock.hcl` in `clean` after Plan 03-01 commits it. Removing it from `clean` is the fix.

    Step 4 — Edit `terragrunt.hcl`. Remove line 45 (`ami_id = "ami-0b7cfe2135f319a55"`) and add a comment explaining the new mechanism. The block transformation (showing the inputs section, lines 43-63 currently):

      OLD inputs block (current lines 43-63):
        inputs = {
          devbox_user = local.user
          ami_id      = "ami-0b7cfe2135f319a55"
          # Per-operator SSH key. Operator must `aws ec2 import-key-pair` once before tg-apply.
          # See `make secrets-show` and Phase 1 docs for the rotation procedure.
          key_name         = "${local.user}-devbox"
          subnet_id        = "subnet-07513680b824b3dbe"
          vpc_id           = "vpc-0dafcc61f21dac9cd"
          instance_type    = "t3.medium"
          root_volume_size = 50
          instance_name    = "devbox"
          aws_region       = "us-east-1"

          # Phase 2: CIDR allowlist ...
          allowed_web_cidrs = local.allowed_web_cidrs
        }

      NEW inputs block:
        # Phase 3 (REP-05): ami_id is no longer set here. `make packer-bake` writes
        # users/${local.user}.auto.tfvars which Terraform auto-loads. If the file is
        # missing, var.ami_id is undefined and `terragrunt plan` errors loudly (per
        # terraform/variables.tf — no default on var.ami_id).
        # See 03-RESEARCH.md Pattern 5 (line 292-346).
        inputs = {
          devbox_user = local.user
          # Per-operator SSH key. Operator must `aws ec2 import-key-pair` once before tg-apply.
          # See `make secrets-show` and Phase 1 docs for the rotation procedure.
          key_name         = "${local.user}-devbox"
          subnet_id        = "subnet-07513680b824b3dbe"
          vpc_id           = "vpc-0dafcc61f21dac9cd"
          instance_type    = "t3.medium"
          root_volume_size = 50
          instance_name    = "devbox"
          aws_region       = "us-east-1"

          # Phase 2: CIDR allowlist for code-server (:8080) and noVNC (:6080).
          # Sourced from CODE_SERVER_ALLOWED_CIDRS / VNC_ALLOWED_CIDRS env vars
          # (comma-separated). If both are unset, this stays [] and Terraform's
          # validation block rejects apply (unless var.allow_open_ingress = true).
          # The file-based path is `make devbox-allowlist-me` which writes
          # allowlist.auto.tfvars (gitignored).
          allowed_web_cidrs = local.allowed_web_cidrs
        }

    KEEP these unchanged: `locals { ... }` block (lines 1-20), `terraform_binary = "tofu"` (line 22), `terraform { source = "./terraform" }` (lines 24-26), `remote_state { ... }` (lines 28-41).

    Step 5 — Smoke-validate Makefile syntax. We cannot run `packer-bake` (requires AWS), but we can confirm make parses the file and surfaces the target:

      make -n packer-bake DEVBOX_USER=test 2>&1 | head -20
      # Expected first non-help lines:
      #   rm -f packer/packer-manifest.json
      #   cd packer && packer build .
      # If make errors with "*** missing separator", indentation is wrong (must be TAB).

      make help | grep packer-bake
      # Expected: one line listing packer-bake in the help text.

    Step 6 — Smoke-validate terragrunt.hcl syntax:

      terragrunt hclfmt --check terragrunt.hcl
      # Or fallback: terragrunt validate (requires AWS creds — skip if no creds, the hclfmt check is sufficient for plan-time)

    Do NOT run `terragrunt plan` or `terragrunt apply` in this task — those require AWS credentials AND a pre-baked AMI. Phase 3 is plan-time pinning only; the first end-to-end test happens when the operator runs `make packer-bake && make tg-apply` after this plan ships.
  </action>
  <verify>
    <automated>
      bash -lc '
        set -e
        cd /Users/me/Documents/code/devbox
        # .gitignore additions
        grep -q "packer/packer-manifest\\.json" .gitignore || { echo "FAIL: packer-manifest.json not gitignored"; exit 1; }
        grep -q "users/\\*\\.auto\\.tfvars" .gitignore || { echo "FAIL: users/*.auto.tfvars not gitignored (defensive entry)"; exit 1; }
        # Makefile: target present, hand-copy removed from terragrunt.hcl
        grep -E "^packer-bake:" Makefile >/dev/null || { echo "FAIL: packer-bake target missing"; exit 1; }
        grep -E "users/\\\$\\(DEVBOX_USER\\)\\.auto\\.tfvars" Makefile >/dev/null || { echo "FAIL: tfvars write missing in Makefile"; exit 1; }
        grep -E "packer-manifest\\.json" Makefile >/dev/null || { echo "FAIL: manifest path missing in Makefile"; exit 1; }
        grep -E "^[[:space:]]*\\.PHONY:.*packer-bake" Makefile >/dev/null || { echo "FAIL: packer-bake not in .PHONY"; exit 1; }
        # clean target no longer removes terraform/.terraform.lock.hcl
        if grep -E "rm.*terraform/\\.terraform\\.lock\\.hcl" Makefile; then
          echo "FAIL: clean target still deletes the lockfile — would break Plan 03-01"; exit 1
        fi
        # terragrunt.hcl: ami_id literal removed from inputs
        if grep -E "^[[:space:]]*ami_id[[:space:]]*=[[:space:]]*\"ami-[0-9a-f]+\"" terragrunt.hcl; then
          echo "FAIL: hand-copied ami_id still present in terragrunt.hcl"; exit 1
        fi
        # Makefile dry-run parses without "missing separator" (TAB indent OK)
        make -n packer-bake DEVBOX_USER=smoketest >/dev/null 2>&1 || { echo "FAIL: make -n packer-bake errored — likely TAB/space mixup"; exit 1; }
        # help target shows packer-bake
        make help 2>/dev/null | grep -q "packer-bake" || { echo "FAIL: packer-bake not advertised in make help"; exit 1; }
        # terragrunt.hcl still parses (hclfmt is non-network)
        terragrunt hclfmt --check terragrunt.hcl >/dev/null 2>&1 || terragrunt hclfmt terragrunt.hcl >/dev/null 2>&1 || { echo "FAIL: terragrunt.hcl no longer parses"; exit 1; }
        echo "PASS: Makefile wired, terragrunt.hcl hand-copy removed, gitignore updated"
      '
    </automated>
  </verify>
  <done>
    `.gitignore` ignores `packer/packer-manifest.json` and `users/*.auto.tfvars`; `Makefile` has a `packer-bake` target (in `.PHONY`, in `help`, with correct TAB indentation, jq parses `.builds | sort_by(.build_time) | .[-1]`, writes `users/$(DEVBOX_USER).auto.tfvars`); `Makefile` `clean` target NO LONGER deletes `terraform/.terraform.lock.hcl`; `terragrunt.hcl` `inputs` block no longer contains a hand-copied `ami_id = "ami-..."`; `make -n packer-bake` parses cleanly; `terragrunt hclfmt --check terragrunt.hcl` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AWS SSM Parameter Store (`/aws/service/...`) → Packer build host | AWS-owned namespace; trust anchored by AWS IAM + the parameter's `:NN` version pin |
| Packer build (root on bake instance) → packer-manifest.json | Bake host writes manifest; operator workstation reads it — manifest content controls which AMI `terragrunt apply` launches |
| packer-manifest.json → users/${DEVBOX_USER}.auto.tfvars | The Makefile `jq` pipeline transforms one into the other on the operator workstation |
| users/${DEVBOX_USER}.auto.tfvars → terragrunt apply (and from there, the operator's AWS account) | A wrong AMI ID here launches the wrong instance — potentially an attacker-controlled AMI if the manifest was tampered with |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | AWS-managed SSM AMI parameter | accept | AWS owns `/aws/service/...` namespace; tamper resistance = AWS account boundary. The `:NN` version pin defends against AWS silently rotating to a newer-malicious image (would require both compromise + version-number reuse, contradicted by SSM parameter store immutability of historical versions) |
| T-03-09 | Tampering | Pinned SSM parameter version is deprecated/disabled | mitigate (operational) | Pitfall 3 at 03-RESEARCH.md:411-420 — document quarterly bake cadence in SUMMARY; deprecation surfaces as `InvalidAMIID.NotFound` immediately on next `make packer-bake`, not a silent compromise |
| T-03-10 | Tampering | packer-manifest.json mutated between `packer build` and `jq` parse | mitigate (low-risk window) | Manifest lives only on the operator's local filesystem; both writer and reader are the same `make packer-bake` invocation in the same shell. Window is sub-second. Defense in depth: the `printf` writes to `users/${DEVBOX_USER}.auto.tfvars` includes a header comment marking the file as generated; manual inspection of `git status` shows nothing committed (gitignored). |
| T-03-11 | Tampering | Stale manifest from prior failed build | mitigate | `rm -f packer/packer-manifest.json` is the FIRST line of the `packer-bake` target (Pitfall 4 at 03-RESEARCH.md:422-432). Belt-and-braces: jq uses `sort_by(.build_time) | .[-1]` so even if pre-delete fails, the latest entry wins. |
| T-03-12 | Information Disclosure | users/${DEVBOX_USER}.auto.tfvars contains an AMI ID | accept | AMI IDs are not secret; they're enumerable across an AWS account via `describe-images`. File is gitignored to avoid noise, not to hide secrets. |
| T-03-13 | Denial of Service | jq missing from operator workstation | accept | jq is already an existing prerequisite (used in `scripts/devbox-status.sh` per STACK.md — see 03-RESEARCH.md:667). Phase 4 DOC-01 will surface the prereq explicitly. |
| T-03-14 | Elevation of Privilege | Wrong AMI launched by terragrunt apply (e.g., manifest pointed at an attacker-controlled AMI) | mitigate | The AMI is produced by the operator's own `packer build` invocation, which uses an AWS-managed SSM parameter as its source. End-to-end the AMI is built from AWS-trusted base + operator-trusted Ansible + operator's own AWS account — no untrusted intermediary. Phase 4 CI grep gate (`! grep -E '^\s*ami_id\s*=\s*"ami-' terragrunt.hcl`) prevents regression to hand-copied IDs of unknown provenance. |
| T-03-15 | Repudiation | "Which AMI is this devbox actually running?" | mitigate | Manifest post-processor's `custom_data` block records `base_ami_id` and `devbox_user`; manifest stays on the bake host's operator workstation in `packer/packer-manifest.json` (overwrites each build but the operator can `git show` historical bakes if they save snapshots elsewhere). AWS EC2 instance tags (set in Packer `tags = merge(...)`) provide the on-instance audit trail. |
</threat_model>

<verification>
- `! grep -rE 'most_recent\s*=\s*true' packer/` (REP-04 anti-pattern eliminated)
- `grep -E '/aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64:[0-9]+' packer/devimage.pkr.hcl` returns 1 match
- `grep -E 'amazon-parameterstore' packer/devimage.pkr.hcl` returns matches (data source + reference)
- `grep -E 'post-processor "manifest"' packer/devimage.pkr.hcl` returns 1 match
- `cd packer && packer validate .` exits 0
- `! grep -E '^\s*ami_id\s*=\s*"ami-[0-9a-f]+"' terragrunt.hcl` (REP-05 hand-copy eliminated)
- `grep -E '^packer-bake:' Makefile` returns 1 match
- `grep -E 'users/\$\(DEVBOX_USER\)\.auto\.tfvars' Makefile` returns 1 match
- `make -n packer-bake DEVBOX_USER=test` parses cleanly (no "missing separator")
- `make help | grep packer-bake` returns 1 line
- `! grep -E 'rm.*terraform/\.terraform\.lock\.hcl' Makefile` (clean target no longer deletes the lockfile)
- `terragrunt hclfmt --check terragrunt.hcl` exits 0
- `grep -q 'packer/packer-manifest\.json' .gitignore`
</verification>

<success_criteria>
- REP-04 closed: `packer/devimage.pkr.hcl` resolves source AMI via a Packer `amazon-parameterstore` data source pinned to `/aws/service/ami-amazon-linux-latest/al2023-ami-minimal-kernel-default-x86_64:<integer>`; the `:NN` makes it a parameter-version pin (the true reproducibility win per 03-RESEARCH.md:262-265)
- REP-05 closed: AMI ID flows Packer → manifest → Makefile → `users/${DEVBOX_USER}.auto.tfvars` → Terraform via auto-load. No hand-copying. `terragrunt.hcl` `inputs` block no longer contains the literal AMI ID
- `packer validate packer/` exits 0
- `make -n packer-bake DEVBOX_USER=test` parses cleanly (Makefile TAB indentation correct)
- `make clean` no longer deletes `terraform/.terraform.lock.hcl` (would break Plan 03-01's REP-01 close)
- `.gitignore` ignores both `packer/packer-manifest.json` and `users/*.auto.tfvars`
- No live AWS calls (no `packer build`, no `terragrunt apply`) — these are operator-driven and tested in Phase 4 / next bake cycle
</success_criteria>

<output>
After completion, create `.planning/phases/03-reproducibility-version-pinning/03-02-SUMMARY.md` covering:
- The resolved SSM parameter version number that was baked into the pin (e.g. `:42`), and the AMI ID it currently resolves to (from Step 1 of Task 1)
- New operator flow:
    1. `make packer-bake DEVBOX_USER=$(whoami)` — builds AMI + writes auto.tfvars
    2. `make tg-apply DEVBOX_USER=$(whoami)` — picks up auto.tfvars
  Replaces the prior 3-step flow: `make build` → manually copy AMI ID from output → paste into `terragrunt.hcl` → `make tg-apply`
- Pin-bump procedure: `aws ssm get-parameter-history --name /aws/service/.../al2023-ami-minimal-kernel-default-x86_64 --region us-east-1` to discover newer versions, then edit the `:NN` literal in `packer/devimage.pkr.hcl` (reviewable diff)
- Phase 4 CI gates needed (per 03-RESEARCH.md:691-700):
    - `packer validate packer/`
    - `grep -E 'most_recent\s*=\s*true' packer/ && exit 1 || exit 0`
    - `grep -E '^\s*ami_id\s*=\s*"ami-' terragrunt.hcl && exit 1 || exit 0`
- Documentation feed for Phase 4 DOC-01 (`CLAUDE.md` operator quickstart):
    - `make packer-bake` is the primary build target (legacy `make build` retained for low-level bake-only without handoff)
    - Quarterly bake recommendation (Pitfall 3 — pinned source AMI deprecation)
    - Multi-operator note: if operator B needs operator A's AMI, B must either (a) re-bake or (b) ask A for the AMI ID and write it into B's own `users/B.auto.tfvars` manually. The single-operator-per-AMI design is per Pattern 5 trade-off at 03-RESEARCH.md:306.
- Operator migration note: existing running devboxes (from before this plan) keep running on their current AMI; the new pinning takes effect on the next `make packer-bake` cycle.
</output>
