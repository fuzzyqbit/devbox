---
phase: 16-chrome-in-the-desktop-role
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/roles/desktop/tasks/main.yml
autonomous: true
requirements: [CHROME-02]

must_haves:
  truths:
    - "A layers.desktop: true bake installs google-chrome-stable from Google's official signed dnf repo via a baked .repo config + imported Google GPG key, with no new layer flag or sub-gate"
    - "GPG verification stays ON: the baked .repo carries gpgcheck=1 and the change introduces no --nogpgcheck / disable_gpg_check: true anywhere"
    - "A layers.desktop: false bake is unchanged — no Google repo config, GPG key, or Chrome package lands outside the desktop layer"
    - "Playbook invariants hold: hardening remains the last role in ansible/playbook.yml, sbom.yml remains the last import, pre-commit + CI grep-gates stay green"
  artifacts:
    - path: "ansible/roles/desktop/tasks/main.yml"
      provides: "Google Chrome install block (rpm_key -> copy .repo -> dnf) as unconditional desktop-role content, appended after the VS Code block"
      contains: "baseurl=https://dl.google.com/linux/chrome/rpm/stable/x86_64"
  key_links:
    - from: "ansible/playbook.yml"
      to: "ansible/roles/desktop/tasks/main.yml"
      via: "role-level gate (role: desktop / when: layers.desktop) — the ONLY gate on the Chrome tasks; no task-level when:/block: wrapper"
      pattern: "when: layers\\.desktop"
    - from: "rpm_key task (Google Linux signing key)"
      to: "dnf install google-chrome-stable"
      via: "key imported into rpm db BEFORE install so signature verification succeeds with disable_gpg_check: false"
      pattern: "key: https://dl\\.google\\.com/linux/linux_signing_key\\.pub"
    - from: "copy task (/etc/yum.repos.d/google-chrome.repo)"
      to: "dnf install google-chrome-stable"
      via: "dnf resolves google-chrome-stable from the [google-chrome] repo id baked by the copy task, gpgcheck=1"
      pattern: "gpgcheck=1"
---

<objective>
Bake Google Chrome into every desktop bake: append a three-task `# --- Google Chrome (CHROME-02) ---`
block (rpm_key -> copy byte-canonical `.repo` -> dnf `google-chrome-stable`) to
`ansible/roles/desktop/tasks/main.yml`, gated ONLY by the pre-existing role-level
`layers.desktop` gate in playbook.yml, then prove the posture statically (lint, grep-gates,
forbidden-string sweep, invariant checks).

Purpose: CHROME-02 — Chrome installs from Google's official signed dnf repo (baked `.repo`
config + GPG key, `gpgcheck=1`) with the airgap GPG posture intact (CLAUDE.md §2/§8).
Runtime launch proof (CHROME-01) is Phase 17's live UAT, not this plan.

Output: One modified file (`ansible/roles/desktop/tasks/main.yml`); zero new files; all
local gates green (pre-commit fast hooks + push-stage ansible-lint + playbook syntax check).

Locked decisions honored (ROADMAP v4.1 / REQUIREMENTS.md — no CONTEXT.md exists):
- Plain part of the desktop role — NO sub-gate flag, no `when:`/`block:` wrapper on the Chrome tasks.
- Latest-at-bake — NO version pin, no version var (Google hosts only current stable; remediation = rebake, SPAL precedent).
- CHROME-03/04 deferred — do NOT add SBOM-capture tasks or bake-asserts (headless --version, W1-style guard). Do not smuggle them in.
- gpgcheck=1 everywhere; hardening stays last role; sbom.yml stays last import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-chrome-in-the-desktop-role/16-RESEARCH.md
@.planning/phases/16-chrome-in-the-desktop-role/16-PATTERNS.md
@CLAUDE.md
@ansible/roles/desktop/tasks/main.yml
@ansible/playbook.yml

Executor notes:
- Commit each task as its OWN standalone Bash call (the repo's block-no-verify hook
  false-positives on chained `git commit` invocations).
- Files that must NOT be touched (16-PATTERNS.md §Reference): `ansible/playbook.yml`,
  `ansible/layer_config.yml`, `ansible/sbom.yml`, `ansible/requirements.yml`, any
  `ansible/roles/desktop/defaults/` file (no version var), and the SPAL `chromium`
  entry at `ansible/roles/desktop/tasks/main.yml:22` (coexists with Chrome — out of scope).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Google Chrome block (CHROME-02) to the desktop role</name>
  <files>ansible/roles/desktop/tasks/main.yml</files>
  <read_first>
    - ansible/roles/desktop/tasks/main.yml (the file being modified — full read; the VS Code block at lines 128-158 is the placement anchor AND the anti-analog: its `when: layers.vscode_desktop` sub-gate at line 136 must NOT be copied; the dconf copy block at lines 34-43 is the inline-`content:` idiom)
    - .planning/phases/16-chrome-in-the-desktop-role/16-RESEARCH.md §Code Examples ("canonical repo file content" + "Recommended task block" — the executor's source for exact content and comments) and §Common Pitfalls 1-3, 5
    - .planning/phases/16-chrome-in-the-desktop-role/16-PATTERNS.md §Pattern Assignments (per-task analogs) and §Anti-analog
    - ansible/roles/dcv/tasks/main.yml lines 21-26 and 83-92 (rpm_key vendor-key idiom; dnf GPG-stance comment style)
    - ansible/roles/xrdp/tasks/main.yml lines 59-72 (repo-sourced dnf install idiom — trim the version-pin Jinja; Chrome is latest-at-bake)
  </read_first>
  <action>
    Append to the END of ansible/roles/desktop/tasks/main.yml (after the VS Code block, line 158)
    a section headed `# --- Google Chrome (CHROME-02) — from Google's official signed dnf repo ---`
    with rationale prose (unconditional desktop content per operator decision — no
    vscode_desktop-style sub-flag; latest-at-bake, remediation = rebake per SPAL precedent
    CLAUDE.md §8; the .repo content is byte-identical to what the Chrome RPM's %post writes so
    the RPM's unconditional overwrite is a content no-op and the daily google-chrome cron finds
    nothing to fix). Then exactly three tasks, per the RESEARCH.md §Code Examples recommended
    block:

    1. `ansible.builtin.rpm_key` — `key: https://dl.google.com/linux/linux_signing_key.pub`,
       `state: present`. NO `fingerprint:` parameter (Pitfall 3: the key file is a multi-key
       bundle — active + obsolete — and ansible-core 2.15's single-fingerprint pin can
       mismatch; matches the MS/NICE key idiom). Document the active fingerprint
       `EB4C 1BFD 4F04 2F6D DDCC EC91 7721 F63B D38B 4796` in a task comment for human
       verification. Task name: "Import the Google Linux signing key (Chrome)".

    2. `ansible.builtin.copy` — `dest: /etc/yum.repos.d/google-chrome.repo`, inline
       `content: |` whose body is EXACTLY the six canonical lines, in order, with NO spaces
       around `=` and NO comments or extra lines inside the content (Pitfalls 1-2 — Chrome's
       %post `cat >` overwrite and its cron's `^baseurl=` grep demand byte-fidelity; this is
       also why `ansible.builtin.yum_repository` is forbidden — it renders `key = value` with
       spaces): line 1 `[google-chrome]`; line 2 `name=google-chrome`; line 3
       `baseurl=https://dl.google.com/linux/chrome/rpm/stable/x86_64`; line 4 `enabled=1`;
       line 5 `gpgcheck=1`; line 6 `gpgkey=https://dl.google.com/linux/linux_signing_key.pub`.
       `owner: root`, `group: root`, `mode: "0644"` (quoted — risky-file-permissions lists
       `copy`). Do NOT add a `repo_gpgcheck=` line (the %post overwrite would strip it; CIS
       1.2.4 already enforces repo_gpgcheck=1 globally at hardening). Task name: "Bake the
       Google Chrome dnf repo config (gpgcheck on)".

    3. `ansible.builtin.dnf` — `name: google-chrome-stable`, `state: present` (NEVER
       `latest` — package-latest lint rule; a fresh AMI makes present = latest-at-bake),
       `disable_gpg_check: false` explicit (the repo-wide idiom: desktop VS Code line 153,
       xrdp line 72, dcv line 92, base line 48). Task name: "Install Google Chrome
       (latest-at-bake from the signed repo)".

    Constraints on the new block:
    - NO `when:` and NO `block:` wrapper on any of the three tasks — gating comes solely from
      `role: desktop / when: layers.desktop | default(false)` in ansible/playbook.yml lines
      69-70 (locked decision; this is what makes a layers.desktop:false bake unchanged).
    - Comment wording must NOT contain these literal strings anywhere in the block: the
      double-dash nogpgcheck dnf flag, `disable_gpg_check: true`, `when:`, or `changeme` —
      each would self-trip a grep acceptance gate or the no-changeme pre-commit hook. Phrase
      the GPG stance as "GPG verification stays ON (CLAUDE.md §2/§8)" instead of quoting the
      forbidden flag (deliberate deviation from the dcv comment that quotes it).
    - FQCN module names, task names start uppercase (ansible-lint v26.4.0 production
      profile, Pitfall 5). No `no_log` needed — no secrets in this block.
    - Touch NOTHING else: no playbook.yml/layer_config.yml/sbom.yml/defaults/requirements.yml
      edits, no new files, no removal of the SPAL `chromium` package (line 22 stays).

    Commit (standalone Bash call, message: `feat(16-01): bake Google Chrome from official
    signed dnf repo into desktop role (CHROME-02)`).
  </action>
  <verify>
    <automated>ansible-playbook ansible/playbook.yml --syntax-check -e @ansible/layer_config.yml && grep -c 'baseurl=https://dl.google.com/linux/chrome/rpm/stable/x86_64' ansible/roles/desktop/tasks/main.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'baseurl=https://dl.google.com/linux/chrome/rpm/stable/x86_64' ansible/roles/desktop/tasks/main.yml` outputs `1` (no spaces around `=`)
    - `grep -c 'dest: /etc/yum.repos.d/google-chrome.repo' ansible/roles/desktop/tasks/main.yml` outputs `1`
    - `grep -c 'gpgkey=https://dl.google.com/linux/linux_signing_key.pub' ansible/roles/desktop/tasks/main.yml` outputs `1` AND `grep -c 'key: https://dl.google.com/linux/linux_signing_key.pub' ansible/roles/desktop/tasks/main.yml` outputs `1`
    - The copy task's `content:` body is exactly the six canonical lines in order ([google-chrome] / name=google-chrome / baseurl=... / enabled=1 / gpgcheck=1 / gpgkey=...) — no extra lines, no comments, no repo_gpgcheck line: `grep -c 'repo_gpgcheck' ansible/roles/desktop/tasks/main.yml` outputs `0`
    - `grep -c 'name: google-chrome-stable' ansible/roles/desktop/tasks/main.yml` outputs `1` and the same task has `state: present` (`grep -c 'state: latest' ansible/roles/desktop/tasks/main.yml` outputs `0`)
    - `grep -cE '^\s*disable_gpg_check: false' ansible/roles/desktop/tasks/main.yml` outputs `2` (anchored — the two matches are the pre-existing VS Code dnf task's `disable_gpg_check: false` param and the new Chrome dnf task's param; note: bare `grep -c` would count 3 because the pre-existing comment at line 131 also contains the literal — do not use the unanchored form)
    - `grep -cE '^\s*when:' ansible/roles/desktop/tasks/main.yml` outputs `1` (only the pre-existing VS Code sub-gate; the Chrome tasks carry no gate — note: bare `grep -c 'when:'` is 3 from two `changed_when:` lines, do not use it)
    - `grep -cE '^\s*fingerprint:' ansible/roles/desktop/tasks/main.yml` outputs `0` (fingerprint documented only in a comment)
    - `grep -vE '^\s*#' ansible/roles/desktop/tasks/main.yml | grep -cE 'nogpgcheck|disable_gpg_check: true'` outputs `0` (comment-filtered; baseline before this task is also 0)
    - `ansible-playbook ansible/playbook.yml --syntax-check -e @ansible/layer_config.yml` exits 0
    - Change footprint: `git show --name-only --format= HEAD | grep '^ansible/'` (after the commit) outputs exactly `ansible/roles/desktop/tasks/main.yml`
  </acceptance_criteria>
  <done>The Chrome block (3 tasks: rpm_key -> copy canonical .repo with gpgcheck=1 -> dnf google-chrome-stable) exists at the end of the desktop role with no task-level gate, playbook syntax-checks clean, and the commit touches only ansible/roles/desktop/tasks/main.yml under ansible/.</done>
</task>

<task type="auto">
  <name>Task 2: Static verification sweep — lint, grep-gates, GPG posture, invariants (CHROME-02)</name>
  <files>ansible/roles/desktop/tasks/main.yml</files>
  <read_first>
    - .pre-commit-config.yaml (hook ids + stages: fast pre-commit hooks incl. `grep-gates` and `no-changeme`; `ansible-lint` at the `pre-push` stage — run it by id, NOT the whole pre-push stage, which would also invoke checkov/packer-validate/tofu_validate)
    - .planning/phases/16-chrome-in-the-desktop-role/16-RESEARCH.md §Verification commands + §Common Pitfalls 5 and 7
    - CLAUDE.md §8 (invariants list — hardening-last, sbom-last, no-changeme; the grep-gates hook is the mechanical enforcement)
  </read_first>
  <action>
    Run the full static gate set locally (no AWS needed — the bake itself is the operator's
    live session, composing with the Phase-17 UAT backlog):

    1. `pre-commit run --all-files` — fast hooks: grep-gates (mechanically enforces
       hardening-last-role and sbom-last-import), no-changeme, gitleaks, check-yaml,
       end-of-file-fixer, trailing-whitespace, shellcheck, tofu_fmt, packer-fmt.
    2. `pre-commit run --hook-stage pre-push ansible-lint` — the pinned v26.4.0 production
       profile (authoritative over any locally-installed ansible-lint). Expected clean per
       RESEARCH Pitfall 5 checklist: FQCN modules, uppercase task names, quoted mode "0644"
       on copy, state: present. If it reports findings, fix them ONLY within the Chrome
       block, re-run, and re-check all Task 1 acceptance grep counts still hold (a fix must
       not alter the canonical .repo bytes or add a gate). No handlers exist in this change,
       so the ansible-lint --fix handler-rename gotcha does not apply.
    3. Re-run `ansible-playbook ansible/playbook.yml --syntax-check -e @ansible/layer_config.yml`.
    4. GPG-posture sweep (comment-filtered per grep-gate hygiene):
       `grep -vE '^\s*#' ansible/roles/desktop/tasks/main.yml | grep -cE 'nogpgcheck|disable_gpg_check: true'`
       must output 0.
    5. Untouched-files proof (success criteria 3 and 4 rest on this):
       `git status --porcelain ansible/playbook.yml ansible/layer_config.yml ansible/sbom.yml ansible/requirements.yml ansible/roles/desktop/defaults/` outputs nothing, and no commit in
       this phase lists any of those paths (`git log --format= --name-only HEAD~2..HEAD | grep -E 'ansible/(playbook|layer_config|sbom|requirements)'` outputs nothing — adjust the
       rev range to cover exactly this phase's commits). The desktop role-level gate is
       intact: `grep -A1 'role: desktop' ansible/playbook.yml` still shows
       `when: layers.desktop | default(false)`.

    If any fixes were required, commit them (standalone Bash call, message:
    `fix(16-01): lint/posture fixes for Chrome desktop block`). If everything passes with no
    changes, no commit is needed for this task.
  </action>
  <verify>
    <automated>pre-commit run --all-files && pre-commit run --hook-stage pre-push ansible-lint && ansible-playbook ansible/playbook.yml --syntax-check -e @ansible/layer_config.yml</automated>
  </verify>
  <acceptance_criteria>
    - `pre-commit run --all-files` exits 0 (grep-gates green — hardening still last role, sbom.yml still last import, no changeme literal, no retired make targets)
    - `pre-commit run --hook-stage pre-push ansible-lint` exits 0
    - `ansible-playbook ansible/playbook.yml --syntax-check -e @ansible/layer_config.yml` exits 0
    - `grep -vE '^\s*#' ansible/roles/desktop/tasks/main.yml | grep -cE 'nogpgcheck|disable_gpg_check: true'` outputs `0`
    - `git status --porcelain ansible/playbook.yml ansible/layer_config.yml ansible/sbom.yml ansible/requirements.yml` outputs nothing, and this phase's commits contain no path other than `ansible/roles/desktop/tasks/main.yml` under `ansible/`
    - `grep -A1 'role: desktop' ansible/playbook.yml` output includes `when: layers.desktop | default(false)` (unchanged role-level gate)
    - All Task 1 grep acceptance counts still hold after any lint fixes (spot-recheck: baseurl count `1`, `^\s*when:` count `1`, anchored `grep -cE '^\s*disable_gpg_check: false'` count `2`)
  </acceptance_criteria>
  <done>All local gates are green under the pinned tool versions, the GPG posture sweep is clean, the change footprint is exactly one file, and both playbook invariants are mechanically confirmed — the four Phase 16 success criteria are statically proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bake host -> dl.google.com | HTTPS fetch of the Google signing key, repo metadata, and the Chrome RPM at bake time — vendor supply chain crosses here |
| baked image -> dl.google.com (runtime) | Chrome's own `/etc/cron.daily/google-chrome` keeps key/repo healthy and dnf may update Chrome on live instances |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Chrome RPM / repo metadata in transit or via mirror compromise | mitigate | `gpgcheck=1` in the baked `.repo` + `rpm_key` pre-import of the Google signing key (active fingerprint EB4C 1BFD 4F04 2F6D DDCC EC91 7721 F63B D38B 4796, google.com/linuxrepositories); Google serves signed `repomd.xml.asc` [VERIFIED in RESEARCH]; CIS 1.2.4 sets global `repo_gpgcheck=1` at hardening (pre-existing) |
| T-16-02 | Spoofing | key/baseurl URL substitution (lookalike domain) | mitigate | Bake the exact canonical URLs verified against the Chromium installer source (16-RESEARCH.md §Sources); HTTPS only; fingerprint documented in the task comment for human verification |
| T-16-03 | Tampering | GPG-bypass creep in future edits to the role | mitigate | Task 2 comment-filtered grep gate (0 hits for the nogpgcheck flag / `disable_gpg_check: true` in the role file); CLAUDE.md §8 posture + CI grep-gates |
| T-16-04 | Elevation of Privilege | Browser attack surface on the hardened image (SUID `/opt/google/chrome/chrome-sandbox`) | accept | Same class as the existing SPAL `chromium` (operator-accepted); CIS 6.1.12 SUID review is audit-only; runtime sandbox/AVC proof is Phase 17's explicit criterion (CHROME-01) |
| T-16-05 | Tampering | Latest-at-bake version drift; Chrome cron may dnf-update at runtime | accept | Locked operator decision (ROADMAP v4.1): remediation = rebake (SPAL precedent); existing SBOM pass inventories the exact baked version as ordinary behavior |
| T-16-SC | Tampering | Package supply chain (installs) | mitigate | rpm-ecosystem legitimacy audit completed in 16-RESEARCH.md §Package Legitimacy Audit — `google-chrome-stable` Approved [VERIFIED: repo metadata + .asc probes + official key page]; transitive deps from Amazon-signed AL2023 core; NO npm/pip/cargo installs in this phase, so no slopcheck human checkpoint is required |

No high-severity unmitigated threats; both accepted threats are documented operator decisions.
</threat_model>

<verification>
Phase-level static checks (maps 1:1 to the four ROADMAP Phase 16 success criteria; runtime
launch proof is deliberately Phase 17):

1. **Chrome installs from Google's signed repo, no new flag (criterion 1):** the three-task
   block exists in `ansible/roles/desktop/tasks/main.yml` with rpm_key + canonical `.repo`
   (`baseurl=https://dl.google.com/linux/chrome/rpm/stable/x86_64`) + dnf
   `google-chrome-stable`, and `grep -cE '^\s*when:'` on the file is still `1` (no sub-gate).
2. **GPG on (criterion 2):** `.repo` content carries `gpgcheck=1`;
   `grep -vE '^\s*#' ansible/roles/desktop/tasks/main.yml | grep -cE 'nogpgcheck|disable_gpg_check: true'` = 0;
   the dnf task states `disable_gpg_check: false` explicitly.
3. **desktop:false unchanged (criterion 3):** all Chrome tasks live inside the desktop role
   (skipped wholesale when `layers.desktop` is false); `ansible/playbook.yml` and
   `ansible/layer_config.yml` are untouched (`git status --porcelain` empty; role gate line
   `when: layers.desktop | default(false)` intact).
4. **Invariants green (criterion 4):** `pre-commit run --all-files` (grep-gates: hardening
   last role, sbom.yml last import) and `pre-commit run --hook-stage pre-push ansible-lint`
   both exit 0; `ansible-playbook ansible/playbook.yml --syntax-check -e @ansible/layer_config.yml` exits 0.
</verification>

<success_criteria>
- CHROME-02 delivered: baked `.repo` config + Google GPG key + `gpgcheck=1` install of
  `google-chrome-stable`, all inside `ansible/roles/desktop/tasks/main.yml`, gated only by
  `layers.desktop`.
- Zero occurrences (comment-filtered) of the nogpgcheck flag or `disable_gpg_check: true`
  in the change; `disable_gpg_check: false` explicit on the new dnf task.
- Change footprint is exactly one file; playbook.yml / layer_config.yml / sbom.yml /
  requirements.yml / desktop defaults untouched; SPAL `chromium` entry untouched.
- No CHROME-03/04 smuggling: no SBOM-capture tasks, no bake-asserts, no version-pin var.
- All local gates green: pre-commit fast hooks, push-stage ansible-lint (v26.4.0 production
  profile), playbook syntax check.
</success_criteria>

<output>
Create `.planning/phases/16-chrome-in-the-desktop-role/16-01-SUMMARY.md` when done.
</output>
