---
phase: 12-network-operator-surface-vnc-novnc-removal
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - ansible/novnc-plain-username-fix.yml
  - ansible/playbook.yml
  - ansible/firewalld-docker-fix.yml
  - ansible/roles/xrdp/tasks/main.yml
autonomous: true
requirements: [RDP-12, RDP-09]
must_haves:
  truths:
    - "The noVNC username-injection workaround playbook is deleted and no longer imported"
    - "The hardening role remains the LAST role in playbook.yml (grep-gate prints 1)"
    - "The xrdp role opens host firewalld :3389/tcp idempotently, safe under both containers:true (docker zone) and containers:false (public zone)"
    - "The firewalld :3389 task is inside the xrdp role (already before hardening) — no new trailing role/import"
    - "The firewalld-docker-fix.yml comment no longer advertises noVNC :6080"
  artifacts:
    - path: "ansible/playbook.yml"
      provides: "novnc-plain-username-fix import removed; hardening still last; firewalld-docker-fix import kept"
    - path: "ansible/roles/xrdp/tasks/main.yml"
      provides: "idempotent firewalld --add-port=3389/tcp task guarded on firewalld presence"
      contains: "3389"
  key_links:
    - from: "ansible/playbook.yml"
      to: "hardening role position"
      via: "hardening is the last `- role:` entry"
      pattern: "role:[\\s]*hardening"
---

<objective>
Revert the noVNC username-injection workaround (RDP-12, commit 29de35b) — delete its standalone
playbook and drop its import — and close the `containers:false` host-firewall edge case by
adding an idempotent firewalld `:3389/tcp` allow inside the `xrdp` role (RDP-09-adjacent
robustness, routed here from Phase 11's adversarial review). The workaround revert follows the
project's "kludge in its own named playbook" convention in reverse. The firewalld task goes in
the xrdp role (already positioned BEFORE hardening) — never as a new trailing role/import — so
the hardening-stays-last invariant holds.

Purpose: The obsolete noVNC workaround is gone with its import; the host firewall permits
:3389 even on a `containers:false` bake (where the stock `public` zone would otherwise drop it),
matching the SG ingress opened in plan 12-01. The default `containers:true` build is already
permissive via the docker-zone workaround — this task is belt-and-braces for the edge case.

Output: deleted `ansible/novnc-plain-username-fix.yml`, edited `ansible/playbook.yml` (import
dropped), edited `ansible/firewalld-docker-fix.yml` (comment), and a new idempotent firewalld
:3389 task in `ansible/roles/xrdp/tasks/main.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-network-operator-surface-vnc-novnc-removal/12-RESEARCH.md

<interfaces>
<!-- Exact targets (research rows P1-P3 + "Host firewall" section + "RDP-12 Workaround Revert"). -->

ansible/playbook.yml:113-122 — the two trailing import_playbook entries:
  113-116  FIXME comment + `- import_playbook: firewalld-docker-fix.yml`  (KEEP — leave intact)
  118-122  FIXME comment block + `- import_playbook: novnc-plain-username-fix.yml`  (DELETE all 5 lines — P2)
ansible/playbook.yml:59-66 — the xrdp role (before hardening) + hardening role (line 65, MUST stay last).
  The grep gate: the LAST `- role:` line must be `hardening`. Deleting an import_playbook (not a
  role) does NOT change role order — but re-assert the gate after editing (Pitfall 5).

ansible/novnc-plain-username-fix.yml — DELETE the whole 86-LOC file (P1, git rm).

ansible/firewalld-docker-fix.yml:6 — the comment "(noVNC :6080, code-server :8080)" (P3):
  reword to drop noVNC :6080 — e.g. "(code-server :8080, RDP :3389)". The play BODY opens no
  specific port (it sets the default zone to docker), so this is comment-only — NO functional change.

ansible/roles/xrdp/tasks/main.yml — ADD the firewalld :3389 task. The role currently has NO
  firewalld handling (grep returned nothing). Insert the new task in the role's main.yml (it is
  already gated by the playbook to run before hardening). The xrdp service units are `xrdp` +
  `xrdp-sesman` (files xrdp.service / xrdp-sesman.service). Reference: the existing
  ansible/firewalld-docker-fix.yml shows the project's firewalld command style
  (firewall-cmd ...). hardening role disables CIS host-firewall rules (firewalld present for CIS
  scans), so firewalld is INSTALLED; under containers:false the default zone is `public`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert the noVNC username-injection workaround (RDP-12) — delete the file + drop its import; reword the firewalld-docker-fix comment</name>
  <read_first>
    - ansible/novnc-plain-username-fix.yml (the whole file — confirm it is the workaround, header dated 2026-06-15)
    - ansible/playbook.yml (lines 56-66 role order incl. hardening-last; lines 113-122 the two imports)
    - ansible/firewalld-docker-fix.yml (lines 1-10 header comment, esp. line 6)
    - .planning/phases/12-network-operator-surface-vnc-novnc-removal/12-RESEARCH.md ("RDP-12 Workaround Revert — Exact Steps" + inventory P1/P2/P3 + "Hook/Invariant/Grep-Gate Interactions -> grep-gates" + Pitfall 5)
  </read_first>
  <action>
    (P1) git rm ansible/novnc-plain-username-fix.yml — delete the entire 86-LOC workaround file.
    (P2) In ansible/playbook.yml, DELETE the trailing 5-line block (current lines 118-122): the
    "# FIXME: see novnc-plain-username-fix.yml header ..." comment AND the
    `- import_playbook: novnc-plain-username-fix.yml` line. KEEP the firewalld-docker-fix import
    block above it (lines 113-116) intact. Do NOT touch the roles list or the post_tasks — the
    hardening role MUST remain the last `- role:` entry (deleting an import_playbook does not move
    it, but verify).
    (P3) In ansible/firewalld-docker-fix.yml line 6, reword the parenthetical "(noVNC :6080,
    code-server :8080)" to "(code-server :8080, RDP :3389)". Comment-only — the play body (which
    sets the default zone to docker) is unchanged.
    After edits, re-assert the hardening-stays-last grep gate.
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox && ! test -f ansible/novnc-plain-username-fix.yml && ! grep -q novnc-plain-username-fix ansible/playbook.yml && ! grep -Eqi '6080|novnc' ansible/firewalld-docker-fix.yml && grep -q firewalld-docker-fix ansible/playbook.yml && [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    REMOVAL PROVEN (! test / ! grep):
    - `! test -f ansible/novnc-plain-username-fix.yml` (workaround file deleted — RDP-12)
    - `! grep -q novnc-plain-username-fix ansible/playbook.yml` (import dropped — RDP-12)
    - `! grep -Eqi '6080|novnc' ansible/firewalld-docker-fix.yml` (no noVNC :6080 in the comment)
    SURVIVING / INVARIANT INTACT (positive grep):
    - `grep -q firewalld-docker-fix ansible/playbook.yml` (the firewalld-docker-fix import is KEPT)
    - INVARIANT: `[ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ]` (hardening is the last role — grep-gate #9 prints 1; Pitfall 5)
    - `grep -q 'role: xrdp' ansible/playbook.yml` (xrdp role still wired before hardening)
  </acceptance_criteria>
  <done>novnc-plain-username-fix.yml deleted and unimported; firewalld-docker-fix import kept; firewalld-docker-fix comment scrubbed of noVNC :6080; hardening still last (gate=1).</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotent host-firewalld :3389/tcp allow in the xrdp role (RDP-09-adjacent; both container modes)</name>
  <read_first>
    - ansible/roles/xrdp/tasks/main.yml (the systemd-units + SELinux section ~349-410 — a sensible place to append the firewall task; the role runs before hardening)
    - ansible/firewalld-docker-fix.yml (lines 53-87 — the project's firewall-cmd command style + the firewalld-present assumption)
    - ansible/roles/hardening/defaults/main.yml (the CIS host-firewall rules are disabled; firewalld stays installed for CIS scans)
    - .planning/phases/12-network-operator-surface-vnc-novnc-removal/12-RESEARCH.md ("Host firewall (firewalld) — already handled ... one edge case" + "Open Questions" Q1 + Pitfall 5 + Assumptions A3)
  </read_first>
  <action>
    Add an idempotent task (or short task pair) to ansible/roles/xrdp/tasks/main.yml that opens
    :3389/tcp on the host firewall, robust under BOTH container modes:
    - Guard so it no-ops when firewalld is absent/inactive (e.g. check `command -v firewall-cmd`
      or stat the binary / query `firewall-cmd --state` with failed_when:false, and run the add
      only when firewalld is present and running). Do NOT hard-fail a host without firewalld.
    - Open 3389/tcp permanently AND in the runtime, then reload — mirror the project's existing
      firewall-cmd command style from firewalld-docker-fix.yml (command module + firewall-cmd;
      no community.general dependency, consistent with the SELinux semanage approach in this same
      role). Make it idempotent: query whether 3389/tcp is already allowed (e.g.
      `firewall-cmd --query-port=3389/tcp`) and skip/changed_when accordingly, OR use
      `--add-port=3389/tcp --permanent` which is idempotent and a `changed_when` guard.
    - Under containers:true the default zone is `docker` (target=ACCEPT, already permissive) — the
      add applies to the active/default zone and is harmless. Under containers:false the default
      zone is `public` and this add is what makes :3389 reachable host-side. Adding to the
      default zone covers both.
    CRITICAL (Pitfall 5): the task MUST live INSIDE the xrdp role (already before hardening) —
    do NOT add a new role or a new trailing import_playbook (that would break hardening-last).
    Re-run ansible-lint and re-assert the hardening grep gate after editing.
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox && grep -q 3389 ansible/roles/xrdp/tasks/main.yml && grep -qi 'firewall' ansible/roles/xrdp/tasks/main.yml && [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ] && grep -q 'param=/usr/libexec/Xorg' ansible/roles/xrdp/templates/sesman.ini.j2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    ADDED + GUARDED:
    - `grep -q 3389 ansible/roles/xrdp/tasks/main.yml` AND `grep -qi 'firewall' ansible/roles/xrdp/tasks/main.yml` (firewalld :3389 task present in the xrdp role)
    - `grep -qiE 'firewall-cmd --state|command -v firewall-cmd|firewalld' ansible/roles/xrdp/tasks/main.yml` (a presence/active guard exists — no hard-fail when firewalld is absent)
    INVARIANT / SURVIVING INTACT:
    - INVARIANT: `[ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ]` (hardening still last — the firewall task is IN the xrdp role, not a new trailing role; Pitfall 5)
    - SURVIVING-FEATURE CHECK: `grep -q 'param=/usr/libexec/Xorg' ansible/roles/xrdp/templates/sesman.ini.j2` (the xrdp Xorg backend config is untouched — removal did not bleed into xrdp)
    - SURVIVING-FEATURE CHECK: `grep -q 'name: xrdp' ansible/roles/xrdp/tasks/main.yml` (the existing xrdp service enable tasks are intact)
    - `ansible-lint ansible/playbook.yml` passes (or CI-authoritative note per STATE.md if .ansible-lint is the known-broken config)
  </acceptance_criteria>
  <done>xrdp role opens host firewalld :3389/tcp idempotently and guarded (no-op when firewalld absent), safe under both container modes; task lives inside xrdp (before hardening); hardening still last; xrdp Xorg config untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator network -> host firewalld -> service | Under containers:false the host `public` zone would drop :3389 despite the SG allowing it; this plan opens it host-side. |
| Ansible playbook role/import ordering | hardening must run LAST to lock down what prior roles opened; the firewall task is placed in the xrdp role (before hardening) so hardening can still re-tighten the host posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering/Elevation | A new trailing role/import for the firewall task could break hardening-last (re-open locked-down config) | mitigate | The firewall task is added INSIDE the existing xrdp role (already before hardening); the hardening grep-gate is re-asserted in both task acceptance criteria (=1). No new role/import (Pitfall 5). |
| T-12-10 | Info-disclosure | Host firewalld opening :3389 too widely | accept | firewalld opens the host port; the actual perimeter is the EC2 SG (gated on allowed_web_cidrs, plan 12-01). The hardening role's stance is "EC2 SGs provide perimeter security" — host firewalld is permissive by design (docker-zone workaround). Net posture unchanged vs the prior :6080/:8080 host-side reachability. |
| T-12-11 | DoS | firewall-cmd hard-failing the bake on a host without firewalld | mitigate | The task is guarded on firewalld presence/active state (failed_when:false / command -v guard) — it no-ops rather than aborting the bake. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs. firewalld is already installed by the CIS/firewalld-docker-fix path; the xrdp task only opens a port. Airgap policy unaffected (no new package). |
</threat_model>

<verification>
After both tasks:
```bash
# RDP-12 revert + RDP-09-adjacent host firewall; invariant + xrdp survivors intact.
! test -f ansible/novnc-plain-username-fix.yml && ! grep -q novnc-plain-username-fix ansible/playbook.yml && echo "RDP-12 reverted OK"
grep -q firewalld-docker-fix ansible/playbook.yml && ! grep -Eqi '6080|novnc' ansible/firewalld-docker-fix.yml && echo "firewalld-docker-fix kept + scrubbed OK"
grep -q 3389 ansible/roles/xrdp/tasks/main.yml && grep -qi firewall ansible/roles/xrdp/tasks/main.yml && echo "host firewalld :3389 task OK"
[ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ] && echo "hardening-last gate=1 OK"
grep -q 'param=/usr/libexec/Xorg' ansible/roles/xrdp/templates/sesman.ini.j2 && echo "xrdp Xorg backend untouched OK"
ansible-lint ansible/playbook.yml || echo "ansible-lint: CI authoritative if .ansible-lint is the known-broken config (STATE.md)"
```
</verification>

<success_criteria>
- novnc-plain-username-fix.yml deleted and its import dropped (RDP-12); firewalld-docker-fix import kept
- firewalld-docker-fix comment scrubbed of noVNC :6080
- xrdp role opens host firewalld :3389/tcp idempotently + guarded, both container modes (RDP-09-adjacent)
- hardening remains the last role (grep-gate=1); firewall task is in the xrdp role, no new trailing role/import
- xrdp Xorg backend config + service enables untouched
</success_criteria>

<output>
Create `.planning/phases/12-network-operator-surface-vnc-novnc-removal/12-04-SUMMARY.md` when done
</output>
