---
phase: 06-gitlab-ci-polish
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [.gitlab-ci.yml, .pre-commit-config.yaml, run]
autonomous: true
requirements: [CI-01, CI-02, CI-03, CI-04]

must_haves:
  truths:
    - "GitLab CI bake stage runs ./run build instead of inline packer commands"
    - "GitLab CI deploy stage runs ./run tf-init and ./run tf-auto-apply instead of inline tofu commands"
    - "GitLab CI shellcheck job lints the run file alongside scripts/*.sh"
    - "GitLab CI grep-gates job checks invariant #8 (run executable bit)"
    - "Pre-commit grep-gates hook checks run executable bit (parity with CI)"
    - "./run tf-init adds -lockfile=readonly when CI=true"
  artifacts:
    - path: ".gitlab-ci.yml"
      provides: "Updated bake, deploy, shellcheck, and grep-gates jobs"
      contains: "./run build"
    - path: ".pre-commit-config.yaml"
      provides: "Updated grep-gates hook with invariant #8"
      contains: "run.*100755"
    - path: "run"
      provides: "CI-aware -lockfile=readonly in cmd_tf_init"
      contains: "lockfile=readonly"
  key_links:
    - from: ".gitlab-ci.yml (bake:packer-build)"
      to: "run (cmd_build)"
      via: "./run build in script block"
      pattern: "./run build"
    - from: ".gitlab-ci.yml (deploy:tofu-apply)"
      to: "run (cmd_tf_init, cmd_tf_auto_apply)"
      via: "./run tf-init and ./run tf-auto-apply in script block"
      pattern: "./run tf-init"
    - from: ".gitlab-ci.yml (validate:grep-gates)"
      to: "run file in git index"
      via: "git ls-files -s run checks executable bit"
      pattern: "git ls-files -s run"
---

<objective>
Wire the GitLab CI pipeline to call `./run` for bake and deploy, extend shellcheck and grep-gates to cover the `run` file, and add CI-aware `-lockfile=readonly` to `./run tf-init`.

Purpose: CI-01 through CI-04 eliminate inline shell duplication between CI and the run script, establishing `./run` as the single source of truth for all operator commands in both local and CI contexts.

Output: Updated `.gitlab-ci.yml`, `.pre-commit-config.yaml`, and `run` files.
</objective>

<execution_context>
@/Users/me/.claude/get-shit-done/workflows/execute-plan.md
@/Users/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gitlab-ci-polish/06-RESEARCH.md
@.planning/phases/05-run-script-core/05-01-SUMMARY.md
@.planning/phases/06-gitlab-ci-polish/06-01-SUMMARY.md

<interfaces>
<!-- Current CI job structures that will be modified -->

From .gitlab-ci.yml (line 363-377): bake:packer-build
```yaml
bake:packer-build:
  stage: bake
  extends: .aws-auth
  image: hashicorp/packer:1.15.3@sha256:cb9c526a...
  script:
    - cd packer
    - packer init .
    - packer build -var "devbox_user=" -var "aws_region=${AWS_REGION}" .
  rules: [...]
```

From .gitlab-ci.yml (line 401-450): deploy:tofu-apply
```yaml
deploy:tofu-apply:
  stage: deploy
  extends: .aws-auth
  image: ghcr.io/opentofu/opentofu:1.10.6@sha256:43f73c1e...
  environment:
    name: devbox/${DEVBOX_USER}
  before_script: [DEVBOX_USER fail-fast + !reference .aws-auth]
  script:
    - cd terraform
    - tofu init -lockfile=readonly -backend-config=...
    - tofu plan -var ... -out=tfplan
    - tofu apply -auto-approve tfplan
  artifacts:
    paths: [terraform/tfplan]
```

From .gitlab-ci.yml (line 230-234): validate:shellcheck
```yaml
validate:shellcheck:
  stage: validate
  image: koalaman/shellcheck-alpine:v0.10.0@sha256:7c6a5115...
  script:
    - shellcheck scripts/*.sh
```

From .gitlab-ci.yml (line 253-296): validate:grep-gates
Ends with: echo "All 7 invariants pass."

From .pre-commit-config.yaml (line 96-123): grep-gates hook
6 invariants, no run executable bit check.

From run (line 122-127): cmd_tf_init
```bash
cmd_tf_init() {
  _require_devbox_user
  _derive_tf_state_bucket
  # shellcheck disable=SC2046
  (cd "$REPO_ROOT/terraform" && "$TF_BIN" init $(_tf_backend_args))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CI-aware lockfile flag to ./run tf-init</name>
  <files>run</files>
  <read_first>
    - run (full file — current state after Plan 01 modifications)
    - .planning/phases/06-gitlab-ci-polish/06-RESEARCH.md (Pattern 3: CI-Aware Init Flags, Pitfall 4: lockfile-readonly)
  </read_first>
  <action>
Modify `cmd_tf_init` in the `run` script to add `-lockfile=readonly` when running in CI:

In `cmd_tf_init`, between the `_derive_tf_state_bucket` call and the `(cd ... init ...)` subshell:
- Declare `local lockfile_flag=""`.
- If `CI` env var equals "true", set `lockfile_flag="-lockfile=readonly"`.
- Pass `$lockfile_flag` before `$(_tf_backend_args)` in the init command. The shellcheck SC2046 disable already covers the word-splitting.

Also apply the same pattern to `cmd_tf_reinit` (which also runs `init` and should enforce readonly lockfile in CI to prevent `-reconfigure` from rewriting the lockfile in CI).

Do NOT modify `cmd_tf_ensure_init` — that function calls `cmd_tf_reinit` which will inherit the behavior.
  </action>
  <verify>
    <automated>shellcheck run && grep -c 'lockfile=readonly' run</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck run` exits 0
    - `grep -c 'lockfile=readonly' run` returns exactly 2 (one in cmd_tf_init, one in cmd_tf_reinit)
    - `grep -A5 'cmd_tf_init()' run` contains the string `CI.*true.*lockfile`
    - `grep -A5 'cmd_tf_reinit()' run` contains the string `CI.*true.*lockfile`
  </acceptance_criteria>
  <done>
    - cmd_tf_init adds -lockfile=readonly when CI=true (Phase 3 REP-01 enforcement in CI)
    - cmd_tf_reinit adds -lockfile=readonly when CI=true
    - shellcheck passes clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire GitLab CI bake, deploy, shellcheck, and grep-gates to use ./run</name>
  <files>.gitlab-ci.yml, .pre-commit-config.yaml</files>
  <read_first>
    - .gitlab-ci.yml (full file — all job definitions, especially bake:packer-build, deploy:tofu-apply, validate:shellcheck, validate:grep-gates)
    - .pre-commit-config.yaml (full file — especially grep-gates hook at line 96-123)
    - .planning/phases/06-gitlab-ci-polish/06-RESEARCH.md (CI Bake Integration, CI Deploy Integration, Shellcheck Integration, Grep-Gate Integration, Pitfall 2-6)
  </read_first>
  <action>
Modify `.gitlab-ci.yml` with these four changes:

A. BAKE JOB (CI-01) — `bake:packer-build`:
   - Add a `variables:` block to the job with `PKR_VAR_aws_region: ${AWS_REGION}`. This passes the region to Packer via its `PKR_VAR_*` env var convention.
   - Replace the 3-line script block (`cd packer`, `packer init .`, `packer build -var ...`) with a single line: `- ./run build`.
   - Preserve the existing `rules:` block unchanged.
   - Update the preceding comment block to note that `PKR_VAR_aws_region` threads the CI region variable through to Packer, and that `devbox_user` defaults to "" in `variables.pkr.hcl`.

B. DEPLOY JOB (CI-02) — `deploy:tofu-apply`:
   - Replace the `script:` block (currently 4 lines: `cd terraform`, `tofu init -lockfile=readonly ...`, `tofu plan ... -out=tfplan`, `tofu apply -auto-approve tfplan`) with 2 lines: `- ./run tf-init` and `- ./run tf-auto-apply`.
   - The `-lockfile=readonly` flag is now handled by `./run tf-init` when `CI=true` (Task 1 of this plan).
   - The `-var` args are handled by `./run` internally via `_tf_var_args`.
   - The `-backend-config` args are handled by `./run` internally via `_tf_backend_args`.
   - The DEVBOX_USER, TF_STATE_BUCKET, TF_STATE_REGION, TF_STATE_LOCK_TABLE, TF_STATE_KEY variables are already defined at the pipeline level and will be picked up by `./run` from the environment.
   - Remove the `artifacts:` block (paths: terraform/tfplan). The plan-file workflow is dropped per RESEARCH.md recommendation — `./run tf-auto-apply` does not produce a tfplan file. The `when: manual` gate + protected branch + OIDC + DEVBOX_USER validation provide sufficient defense-in-depth.
   - Preserve the `before_script:` block (DEVBOX_USER fail-fast + .aws-auth reference) unchanged — this is defense-in-depth per RESEARCH.md Open Question 3.
   - Preserve the `rules:` block unchanged.
   - Update the preceding comment block: note that the plan-file workflow is dropped in favor of `./run tf-auto-apply` (simpler, defense-in-depth sufficient without plan artifact), and that `./run tf-init` auto-adds `-lockfile=readonly` when `CI=true` (Phase 3 REP-01).

C. SHELLCHECK JOB (CI-03) — `validate:shellcheck`:
   - Change the script line from `- shellcheck scripts/*.sh` to `- shellcheck scripts/*.sh run`.
   - The `run` file has a `#!/usr/bin/env bash` shebang so shellcheck detects it as a shell script regardless of the missing `.sh` extension.

D. GREP-GATES JOB (CI-04) — `validate:grep-gates`:
   - Add invariant #8 after the existing invariant #7 block and before the final echo line. The new invariant:
     ```
     # 8. run file must have executable bit in git (RUN-07 / CI-04)
     git ls-files -s run | grep -q '^100755' || {
       echo "FAIL: run file does not have executable bit in git (must be 100755)" >&2
       exit 1
     }
     ```
   - Update the final echo from `echo "All 7 invariants pass."` to `echo "All 8 invariants pass."`.

Modify `.pre-commit-config.yaml` (CI-04 parity, Pitfall 6):
   - In the `grep-gates` hook entry block, add invariant #7 (which becomes the pre-commit hook's 7th check — note: CI has been at 7 invariants, pre-commit has 6 since it does not have the image digest-pin check which is CI-only). Add after the existing invariant #6 (`! grep -E "^[^#]*terraform/\\.terraform\\.lock\\.hcl" .gitignore`):
     ```
     # 7. run file must have executable bit in git (RUN-07 / CI-04)
     git ls-files -s run | grep -q "^100755"
     ```
   - This is connected with `&&` to the preceding check, following the existing pattern of `&&` chaining in the hook entry.
  </action>
  <verify>
    <automated>grep -c './run build' .gitlab-ci.yml && grep -c './run tf-init' .gitlab-ci.yml && grep -c './run tf-auto-apply' .gitlab-ci.yml && grep -c 'scripts/\*\.sh run' .gitlab-ci.yml && grep -c '100755' .gitlab-ci.yml && grep -c '100755' .pre-commit-config.yaml && grep 'All 8 invariants' .gitlab-ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep './run build' .gitlab-ci.yml` matches exactly 1 line (bake job script)
    - `grep './run tf-init' .gitlab-ci.yml` matches exactly 1 line (deploy job script)
    - `grep './run tf-auto-apply' .gitlab-ci.yml` matches exactly 1 line (deploy job script)
    - `grep 'PKR_VAR_aws_region' .gitlab-ci.yml` matches at least 1 line (bake job variables)
    - `grep 'shellcheck scripts/\*\.sh run' .gitlab-ci.yml` matches 1 line (shellcheck job)
    - `grep '100755' .gitlab-ci.yml` matches at least 1 line (grep-gates invariant #8)
    - `grep 'All 8 invariants' .gitlab-ci.yml` matches exactly 1 line
    - `grep '100755' .pre-commit-config.yaml` matches at least 1 line (pre-commit parity)
    - `grep -c 'packer build' .gitlab-ci.yml` returns exactly 0 in the bake:packer-build script block (inline packer commands removed)
    - `grep -c 'tofu plan' .gitlab-ci.yml` returns exactly 0 in the deploy job script block (plan-file workflow removed)
    - `grep -c 'tofu apply' .gitlab-ci.yml` returns exactly 0 in the deploy job script block
    - The `artifacts:` block is removed from the deploy job
    - No `cd packer` or `cd terraform` in bake or deploy job script blocks (./run handles directory changes)
    - The bake job `rules:` block is unchanged
    - The deploy job `rules:` block is unchanged
    - The deploy job `before_script:` (DEVBOX_USER fail-fast) is unchanged
  </acceptance_criteria>
  <done>
    - CI bake stage delegates to ./run build with PKR_VAR_aws_region for region passthrough (CI-01)
    - CI deploy stage delegates to ./run tf-init + ./run tf-auto-apply (CI-02)
    - CI shellcheck job lints run alongside scripts/*.sh (CI-03)
    - CI grep-gates has invariant #8 checking run executable bit, count updated to 8 (CI-04)
    - Pre-commit grep-gates has matching executable bit check (Pitfall 6 parity)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab CI runner -> ./run script | CI delegates execution to the run script; script receives env vars from CI |
| ./run script -> AWS APIs | Script uses OIDC-derived credentials set by .aws-auth before_script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | CI env var injection | mitigate | DEVBOX_USER validated in both CI before_script AND ./run _require_devbox_user; PKR_VAR_aws_region is a CI protected variable |
| T-06-03 | Elevation of Privilege | ./run tf-auto-apply without plan review | mitigate | when: manual gate + protected branch + OIDC scoped to project/branch + DEVBOX_USER fail-fast provide defense-in-depth (same posture as current inline commands) |
| T-06-04 | Tampering | lockfile rewrite in CI | mitigate | ./run tf-init auto-adds -lockfile=readonly when CI=true; grep-gates enforce lockfile is tracked |
| T-06-05 | Tampering | run script executable bit removed | mitigate | CI grep-gates invariant #8 + pre-commit grep-gates check both verify 100755 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. `grep './run build' .gitlab-ci.yml` matches in bake job
2. `grep './run tf-init' .gitlab-ci.yml` matches in deploy job
3. `grep './run tf-auto-apply' .gitlab-ci.yml` matches in deploy job
4. `grep 'scripts/\*\.sh run' .gitlab-ci.yml` matches in shellcheck job
5. `grep '100755' .gitlab-ci.yml` matches in grep-gates job
6. `grep 'All 8 invariants' .gitlab-ci.yml` matches
7. `grep '100755' .pre-commit-config.yaml` matches in grep-gates hook
8. `shellcheck run` passes (lockfile-readonly changes are valid bash)
9. No inline `packer build`, `tofu plan`, `tofu apply`, `tofu init` in bake/deploy script blocks
</verification>

<success_criteria>
CI-01: Bake stage calls ./run build (no inline packer commands)
CI-02: Deploy stage calls ./run tf-init + ./run tf-auto-apply (no inline tofu commands)
CI-03: Shellcheck validates run file alongside scripts/*.sh
CI-04: Grep-gate invariant #8 verifies run executable bit; pre-commit parity maintained
</success_criteria>

<output>
Create `.planning/phases/06-gitlab-ci-polish/06-02-SUMMARY.md` when done
</output>
