---
context: phase
phase: 11-service-config-pam-session-bake-verification
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-06-15T19:52:07.256Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **The static gsd-verifier "passed" on Phase 11 is NOT trustworthy.** It checks config-matches-plan, not runtime reality. An adversarial review caught 4 CRITICAL runtime blockers it missed. Mitigation: do NOT mark Phase 11 done on the static verdict; the real status is FAILED (see `11-VERIFICATION.md` ADVERSARIAL ADDENDUM). Always adversarially verify infra phases before trusting a "passed".
- [ ] CONSTRAINT: **xorgxrdp collides with the CIS baseline.** CIS 2.2.1 removes the X server `xorgxrdp` needs, and `hardening` runs after `xrdp`. A bake will be GREEN while no RDP session can start. Do NOT proceed to a gap-closure plan until the architecture decision (below) is made.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Static-verify-as-done | gsd-verifier "passed" (7/7) on config-match while runtime was broken (X server deleted by CIS). | blocking | Run an adversarial (opus) reviewer on infra phases; extend bake asserts to stat EVERY runtime dep (X server, cert/key, startwm, PAM), not just the role's own artifacts. |
| Deferred verification | The original VNC-PAM scheme (commit 8d9fa7b) shipped "verification deferred to bake" and was broken; same trap nearly repeated here. | blocking | Verification is first-class; never close an infra phase on un-baked assumptions. |

<current_state>
Milestone v3.2 (XRDP Remote Desktop). Phase 10 (from-source build role) DONE + verified. Phase 11 (config/PAM/session/systemd) was EXECUTED — commits f30fbc7, 3a80ae6, e296d0b, f3bbc4b — and the xrdp role is wired into playbook.yml before hardening (invariant holds). BUT Phase 11 verification is FAILED: an adversarial review found the bake would succeed while RDP cannot start. We are BLOCKED on an architecture decision the user wanted to clarify first, then paused.
</current_state>

<completed_work>
- Phase 10: ansible/roles/xrdp builds xrdp 0.10.6 + xorgxrdp 0.10.5 from sha256-pinned github source. Verified passed (10-VERIFICATION.md). Commits b12864f, 9991e6b.
- Phase 11 execution: xrdp.ini (TLS :3389, own self-signed cert), sesman.ini (Xorg backend /usr/libexec/Xorg), /etc/pam.d/xrdp-sesman -> password-auth, startwm.sh (GNOME x11), colord pkla, both systemd units (/usr/local/sbin) enabled, role wired before hardening, layers.xrdp toggle, RDP-13 bake assert. Commits f30fbc7/3a80ae6/e296d0b.
- GSD settings: model_profile=quality, workflow.use_worktrees=false (uncommitted config.json, in this WIP commit).
- 11-VERIFICATION.md corrected from "passed" to "failed" with the full adversarial addendum.
</completed_work>

<remaining_work>
- DECIDE the xorgxrdp-vs-CIS resolution (see blockers).
- Then `/gsd:plan-phase 11 --gaps` to fix: CRITICAL #1 install xorg-x11-server-Xorg+dbus-x11 (if keeping xorgxrdp), #2 CIS 2.2.1, #3 gate xrdp `when: layers.xrdp and layers.desktop`, #4 extend RDP-13 assert to stat /usr/libexec/Xorg + cert/key/startwm/PAM; HIGH: SELinux restorecon /usr/local + /etc/xrdp, FIPS cert (SAN/handshake), sesman unit dual-enable; RISK: polkit .rules vs .pkla, dbus-x11, pam_loginuid.
- Phase 12: SG :3389, run port-forward, remove noVNC/vncserver, revert noVNC username fix (commit 29de35b), RDP-14 live UAT.
</remaining_work>

<decisions_made>
- v3.2 = xrdp from source (no RPM; github source fetch works; airgap = no S3/private mirror but github + AL2023 dnf repos reachable).
- Phase 11 is FAILED pending the architecture decision — not done.
</decisions_made>

<blockers>
- **ARCHITECTURE DECISION (blocking):** xorgxrdp needs the X server CIS 2.2.1 deletes. Options:
  (a) Keep xorgxrdp — install xorg-x11-server-Xorg + dbus-x11; set amzn2023cis_rule_2_2_1=false (the role's documented "X Windows is needed" knob). Least rework; keeps Phases 10/11. Relaxes one CIS control (justified for a desktop).
  (b) Pivot to xrdp-over-Xvnc backend — sesman spawns the already-installed, CIS-surviving Xvnc. No CIS change, no Xorg install. Discards Phase 10 xorgxrdp build + reworks Phase 11 sesman; keeps tigervnc Xvnc (Phase 12 "remove VNC" -> remove noVNC web + vncserver service only).
  (c) Reinstall X after hardening (post-hardening workaround playbook). Keeps CIS scan green but X present. Kludge; least honest.
  User wanted to CLARIFY the options (CIS impact, Xvnc rework cost, whether VNC works on the hardened AMI today) before choosing.
</blockers>

## Required Reading (in order)
1. `.planning/phases/11-service-config-pam-session-bake-verification/11-VERIFICATION.md` — the ADVERSARIAL ADDENDUM; the real status + all findings.
2. `.planning/phases/11-.../11-RESEARCH.md` — config keys + the Xorg-path "ASSUMED/verify-at-bake" note that proved load-bearing.
3. `ansible/roles/AMAZON2023-CIS/tasks/section_2/cis_2.2.x.yml` + `defaults/main.yml:258` — the rule that deletes the X server.
4. `ansible/playbook.yml` (xrdp wired before hardening) + `ansible/roles/xrdp/` (the deliverable).

## Infrastructure State
- Local `main` is **16 commits ahead of origin** — NOTHING pushed since the DCV revert (d3bd9a0). Push is a pending operator decision.
- noVNC username-injection fix (commit 29de35b) still on main, slated for Phase-12 revert.
- Build env: github downloads + AL2023 dnf repos. No S3 mirror, no private mirror.
- Session: model Opus 4.8, effort ultracode (adversarial verification is why the blockers surfaced — keep doing it).

<context>
This whole xrdp track came after a long failed VNC-PAM-over-noVNC debugging chain (empty username -> vncpasswd crash -> PAM service name -> still broken), which led to scrapping VNC auth in favor of RDP. xrdp built fine (Phase 10). Phase 11 config looked done and static-verified 7/7 — but ultracode adversarial review caught that the CIS hardening baseline deletes the very X server xorgxrdp depends on. The lesson and the blocker are the same: trust adversarial runtime review over static "passed". Do not bake assuming Phase 11 works.
</context>

<next_action>
Start by helping the user CLARIFY the xorgxrdp-vs-CIS options (they asked to clarify before deciding): cover (1) exactly what CIS 2.2.1=false gives up and whether their scan flags it, (2) the Xvnc-backend rework cost, (3) whether the existing VNC actually works on today's hardened AMI (it does — Xvnc dodges 2.2.1). Then, once they pick (a)/(b)/(c), run `/gsd:plan-phase 11 --gaps` to close the CRITICAL+HIGH findings.
</next_action>
