---
phase: 11-service-config-pam-session-bake-verification
plan: "02"
type: execute
wave: 1
gap_closure: true
depends_on: ["11-01"]
files_modified:
  - ansible/roles/xrdp/defaults/main.yml
  - ansible/roles/xrdp/tasks/main.yml
  - ansible/roles/hardening/defaults/main.yml
  - ansible/roles/xrdp/files/xrdp-sesman.service
  - ansible/roles/xrdp/files/45-allow-colord.rules
  - ansible/playbook.yml
  - CLAUDE.md
autonomous: true
requirements: [RDP-05, RDP-07, RDP-08, RDP-13]

must_haves:
  truths:
    - "The X server binary /usr/libexec/Xorg is installed (from xorg-x11-server-Xorg, AL2023 repo) and SURVIVES the hardening role — CIS rule 2.2.1 no longer deletes it"
    - "A post-hardening assertion in playbook.yml fails the bake if /usr/libexec/Xorg is absent AFTER the hardening role runs — making the CIS-2.2.1 override regression-proof against a future CIS-role bump"
    - "dbus-x11 is installed so startwm.sh's dbus-launch resolves; gnome-session can start over RDP"
    - "The xrdp role only runs when BOTH layers.xrdp AND layers.desktop are enabled, so GNOME + the secrets-role ec2-user password always exist before xrdp configures auth"
    - "The self-signed xrdp TLS cert is FIPS-acceptable: RSA>=2048, SHA-256 digest, and carries a subjectAltName — so the RDP TLS handshake survives hardening's FIPS mode"
    - "The new X server, the /usr/local xrdp binaries, and /etc/xrdp (cert/key included) are SELinux-relabelled so an enforcing-mode boot raises no AVC denials"
    - "The xrdp / xrdp-sesman systemd units come up reliably at boot — no StopWhenUnneeded and no reciprocal BindsTo self-defeat; sesman is independently wanted at boot and ordered by xrdp.service's one-directional Requires=/After="
    - "The colord polkit allow-rule is delivered in the AL2023 polkit-121 JavaScript .rules format so the GNOME-over-RDP color-manager prompt cannot hang the session"
    - "The RDP-13 bake assertion FAILS the build if /usr/libexec/Xorg, /etc/xrdp/cert.pem, /etc/xrdp/key.pem, /etc/xrdp/startwm.sh, or /etc/pam.d/xrdp-sesman is absent — not just the role's own binary/module/inis"
    - "The hardening role is still the LAST role in ansible/playbook.yml (grep-gate passes); the new post-hardening assert is a post_task, not a role line, so the gate still prints 1"
  artifacts:
    - path: "ansible/roles/xrdp/defaults/main.yml"
      provides: "xrdp_runtime_deps list including xorg-x11-server-Xorg + dbus-x11"
      contains: "xorg-x11-server-Xorg"
    - path: "ansible/roles/hardening/defaults/main.yml"
      provides: "CIS rule 2.2.1 disabled (single accepted desktop deviation)"
      contains: "amzn2023cis_rule_2_2_1: false"
    - path: "ansible/roles/xrdp/files/45-allow-colord.rules"
      provides: "AL2023 polkit-121 JavaScript rules file allowing colord without a prompt"
      contains: "org.freedesktop.color-manager"
    - path: "ansible/roles/xrdp/files/xrdp-sesman.service"
      provides: "sesman unit with a non-racing boot dependency (no StopWhenUnneeded, no BindsTo)"
      contains: "WantedBy=multi-user.target"
    - path: "ansible/roles/xrdp/tasks/main.yml"
      provides: "runtime-dep install, FIPS-safe cert (SAN+sha256), restorecon over Xorg+/usr/local+/etc/xrdp, polkit .rules install, extended RDP-13 assert"
      contains: "/usr/libexec/Xorg"
    - path: "ansible/playbook.yml"
      provides: "xrdp role gated on layers.xrdp AND layers.desktop, still before hardening; post-hardening post_task stat+assert of /usr/libexec/Xorg"
      contains: "layers.desktop"
  key_links:
    - from: "ansible/roles/xrdp/tasks/main.yml (dnf runtime deps)"
      to: "/usr/libexec/Xorg referenced by sesman.ini [Xorg] param"
      via: "xorg-x11-server-Xorg provides /usr/libexec/Xorg"
      pattern: "xorg-x11-server-Xorg"
    - from: "ansible/roles/hardening/defaults/main.yml (amzn2023cis_rule_2_2_1: false)"
      to: "AMAZON2023-CIS rule 2.2.1 removal task (skipped)"
      via: "parent-role default overrides the vendored child-role default"
      pattern: "amzn2023cis_rule_2_2_1:\\s*false"
    - from: "ansible/playbook.yml (post_tasks stat+assert of /usr/libexec/Xorg)"
      to: "the hardening role having just run (post_tasks run after ALL roles)"
      via: "stat .stat.exists asserted AFTER hardening — catches a CIS-role bump re-enabling 2.2.1"
      pattern: "/usr/libexec/Xorg"
    - from: "ansible/roles/xrdp/tasks/main.yml (RDP-13 assert)"
      to: "/usr/libexec/Xorg + /etc/xrdp/{cert,key}.pem + startwm.sh + /etc/pam.d/xrdp-sesman"
      via: "stat + assert .stat.exists fails the bake on absence"
      pattern: "xrdp_rdp13_xserver"
---

<objective>
Gap-closure for Phase 11. Plan 11-01 static-verified 7/7 but an adversarial review found the
bake would be GREEN while no RDP session could start: no role installs the X server, the
hardening role's CIS rule 2.2.1 deletes it, the xrdp role is gated wrong, and the RDP-13
assert is blind to all of it. The architecture decision is RESOLVED — keep the xorgxrdp/Xorg
backend (option a). This plan implements that decision against the Xorg backend and makes the
bake assertion PROVE a real, runnable RDP+Xorg+PAM+GNOME stack survives hardening.

Purpose: close all four CRITICAL findings (X server install; CIS 2.2.1 deletion; layer-gating
bug; blind RDP-13 assert), all three HIGH findings (SELinux relabel; FIPS-safe cert; sesman
unit boot race), and all three RISK findings (polkit .pkla→.rules; dbus-x11; pam_loginuid).
Do NOT pivot to Xvnc and do NOT add a post-hardening reinstall kludge.

Output: extended xrdp role (runtime-dep install, FIPS-safe cert, full SELinux relabel, polkit
.rules, extended RDP-13 assert), one CIS override line in hardening/defaults, a corrected
sesman unit, the layer-gate fix in playbook.yml, a regression-proof post-hardening X-server
assertion in playbook.yml, and the documented CIS deviation in CLAUDE.md.
11-01-PLAN.md is preserved untouched — this is plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE GAP SOURCE — read the ADVERSARIAL REVIEW ADDENDUM first (it lists every finding + the decision)
@.planning/phases/11-service-config-pam-session-bake-verification/11-VERIFICATION.md

# The executed plan being patched — PRESERVE it; this plan only ADDS to the same role
@.planning/phases/11-service-config-pam-session-bake-verification/11-01-PLAN.md
@.planning/phases/11-service-config-pam-session-bake-verification/11-01-SUMMARY.md

# Config keys + the "ASSUMED/verify-at-bake" Xorg-path note that proved load-bearing
@.planning/phases/11-service-config-pam-session-bake-verification/11-RESEARCH.md

# The role being extended (Phase 11 block begins line 170; the RDP-13 assert is lines 301-356)
@ansible/roles/xrdp/tasks/main.yml
@ansible/roles/xrdp/defaults/main.yml
@ansible/roles/xrdp/files/xrdp-sesman.service
@ansible/roles/xrdp/files/xrdp.service
@ansible/roles/xrdp/files/45-allow-colord.pkla

# The hardening invocation + the CORRECT CIS override site (parent-role defaults)
@ansible/roles/hardening/tasks/main.yml
@ansible/roles/hardening/defaults/main.yml

# CIS rule 2.2.1 (the deletion task at line 3) + its vendored default (line 258)
@ansible/roles/AMAZON2023-CIS/tasks/section_2/cis_2.2.x.yml

# Where the role is wired (layer gate is the bug) + the toggle file
@ansible/playbook.yml
@ansible/layer_config.yml

# Project invariants — §8 hardening-stays-last grep-gate + airgap policy
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the repo. No exploration required. -->

CIS OVERRIDE MECHANISM (load-bearing for CRITICAL #2):
- ansible/roles/hardening/tasks/main.yml runs `include_role: name: AMAZON2023-CIS`.
- The EXISTING working overrides (amzn2023cis_rule_3_4_*, _5_1_2_1_*, _5_3, _config_aide)
  live in ansible/roles/hardening/defaults/main.yml. The parent role (hardening) default
  overrides the vendored child role (AMAZON2023-CIS) default. This is the PROVEN site.
- CRITICAL: do NOT edit ansible/roles/AMAZON2023-CIS/defaults/main.yml (line 258
  amzn2023cis_rule_2_2_1: true) — that vendored default is lost on a role bump.
- The CIS 2.2.1 removal task (ansible/roles/AMAZON2023-CIS/tasks/section_2/cis_2.2.x.yml:3)
  is guarded `when: amzn2023cis_rule_2_2_1 and 'xorg-x11-server-common' in
  ansible_facts.packages`. Setting amzn2023cis_rule_2_2_1: false in hardening/defaults
  skips it cleanly (the CIS prelim gathers package_facts).
- DEFENSE-IN-DEPTH (W1): the override above is the real fix, but it is unguarded against a
  future CIS-role bump silently re-enabling rule 2.2.1. The post_tasks assert in playbook.yml
  (Task 3) makes that regression LOUD: it stats /usr/libexec/Xorg AFTER hardening and fails
  the bake if the X server was deleted — reproducing this exact incident's detection on a
  future bump rather than shipping a GREEN-but-broken bake.

AIRGAP INSTALL POLICY (CLAUDE.md / RESEARCH C2): xorg-x11-server-Xorg AND dbus-x11 ARE in
the AL2023 core dnf repos. Install them with the SAME ansible.builtin.dnf task pattern the
xrdp role already uses (Step 1, tasks/main.yml lines 10-30). NO S3, NO private mirror, NO new
get_url. /usr/libexec/Xorg is provided by xorg-x11-server-Xorg; dbus-launch is provided by
dbus-x11.

PLAYBOOK LAYER GATE (the bug, playbook.yml line 60):
  CURRENT: when: layers.xrdp | default(false)
  TARGET:  when: (layers.xrdp | default(false)) and (layers.desktop | default(false))
xrdp depends on GNOME (desktop role) + the ec2-user password (secrets role, gated on
layers.vscode OR layers.desktop). Gating on desktop too guarantees both exist first.

PLAYBOOK post_tasks (the W1 regression guard site, playbook.yml lines 66-86):
- playbook.yml ALREADY has a `post_tasks:` key (line 66) holding the dnf-clean / temp-file /
  manifest tasks. post_tasks run AFTER all roles, including hardening — exactly where a
  CIS-deleted X server would be observable. Add the new stat+assert as the FIRST post_task
  (before the dnf clean) so it fires immediately after hardening completes.
- The new post_task must be gated on the SAME condition the xrdp role uses:
  `when: (layers.xrdp | default(false)) and (layers.desktop | default(false))` — so a
  no-xrdp bake does not fail on a missing X server it never installed.

HARDENING-STAYS-LAST GREP-GATE (CLAUDE.md §8 — MUST stay 1):
  grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening'
=> the LAST `- role:` line MUST remain `- role: hardening` (line 63). Only edit the xrdp
   role's `when:` (line 60) and ADD a post_task — do NOT move or duplicate any role line.
   post_tasks are NOT `- role:` lines, so the grep-gate is unaffected by the W1 addition.

EXISTING CERT GEN (tasks/main.yml lines 176-184) — to be REPLACED in Task 2:
  openssl req -x509 -nodes -newkey rsa:2048 -keyout /etc/xrdp/key.pem -out /etc/xrdp/cert.pem
  -days 3650 -subj '/CN=devbox'   (args: creates: /etc/xrdp/cert.pem)
  PROBLEM: no SAN, default digest may not be FIPS-asserted. FIPS-safe form needs -sha256 and
  -addext "subjectAltName=DNS:devbox". RSA-2048 is FIPS-acceptable; keep -newkey rsa:2048.

EXISTING SELINUX RELABEL (Phase 10, tasks/main.yml lines 152-155): only
  restorecon -Rv /usr/lib64/xorg/modules/   — does NOT cover /usr/libexec/Xorg,
  /usr/local/sbin, /usr/local/bin, /usr/local/lib, or /etc/xrdp. Task 2 adds those.

EXISTING sesman unit (files/xrdp-sesman.service) HAS the race:
  [Unit] After=network.target / StopWhenUnneeded=true / BindsTo=xrdp.service
  PROBLEM: StopWhenUnneeded=true plus a reciprocal BindsTo (xrdp.service Requires=sesman AND
  sesman BindsTo=xrdp.service) is the fragile pattern — systemd can stop sesman at boot when
  nothing "needs" it. FIX (W2, pinned — no discretion): REMOVE both StopWhenUnneeded AND
  BindsTo; keep [Unit] After=network.target and [Install] WantedBy=multi-user.target. Ordering
  comes solely from xrdp.service's one-directional Requires=xrdp-sesman.service +
  After=...xrdp-sesman.service (confirmed present, unchanged).

xrdp.service (files/xrdp.service, confirmed) ALREADY has:
  [Unit] Requires=xrdp-sesman.service / After=network-online.target xrdp-sesman.service
  [Install] WantedBy=multi-user.target
  => This one-directional dependency is sufficient; do NOT edit xrdp.service.

POLKIT FORMAT (RISK): AL2023 ships polkit 121+, which IGNORES /etc/polkit-1/localauthority
.pkla files. The allow-rule must be a JavaScript .rules file under
/etc/polkit-1/rules.d/ (e.g. /etc/polkit-1/rules.d/45-allow-colord.rules). The existing
.pkla (files/45-allow-colord.pkla) is dead on AL2023.

EXISTING RDP-13 ASSERT (tasks/main.yml lines 301-356) stats only:
  /usr/local/sbin/xrdp, /usr/lib64/xorg/modules/drivers/xrdpdev_drv.so,
  /etc/xrdp/xrdp.ini, /etc/xrdp/sesman.ini, then systemctl is-enabled xrdp xrdp-sesman.
  MISSING: /usr/libexec/Xorg, /etc/xrdp/cert.pem, /etc/xrdp/key.pem, /etc/xrdp/startwm.sh,
  /etc/pam.d/xrdp-sesman, and a SAN check on the cert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the X server + dbus-x11, stop CIS from deleting it, fix the layer gate, document the deviation (CRITICAL #1/#2/#3 + RISK dbus-x11)</name>
  <read_first>
    - ansible/roles/xrdp/defaults/main.yml (add the runtime-deps list here)
    - ansible/roles/xrdp/tasks/main.yml lines 10-30 (the existing Step-1 dnf pattern to mirror) + lines 170-173 (the Phase 11 banner — insert the runtime-dep task at the TOP of the Phase 11 block, before the cert task at line 176)
    - ansible/roles/hardening/defaults/main.yml (the proven CIS-override site; add the 2.2.1 line alongside the existing amzn2023cis_rule_* overrides)
    - ansible/roles/AMAZON2023-CIS/tasks/section_2/cis_2.2.x.yml lines 1-16 (the 2.2.1 removal task this override skips)
    - ansible/playbook.yml lines 56-64 (the desktop→xrdp→hardening block; edit ONLY the xrdp `when:` on line 60)
    - CLAUDE.md lines 186-209 (§8 invariants block — append the documented CIS deviation here)
  </read_first>
  <files>
    ansible/roles/xrdp/defaults/main.yml,
    ansible/roles/xrdp/tasks/main.yml,
    ansible/roles/hardening/defaults/main.yml,
    ansible/playbook.yml,
    CLAUDE.md
  </files>
  <action>
    CRITICAL #1 — install the X server + dbus-x11 from AL2023 repos. In
    ansible/roles/xrdp/defaults/main.yml add a list variable `xrdp_runtime_deps` containing
    `xorg-x11-server-Xorg` (provides /usr/libexec/Xorg referenced by sesman.ini [Xorg]) and
    `dbus-x11` (provides dbus-launch called by startwm.sh). Add a one-line comment per package
    naming what it provides and that both are AL2023-core packages (airgap policy: dnf only,
    no S3/mirror). Then in ansible/roles/xrdp/tasks/main.yml insert a new task at the TOP of
    the Phase 11 block (immediately after the line-170-173 banner, BEFORE the cert task), an
    `ansible.builtin.dnf:` installing `"{{ xrdp_runtime_deps }}"` with `state: present`,
    mirroring the Step-1 dnf style. Name it so it reads as RDP-05/07 runtime deps. Register
    the result and add an `ansible.builtin.assert:` that the install `is success` with a
    fail_msg naming xorg-x11-server-Xorg + dbus-x11 and the AL2023-core requirement (mirror
    the existing RDP-03 assert at lines 33-45).

    CRITICAL #2 — stop CIS 2.2.1 from deleting the X server. In
    ansible/roles/hardening/defaults/main.yml add the single line
    `amzn2023cis_rule_2_2_1: false` alongside the existing amzn2023cis_rule_* overrides, with
    an inline comment justifying it: this host is a GRAPHICAL DESKTOP — the X server is
    required by purpose, so the NIST 800-53 CM-7 / CIS "no X server on a server" rationale
    does not apply; this is the single accepted Level-2 CIS deviation for the devimage. Do NOT
    edit the vendored AMAZON2023-CIS/defaults/main.yml (line 258) — that is lost on a role
    bump. The parent-role default override is the proven mechanism (same as the rule_3_4_*
    overrides already in this file). NOTE: this override is the real fix; Task 3 adds a
    post-hardening assert in playbook.yml that makes a future CIS-role bump re-enabling 2.2.1
    a LOUD bake failure instead of a silent regression.

    CRITICAL #3 — fix the layer-gating bug. In ansible/playbook.yml change the xrdp role's
    `when:` (line 60) from `layers.xrdp | default(false)` to
    `(layers.xrdp | default(false)) and (layers.desktop | default(false))`. Update the trailing
    comment to note xrdp needs GNOME (desktop role) + the secrets-role ec2-user password.
    Touch ONLY line 60 — leave `- role: xrdp` (line 59) and `- role: hardening` (line 63) where
    they are. The last `- role:` line MUST stay `- role: hardening`.

    DOCUMENT the deviation. In CLAUDE.md §8 (after the existing invariant bullets, ~line 209)
    add a bullet recording the accepted CIS deviation: `amzn2023cis_rule_2_2_1: false` is set
    in ansible/roles/hardening/defaults/main.yml because the devimage is a graphical desktop
    that requires the X server (xrdp/xorgxrdp); reference Phase 11 RDP-05/07. Keep it concise;
    do NOT introduce a `changeme` literal and do NOT reference any retired `make` target.
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox &amp;&amp; \
      python3 -c "import yaml; yaml.safe_load(open('ansible/roles/xrdp/defaults/main.yml'))" &amp;&amp; \
      python3 -c "import yaml; yaml.safe_load(open('ansible/roles/xrdp/tasks/main.yml'))" &amp;&amp; \
      python3 -c "import yaml; yaml.safe_load(open('ansible/roles/hardening/defaults/main.yml'))" &amp;&amp; \
      python3 -c "import yaml; yaml.safe_load(open('ansible/playbook.yml'))" &amp;&amp; \
      grep -q 'xorg-x11-server-Xorg' ansible/roles/xrdp/defaults/main.yml &amp;&amp; \
      grep -q 'dbus-x11' ansible/roles/xrdp/defaults/main.yml &amp;&amp; \
      grep -q 'xrdp_runtime_deps' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -Eq 'amzn2023cis_rule_2_2_1:[[:space:]]*false' ansible/roles/hardening/defaults/main.yml &amp;&amp; \
      grep -Eq 'when:.*layers\.xrdp.*and.*layers\.desktop' ansible/playbook.yml &amp;&amp; \
      grep -q 'amzn2023cis_rule_2_2_1' CLAUDE.md &amp;&amp; \
      [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ] &amp;&amp; \
      ! grep -rq 'changeme' ansible/roles/xrdp/ ansible/roles/hardening/defaults/main.yml &amp;&amp; \
      echo PASS</automated>
  </verify>
  <done>
    xrdp/defaults/main.yml defines xrdp_runtime_deps with xorg-x11-server-Xorg + dbus-x11
    (AL2023-core, airgap-compliant); xrdp/tasks/main.yml installs them at the top of the
    Phase 11 block with a success assert; hardening/defaults/main.yml has
    `amzn2023cis_rule_2_2_1: false` with a desktop-exception comment (vendored CIS defaults
    untouched); playbook.yml gates xrdp on `layers.xrdp and layers.desktop` with hardening
    still the last role (grep-gate = 1); CLAUDE.md §8 documents the accepted CIS deviation.
    No `changeme`. All edited YAML parses.
  </done>
</task>

<task type="auto">
  <name>Task 2: FIPS-safe cert (SAN+sha256), full SELinux relabel, sesman unit boot-race fix, colord polkit .rules, pam_loginuid evaluation (HIGH cert/SELinux/unit-race + RISK polkit/pam_loginuid)</name>
  <read_first>
    - ansible/roles/xrdp/tasks/main.yml lines 176-195 (existing cert-gen + perms to REPLACE) and lines 250-265 (existing polkit .pkla install to REPLACE) and lines 149-155 (the Phase-10 restorecon that only covered the xorg module dir) and the PAM-copy task that installs files/xrdp-sesman.pam (where the pam_loginuid justifying comment goes)
    - ansible/roles/xrdp/files/xrdp-sesman.service (the StopWhenUnneeded + BindsTo lines to REMOVE) and ansible/roles/xrdp/files/xrdp.service (its EXISTING Requires=xrdp-sesman.service + After=...xrdp-sesman.service — confirm, do NOT edit)
    - ansible/roles/xrdp/files/xrdp-sesman.pam (the `session required pam_loginuid.so` line to pin in acceptance)
    - ansible/roles/xrdp/files/45-allow-colord.pkla (the dead .pkla content to port to JS .rules)
    - 11-RESEARCH.md Security Notes (cert perms; FIPS), Pitfall 6 (colord polkit)
  </read_first>
  <files>
    ansible/roles/xrdp/tasks/main.yml,
    ansible/roles/xrdp/files/xrdp-sesman.service,
    ansible/roles/xrdp/files/45-allow-colord.rules
  </files>
  <action>
    HIGH — FIPS-safe cert. In ansible/roles/xrdp/tasks/main.yml REPLACE the existing
    `openssl req -x509` cert-gen command (the one with `-subj '/CN=devbox'`) with a
    FIPS-acceptable invocation: keep `-x509 -nodes -newkey rsa:2048` (RSA-2048 is FIPS-valid),
    keep `-keyout /etc/xrdp/key.pem -out /etc/xrdp/cert.pem -days 3650 -subj '/CN=devbox'`,
    and ADD `-sha256` (FIPS-approved digest) and `-addext "subjectAltName=DNS:devbox"` (a SAN
    is required for a modern/FIPS-strict TLS handshake; CN-only certs are rejected). Keep
    `args: creates: /etc/xrdp/cert.pem` for idempotency. Leave the perms task (cert.pem 0644,
    key.pem 0600, root:root) unchanged. Note: a regenerated cert needs the `creates:` guard
    gone OR the old cert removed only on a clean bake — keep `creates:` (fresh bakes have no
    prior cert; this is bake-time, not a live rotation).

    HIGH — full SELinux relabel. Add an `ansible.builtin.command:` running restorecon over the
    new runtime paths so an enforcing-mode boot raises no AVCs. Relabel: `/usr/libexec/Xorg`,
    `/usr/local/sbin`, `/usr/local/bin`, `/usr/local/lib` (xrdp binaries + libxup.so), and
    `/etc/xrdp` (cert.pem + key.pem included). Use `restorecon -RvF` over the directories plus
    the Xorg path; `changed_when: false` (mirror the Phase-10 restorecon at lines 152-155).
    Place this task AFTER the cert + config + unit tasks so every file exists before relabel.
    Do this WITHIN the xrdp role (bake-time, before hardening flips SELinux to enforcing).

    HIGH — sesman unit boot-race (W2, PINNED — no executor discretion). In
    ansible/roles/xrdp/files/xrdp-sesman.service REMOVE BOTH the `StopWhenUnneeded=true` line
    AND the `BindsTo=xrdp.service` line from the `[Unit]` section. A reciprocal
    `Requires=`+`BindsTo` pair (xrdp.service Requires=sesman AND sesman BindsTo=xrdp.service)
    is the fragile pattern that, combined with StopWhenUnneeded, lets systemd stop sesman at
    boot when nothing yet "needs" it. KEEP `[Unit] After=network.target`. KEEP
    `[Install] WantedBy=multi-user.target` so sesman is independently pulled in by
    multi-user.target at every cold boot. Do NOT edit ansible/roles/xrdp/files/xrdp.service —
    rely on its EXISTING one-directional `Requires=xrdp-sesman.service` +
    `After=network-online.target xrdp-sesman.service` to provide both the dependency and the
    ordering. This is the non-racing combination: sesman starts independently (WantedBy) and is
    ordered before xrdp (xrdp's After=/Requires=), with no reverse binding to tear it down. Add
    ONE comment line to xrdp-sesman.service recording the chosen non-racing dependency — that
    StopWhenUnneeded and BindsTo were removed in favour of WantedBy=multi-user.target plus
    xrdp.service's one-directional Requires=/After=.

    RISK — colord polkit format. Create ansible/roles/xrdp/files/45-allow-colord.rules as an
    AL2023 polkit-121 JavaScript rules file: a `polkit.addRule(function(action, subject){...})`
    that returns `polkit.Result.YES` when `action.id` starts with
    `"org.freedesktop.color-manager."` (port the intent of the dead .pkla:
    Action=org.freedesktop.color-manager.*, all results yes). In tasks/main.yml REPLACE the
    existing `.pkla` install (copy of 45-allow-colord.pkla to
    /etc/polkit-1/localauthority/50-local.d/) with: ensure `/etc/polkit-1/rules.d` exists
    (state=directory, 0755), then `ansible.builtin.copy: src=45-allow-colord.rules
    dest=/etc/polkit-1/rules.d/45-allow-colord.rules owner=root group=root mode=0644`. Leave
    the old files/45-allow-colord.pkla file in place (harmless, unreferenced) OR delete its
    install task only — do NOT leave a task that installs the dead .pkla. Goal: no
    color-manager auth popup can hang the GNOME-over-RDP session on AL2023 polkit 121+.

    RISK — pam_loginuid. In files/xrdp-sesman.pam, the intended line is the explicit
    `session    required    pam_loginuid.so` (three fields on one line: type `session`,
    control `required`, module `pam_loginuid.so`). This is the upstream redhat default and the
    standard handling for xrdp-sesman; pam_loginuid setting the audit loginuid does not block a
    PAM-authenticated RDP session under systemd (it is `required` in sshd's stack too).
    CONFIRM-SAFE: leave it `required` and add a one-line comment in tasks/main.yml AT THE
    PAM-copy task recording that `pam_loginuid.so required` is the audited redhat-standard
    handling and is intentionally retained — no change needed. The live-session effect of
    pam_loginuid is a runtime property not fully provable at bake; it is covered by the RDP-14
    human UAT gate. (If the executor finds AL2023-specific evidence it blocks the sesman
    session, relax to `session optional pam_loginuid.so` and note why; default is keep.)
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox &amp;&amp; \
      python3 -c "import yaml; yaml.safe_load(open('ansible/roles/xrdp/tasks/main.yml'))" &amp;&amp; \
      grep -q 'subjectAltName=DNS:devbox' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q -- '-sha256' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -Eq 'restorecon.*(/usr/libexec/Xorg|/usr/local)' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q '/etc/xrdp' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q 'polkit-1/rules.d/45-allow-colord.rules' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      ! grep -q 'StopWhenUnneeded' ansible/roles/xrdp/files/xrdp-sesman.service &amp;&amp; \
      ! grep -q 'BindsTo' ansible/roles/xrdp/files/xrdp-sesman.service &amp;&amp; \
      grep -q 'WantedBy=multi-user.target' ansible/roles/xrdp/files/xrdp-sesman.service &amp;&amp; \
      grep -q 'org.freedesktop.color-manager' ansible/roles/xrdp/files/45-allow-colord.rules &amp;&amp; \
      grep -q 'polkit.addRule' ansible/roles/xrdp/files/45-allow-colord.rules &amp;&amp; \
      grep -Eq '^[[:space:]]*session[[:space:]]+required[[:space:]]+pam_loginuid\.so' ansible/roles/xrdp/files/xrdp-sesman.pam &amp;&amp; \
      grep -q 'pam_loginuid' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      ! grep -rq 'changeme' ansible/roles/xrdp/ &amp;&amp; \
      echo PASS</automated>
  </verify>
  <done>
    Cert-gen uses -sha256 + -addext subjectAltName=DNS:devbox over RSA-2048 (FIPS-safe);
    a restorecon task relabels /usr/libexec/Xorg + /usr/local/{sbin,bin,lib} + /etc/xrdp after
    all files are placed; xrdp-sesman.service has NO StopWhenUnneeded and NO BindsTo and keeps
    WantedBy=multi-user.target, with a comment recording the one-directional non-racing
    dependency (xrdp.service Requires=/After= only); files/45-allow-colord.rules is a
    polkit-121 JS rules file granting color-manager, installed to /etc/polkit-1/rules.d/ (the
    dead .pkla is no longer installed); files/xrdp-sesman.pam carries the explicit
    `session required pam_loginuid.so` line, kept `required` and justified by a comment in
    tasks/main.yml that cross-references the RDP-14 UAT gate. No `changeme`. YAML parses.
  </done>
</task>

<task type="auto">
  <name>Task 3: Make the RDP-13 bake assert PROVE the runtime is real + add a regression-proof post-hardening X-server assert (CRITICAL #4 + W1)</name>
  <read_first>
    - ansible/roles/xrdp/tasks/main.yml lines 301-356 (the existing RDP-13 block to EXTEND — stat tasks + the assert + the is-enabled check)
    - ansible/playbook.yml lines 66-86 (the EXISTING post_tasks block — add the X-server assert as the FIRST post_task, before the dnf-clean task; confirm post_tasks runs after the hardening role)
    - 11-RESEARCH.md Pattern 6 (the assert/stat idiom) + the Open Questions (Xorg path, libxup load)
    - 11-VERIFICATION.md CRITICAL #4 (the assert is blind to /usr/libexec/Xorg, cert/key, startwm, PAM) + W1 (the in-role assert runs BEFORE hardening so it cannot catch CIS deleting the X server)
  </read_first>
  <files>
    ansible/roles/xrdp/tasks/main.yml,
    ansible/playbook.yml
  </files>
  <action>
    Extend the existing `# ── RDP-13 bake assertion ──` block (do NOT remove the current
    binary/module/ini stats + the systemctl is-enabled check — they stay). Add `ansible.builtin.stat:`
    tasks for the five paths the current assert is blind to, each registering its own var:
    `/usr/libexec/Xorg` (register xrdp_rdp13_xserver — the X server CRITICAL #1 installs and
    CRITICAL #2 protects), `/etc/xrdp/cert.pem` (xrdp_rdp13_cert), `/etc/xrdp/key.pem`
    (xrdp_rdp13_key), `/etc/xrdp/startwm.sh` (xrdp_rdp13_startwm), `/etc/pam.d/xrdp-sesman`
    (xrdp_rdp13_pam). Then EITHER extend the existing files assert's `that:` list to also
    require all five new `.stat.exists`, OR add a second `ansible.builtin.assert:` covering
    them — the bake MUST fail if ANY is absent. The fail_msg MUST name each missing path
    explicitly (mirror the existing fail_msg style at lines 331-338).

    W1 — SOFTEN the in-role X-server fail_msg. The in-role RDP-13 assert runs INSIDE the xrdp
    role, which executes BEFORE hardening — so it can only catch "the runtime-dep install was
    skipped," NOT "CIS deleted the X server" (CIS hasn't run yet at this point). The fail_msg
    for the /usr/libexec/Xorg stat in the in-role assert MUST say the X server is absent because
    the runtime-dep install (CRITICAL #1) was skipped or failed. DROP any "CIS deleted it" claim
    from the in-role assert — that claim belongs to the post-hardening assert below, where it can
    actually fire.

    Additionally assert the cert carries a SAN (FIPS handshake requirement from Task 2): add an
    `ansible.builtin.command:` running `openssl x509 -in /etc/xrdp/cert.pem -noout -text`
    (register xrdp_rdp13_certtext, changed_when: false, failed_when: false) and an
    `ansible.builtin.assert:` that `'Subject Alternative Name' in xrdp_rdp13_certtext.stdout`
    (or `'DNS:devbox' in xrdp_rdp13_certtext.stdout`), with a fail_msg pointing at the Task-2
    cert-gen `-addext subjectAltName`. This proves the cert the RDP TLS handshake will present
    is FIPS-acceptable, not just present. Keep this in-role block at the END of the Phase 11
    tasks (after enable + the new restorecon/polkit tasks) so every artifact exists before it is
    asserted. Do not weaken the existing is-enabled rc==0 assert.

    W1 — ADD a post-hardening regression-proof X-server assert in ansible/playbook.yml. The
    in-role assert above runs before hardening; a future CIS-role bump that silently re-enables
    rule 2.2.1 would delete /usr/libexec/Xorg AFTER the xrdp role ran, reproducing this exact
    incident with a GREEN bake. To make that LOUD: playbook.yml ALREADY has a `post_tasks:` key
    (line 66) — post_tasks run after ALL roles including hardening. Add, as the FIRST post_task
    (before the existing dnf-clean task), an `ansible.builtin.stat:` of `/usr/libexec/Xorg`
    (register e.g. xserver_post_hardening) followed by an `ansible.builtin.assert:` that
    `xserver_post_hardening.stat.exists`, with a fail_msg stating that /usr/libexec/Xorg was
    deleted AFTER the xrdp role configured it — most likely CIS rule 2.2.1 was re-enabled on a
    CIS-role bump; the fix is `amzn2023cis_rule_2_2_1: false` in
    ansible/roles/hardening/defaults/main.yml. Gate BOTH the stat and the assert on the SAME
    condition as the xrdp role: `when: (layers.xrdp | default(false)) and (layers.desktop |
    default(false))` so a no-xrdp bake does not fail on an X server it never installed. CRITICAL:
    these are post_tasks, NOT `- role:` lines — the hardening-stays-last grep-gate is unaffected
    (the last `- role:` line stays `- role: hardening`). Do NOT add, move, or duplicate any role
    line.
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/devbox &amp;&amp; \
      python3 -c "import yaml; yaml.safe_load(open('ansible/roles/xrdp/tasks/main.yml'))" &amp;&amp; \
      python3 -c "import yaml; yaml.safe_load(open('ansible/playbook.yml'))" &amp;&amp; \
      grep -q 'xrdp_rdp13_xserver' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q '/usr/libexec/Xorg' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q 'xrdp_rdp13_cert' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q 'xrdp_rdp13_key' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q 'xrdp_rdp13_startwm' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q 'xrdp_rdp13_pam' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q '/etc/pam.d/xrdp-sesman' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -Eq 'Subject Alternative Name|DNS:devbox' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q 'systemctl is-enabled xrdp xrdp-sesman' ansible/roles/xrdp/tasks/main.yml &amp;&amp; \
      grep -q '/usr/libexec/Xorg' ansible/playbook.yml &amp;&amp; \
      grep -Eq 'when:.*layers\.xrdp.*and.*layers\.desktop' ansible/playbook.yml &amp;&amp; \
      [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening')" -eq 1 ] &amp;&amp; \
      echo PASS</automated>
  </verify>
  <done>
    The in-role RDP-13 block now stats /usr/libexec/Xorg, /etc/xrdp/cert.pem, /etc/xrdp/key.pem,
    /etc/xrdp/startwm.sh, /etc/pam.d/xrdp-sesman (each registered) and asserts every
    .stat.exists in addition to the original binary/module/ini stats + the systemctl
    is-enabled rc==0 check; a cert-SAN assert proves the TLS cert carries DNS:devbox; the
    in-role X-server fail_msg blames a skipped runtime-dep install (NOT CIS, which has not run
    yet). A NEW post-hardening post_task in playbook.yml stats /usr/libexec/Xorg and asserts it
    still exists AFTER hardening — gated on layers.xrdp AND layers.desktop, with a fail_msg
    naming a CIS-2.2.1 re-enable and the override fix — making the CIS override regression-proof
    against a future CIS-role bump. The grep-gate still prints 1 (post_tasks are not role lines).
    YAML parses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RDP client → xrdp `:3389` (TLS) | Untrusted network input crosses into xrdp over a TLS-wrapped channel. Network reachability (SG/CIDR) is Phase 12 — at bake the port is configured, not yet internet-reachable. |
| xrdp-sesman → PAM (`/etc/pam.d/xrdp-sesman`) | RDP credentials validated by the CIS-hardened `password-auth` stack (faillock + pwquality). |
| Exposed Xorg server (newly installed) | `/usr/libexec/Xorg` runs per-session under xorgxrdp with `-nolisten tcp` (sesman.ini), so the X server does not open a TCP socket; local session display only. |
| CIS hardening posture (relaxed control) | Disabling CIS rule 2.2.1 keeps an X server on the image, widening attack surface relative to a pure server profile. Regression-guarded by a post-hardening assert (Task 3 / W1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Elevation of Privilege / Tampering | CIS rule 2.2.1 relaxation (X server retained) | accept | The devimage is a graphical desktop; an X server is required by purpose, so the NIST 800-53 CM-7 / CIS "no X on a server" control does not apply. Disposition recorded as the SINGLE accepted Level-2 CIS deviation in CLAUDE.md §8 and inline in hardening/defaults/main.yml. Residual risk: the X server is a local attack surface — bounded by single-operator `ec2-user`, `AllowRootLogin=false`, SELinux enforcing (relabel in Task 2), and `-nolisten tcp`. |
| T-11-08b | Tampering | future CIS-role bump silently re-enabling rule 2.2.1 → X server deleted post-bake | mitigate | Post-hardening post_task in playbook.yml stats /usr/libexec/Xorg AFTER the hardening role and fails the bake if it is absent (Task 3 / W1). Turns a silent GREEN-but-broken regression into a loud bake failure that names the CIS-2.2.1 override as the fix. |
| T-11-09 | Information Disclosure / Spoofing | Xorg listening for network X clients | mitigate | sesman.ini `[Xorg]` passes `-nolisten tcp` + `-nolisten` so the X server opens no TCP port; only the local xorgxrdp session connects. No X11-over-TCP surface added by installing xorg-x11-server-Xorg. |
| T-11-10 | Information Disclosure | RDP TLS handshake under FIPS | mitigate | FIPS-safe self-signed cert: RSA-2048, `-sha256` digest, `subjectAltName=DNS:devbox` (Task 2). FIPS mode (hardening) cannot reject the cert at the `:3389` TLS handshake; bake asserts the SAN is present (Task 3). `security_layer=tls`, `ssl_protocols=TLSv1.2, TLSv1.3` unchanged from 11-01. |
| T-11-11 | Spoofing | self-signed cert (no CA chain) | accept | Documented milestone trust model (no CA-issued cert); client pins on first connect. Verified at the RDP-14 live UAT. key.pem 0600 root-only; relabelled under /etc/xrdp. |
| T-11-12 | Tampering / SELinux denial | unlabelled /usr/local + /usr/libexec/Xorg binaries under enforcing | mitigate | restorecon over /usr/libexec/Xorg, /usr/local/{sbin,bin,lib}, /etc/xrdp at bake (Task 2), before hardening flips SELinux to enforcing. Prevents AVC denials that would silently break the session on first boot. |
| T-11-13 | Elevation of Privilege | colord polkit allow-all in JS .rules | accept | The rule grants `org.freedesktop.color-manager.*` without a prompt to avoid a session-hanging auth dialog (Pitfall 6). Scope is color-manager only (not a broad polkit bypass); single-operator image. .pkla→.rules conversion is required because AL2023 polkit 121+ ignores .pkla. |
| T-11-14 | Denial of Service | sesman/xrdp systemd boot race | mitigate | Remove `StopWhenUnneeded=true` AND `BindsTo=xrdp.service` from xrdp-sesman.service; keep `WantedBy=multi-user.target` so sesman is independently pulled in at boot, ordered by xrdp.service's one-directional Requires=/After= (Task 2). Eliminates the reciprocal-binding pattern that could stop sesman as "unneeded" → no RDP sessions. |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs of third-party registries. xorg-x11-server-Xorg + dbus-x11 are AL2023-CORE dnf packages (airgap policy honored: no S3, no private mirror, no new get_url). No `## Package Legitimacy Audit` gate applies (distro repo, not npm/pip/cargo). |
</threat_model>

<verification>
Phase-level checks (run after all three tasks, before commit):

1. **YAML validity** — all edited YAML parses:
   `python3 -c "import yaml; [yaml.safe_load(open(f)) for f in ['ansible/roles/xrdp/tasks/main.yml','ansible/roles/xrdp/defaults/main.yml','ansible/roles/hardening/defaults/main.yml','ansible/playbook.yml']]"`
2. **Hardening-stays-last invariant (grep-gate verbatim)** — MUST print `1` (post_tasks added in Task 3 are NOT role lines, so the gate is unaffected):
   `grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'role:[[:space:]]*hardening'`
3. **CIS 2.2.1 disabled at the correct site** — the override is in hardening/defaults, NOT the vendored role:
   `grep -Eq 'amzn2023cis_rule_2_2_1:[[:space:]]*false' ansible/roles/hardening/defaults/main.yml` AND
   `grep -Eq 'amzn2023cis_rule_2_2_1:[[:space:]]*true' ansible/roles/AMAZON2023-CIS/defaults/main.yml` (vendored default UNTOUCHED).
4. **Post-hardening X-server regression guard (W1)** — playbook.yml stats /usr/libexec/Xorg as a post_task gated on the xrdp+desktop layers:
   `grep -q '/usr/libexec/Xorg' ansible/playbook.yml` AND `grep -Eq 'when:.*layers\.xrdp.*and.*layers\.desktop' ansible/playbook.yml`.
5. **Airgap policy** — no new S3/mirror fetch; the X server + dbus come via dnf:
   `! grep -rqE 's3://|amazonaws.com/.*\.tar|private-mirror' ansible/roles/xrdp/` AND xorg-x11-server-Xorg/dbus-x11 are in an `ansible.builtin.dnf:` task.
6. **No `changeme` literal** in any new/edited tracked file: `! grep -rq 'changeme' ansible/roles/xrdp/ ansible/roles/hardening/`
7. **Layer gate** — xrdp requires both toggles: `grep -Eq 'when:.*layers\.xrdp.*and.*layers\.desktop' ansible/playbook.yml`
8. **sesman unit pinned (W2)** — no StopWhenUnneeded AND no BindsTo, WantedBy retained:
   `! grep -q 'StopWhenUnneeded' ansible/roles/xrdp/files/xrdp-sesman.service` AND `! grep -q 'BindsTo' ansible/roles/xrdp/files/xrdp-sesman.service` AND `grep -q 'WantedBy=multi-user.target' ansible/roles/xrdp/files/xrdp-sesman.service`.
9. **pam_loginuid explicit line (W3)** — the intended one-line directive is present:
   `grep -Eq '^[[:space:]]*session[[:space:]]+required[[:space:]]+pam_loginuid\.so' ansible/roles/xrdp/files/xrdp-sesman.pam`.
10. **RDP-13 catches the runtime** — `/usr/libexec/Xorg` + cert/key + startwm + PAM all stat'd and asserted (Task 3 verify).
11. **ansible-lint** — run `ansible-lint ansible/roles/xrdp/ ansible/roles/hardening/ ansible/playbook.yml` if available; treat only NEW findings as blocking (pre-existing `.ansible-lint` config-parse issue noted in STATE.md is not a blocker).
12. **11-01-PLAN.md preserved** — `git status` shows no modification to 11-01-PLAN.md.
</verification>

<success_criteria>
- xorg-x11-server-Xorg + dbus-x11 installed from the AL2023 core repo via dnf (airgap-compliant); a success-assert gates the install (CRITICAL #1).
- `amzn2023cis_rule_2_2_1: false` set in ansible/roles/hardening/defaults/main.yml with a documented desktop-exception comment; the vendored CIS default is untouched; the deviation is recorded in CLAUDE.md §8 (CRITICAL #2).
- A post-hardening post_task in playbook.yml asserts /usr/libexec/Xorg still exists AFTER the hardening role (gated on layers.xrdp AND layers.desktop), making the CIS-2.2.1 override regression-proof against a future CIS-role bump (W1).
- The xrdp role is gated on `(layers.xrdp) and (layers.desktop)`; hardening stays the last role; the grep-gate still prints 1 (CRITICAL #3 + W1).
- The in-role RDP-13 bake assert stats and asserts /usr/libexec/Xorg, /etc/xrdp/cert.pem, /etc/xrdp/key.pem, /etc/xrdp/startwm.sh, /etc/pam.d/xrdp-sesman (plus the original binary/module/ini stats + is-enabled), and asserts the cert carries a SAN — the bake fails if the X server or any runtime dep is absent; the in-role X-server fail_msg blames a skipped runtime-dep install, not CIS (CRITICAL #4 + W1).
- restorecon relabels /usr/libexec/Xorg + /usr/local/{sbin,bin,lib} + /etc/xrdp before hardening enforces SELinux (HIGH).
- The self-signed cert is FIPS-safe: RSA-2048, -sha256, subjectAltName=DNS:devbox (HIGH).
- xrdp-sesman.service has no StopWhenUnneeded and no BindsTo and keeps WantedBy=multi-user.target, ordered solely by xrdp.service's one-directional Requires=/After=, with a documented non-racing-dependency comment (HIGH, W2).
- The colord polkit allow-rule is delivered as an AL2023 polkit-121 JS .rules file in /etc/polkit-1/rules.d/; the dead .pkla is no longer installed (RISK).
- dbus-x11 confirmed installed (provides dbus-launch for startwm.sh) (RISK, covered by #1).
- pam_loginuid.so confirmed-safe: the explicit `session required pam_loginuid.so` line is asserted in files/xrdp-sesman.pam and justified by a comment in tasks/main.yml; the live-session effect is covered by the RDP-14 UAT gate (RISK, W3).
- Hardening-stays-last grep-gate passes; no `changeme`; all YAML parses; 11-01-PLAN.md untouched.
</success_criteria>

<output>
Create `.planning/phases/11-service-config-pam-session-bake-verification/11-02-SUMMARY.md` when done.
</output>
