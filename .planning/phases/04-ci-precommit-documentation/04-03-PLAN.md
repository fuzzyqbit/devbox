---
phase: 04-ci-precommit-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - ansible/firewalld-docker-fix.yml
autonomous: true
requirements: [DOC-01, DOC-02]
tags: [documentation, claude-md, operator-quickstart, firewalld-workaround, retirement-criteria]

must_haves:
  truths:
    - "A fresh-clone operator can read CLAUDE.md once and run `make packer-bake && make tg-apply && make start` successfully without consulting any other file"
    - "CLAUDE.md documents every prerequisite tool (aws, packer, tofu, terragrunt, ansible/ansible-lint, jq, gitleaks, pre-commit, shellcheck, session-manager-plugin)"
    - "CLAUDE.md documents the three `pre-commit install` invocations (incl. `--hook-type pre-push` and `--install-hooks`)"
    - "CLAUDE.md documents env vars: DEVBOX_USER, AWS_REGION/AWS_DEFAULT_REGION, AWS_PROFILE"
    - "CLAUDE.md documents the Phase 2 hybrid posture: SSM for SSH (`make devbox-ssm`, `make devbox-port-forward`) + CIDR allowlist for :8080/:6080 (`make devbox-allowlist-me`)"
    - "CLAUDE.md documents the per-operator SSH key rotation procedure (Phase 1 SEC-04)"
    - "CLAUDE.md documents secret rotation (`make packer-bake` re-rolls per-build secrets to SSM)"
    - "CLAUDE.md lists the 5 invariants from RESEARCH lines 830-835 (hardening last, lockfile committed, action-SHA-pin, packer SSM pin, no-changeme)"
    - "CLAUDE.md documents the open follow-up: Packer SSM `:NN` version pin needs AWS creds (Phase 3 deferred)"
    - "CLAUDE.md cross-references ansible/firewalld-docker-fix.yml in troubleshooting"
    - "ansible/firewalld-docker-fix.yml header now contains a numbered retirement-criteria block (any-of) making the workaround removable when conditions are met"
    - "Retirement criteria are testable (`firewall-cmd --get-default-zone` verification command included)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Operator quickstart, prerequisites, daily flow, rotations, troubleshooting, invariants, follow-ups"
      contains: "make packer-bake"
      min_lines: 180
    - path: "ansible/firewalld-docker-fix.yml"
      provides: "Existing workaround play, header expanded with explicit retirement criteria"
      contains: "Retirement criteria"
  key_links:
    - from: "CLAUDE.md troubleshooting section"
      to: "ansible/firewalld-docker-fix.yml header"
      via: "cross-reference link in the firewalld bullet"
      pattern: "firewalld-docker-fix"
    - from: "CLAUDE.md daily flow"
      to: "Makefile targets (packer-bake, tg-apply, start, devbox-ssm, secrets-show, devbox-allowlist-me)"
      via: "make target names quoted verbatim"
      pattern: "make packer-bake"
    - from: "ansible/firewalld-docker-fix.yml header"
      to: "ansible/roles/hardening/defaults/main.yml + ansible/layer_config.yml"
      via: "retirement-criteria #1 (CIS posture) and #2 (containers layer)"
      pattern: "containers"
---

<objective>
Populate the currently-empty `CLAUDE.md` (DOC-01) with the operator quickstart, and expand the FIXME header of `ansible/firewalld-docker-fix.yml` (DOC-02) with testable retirement criteria.

Purpose: Close DOC-01 + DOC-02. Make the repo self-documenting for a fresh-clone operator (no archeology through `.planning/` required), and make the firewalld-docker workaround removable rather than aspirational.

Output: `CLAUDE.md` (~200 lines covering all 9 RESEARCH-mandated sections); `ansible/firewalld-docker-fix.yml` with retirement criteria appended to the existing header (no playbook-task changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-precommit-documentation/04-RESEARCH.md
@.planning/phases/01-secrets-remediation/01-03-SUMMARY.md
@.planning/phases/03-reproducibility-version-pinning/03-01-SUMMARY.md
@.planning/phases/03-reproducibility-version-pinning/03-02-SUMMARY.md
@Makefile
@ansible/firewalld-docker-fix.yml
@CLAUDE.md

<interfaces>
<!-- CLAUDE.md template scope — verbatim outline from RESEARCH lines 742-839.
     DOC-01 says: "no more than ~400 lines total" (RESEARCH line 744).
     Below is the section list. Each must be present, in this order: -->

CLAUDE.md required sections:

  Section 1 — What this is (~5 lines): personal cloud workstation, AMI bake → EC2 provision, single-operator-per-instance, pointer to .planning/PROJECT.md
  Section 2 — Prerequisites: tools list (aws, session-manager-plugin, packer ≥ 1.12, tofu ≥ 1.10, terragrunt ≥ 0.81, ansible ≥ 2.16 + ansible-lint ≥ 26, jq, gitleaks ≥ 8.30, pre-commit ≥ 4.6, shellcheck ≥ 0.10, checkov ≥ 3.2 optional) + the 3 `pre-commit install` invocations
  Section 3 — Environment Variables: DEVBOX_USER, AWS_REGION / AWS_DEFAULT_REGION, AWS_PROFILE
  Section 4 — One-Time Per-Operator Setup: SSH keypair (ssh-keygen + aws ec2 import-key-pair) + CIDR allowlist (make devbox-allowlist-me)
  Section 5 — Daily Flow: packer-bake → tg-apply → start → devbox-ssm / devbox-port-forward → secrets-show → stop
  Section 6 — Rotations: SSH key, secrets (re-bake), CIDR allowlist
  Section 7 — Troubleshooting: session-manager-plugin missing, tofu missing, AMI not found, lockfile checksum mismatch, firewalld blocking ports (CROSS-REF to ansible/firewalld-docker-fix.yml), pre-commit slow on first run
  Section 8 — Invariants — Do Not Violate (5 entries from RESEARCH lines 830-835)
  Section 9 — Known Follow-Ups: SSM `:NN` pin (Phase 3 REP-04 deferred)

Verbatim Makefile target names (so the doc cannot drift from the actual surface — confirmed by Read of Makefile):
  init, validate, build, packer-bake, fmt
  tg-init, tg-reinit, tg-plan, tg-apply, tg-auto-apply, tg-destroy, tg-auto-destroy
  start, stop, status
  devbox-ssm, devbox-port-forward, devbox-allowlist-me
  secrets-show
  clean

Verbatim SSM parameter paths (Phase 1 SEC-03):
  /devbox/${DEVBOX_USER}/code-server-password
  /devbox/${DEVBOX_USER}/vnc-password

Phase 3 follow-up (must appear in §9):
  "Packer SSM parameter `:NN` version pin (Phase 3 REP-04 deferred — needs AWS creds to resolve current version; see `packer/devimage.pkr.hcl` lines 19-31 for the bump procedure; the Phase 4 grep gate `grep -E '/aws/service/ami-amazon-linux-latest/.*:[0-9]+'` will start failing once :NN is added — that is the intentional ratchet)"
  (Source: STATE.md line 73, 03-02-SUMMARY lines 93-131)

Phase 2 hybrid posture (must appear in §5 and §4):
  - SSM for SSH (eliminates :22 ingress entirely; AmazonSSMManagedInstanceCore attached to aws_iam_role.devbox)
  - CIDR allowlist for :8080 (code-server) and :6080 (noVNC), default `[]`, plan-time validation refuses apply when empty unless `var.allow_open_ingress=true`
  (Source: PROJECT.md line 28, 80)

Firewalld retirement criteria (DOC-02 — RESEARCH lines 846-869):
  1. CIS scan requirement lifted (relax firewalld dependency in ansible/roles/AMAZON2023-CIS/defaults/), OR switch to different hardening baseline → remove firewalld entirely, rely on EC2 SG (matches ansible/roles/hardening/defaults/main.yml comment lines 4-13)
  2. `containers` layer removed from ansible/layer_config.yml — no docker → no docker zone → set-default-zone fails → delete this play
  3. Per-port allowances added to `public` zone inside roles/hardening (option (a) from existing header line 14)
  Verification command: `firewall-cmd --get-default-zone` should be `public` (option a) OR the firewalld package must be absent (option b); the workaround state (default zone = docker) is NOT acceptable post-retirement
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CLAUDE.md operator quickstart (DOC-01)</name>
  <files>CLAUDE.md</files>
  <action>
Write `CLAUDE.md` (NEW content; file currently exists as 0 bytes — `wc -c CLAUDE.md` returns 0). Target: 180-280 lines.

Structure exactly per RESEARCH lines 742-839 (the DOC-01 template), with the 9 sections listed in <interfaces>. The RESEARCH file is the canonical content source — this task is largely transcription with project-specific edits.

**Mandatory deviations from the RESEARCH template:**

1. **Reference the actual Makefile surface, not the template's surface.** The RESEARCH was written before the executor confirmed all targets exist; cross-check every `make <target>` mention against the Makefile (read at plan time). Required targets present and verified: `packer-bake`, `tg-apply`, `start`, `stop`, `status`, `devbox-ssm`, `devbox-port-forward`, `devbox-allowlist-me`, `secrets-show`, `clean`, `init`, `validate`, `build`, `fmt`, `tg-init`, `tg-reinit`, `tg-plan`, `tg-auto-apply`, `tg-destroy`, `tg-auto-destroy`. (Confirmed from Makefile line 1 `.PHONY` declaration.)

2. **Section 2 — Prerequisites:** Use a single fenced bash block per OS family (macOS via brew, RHEL/Fedora via dnf) — not the comma-separated prose form from RESEARCH. Easier to copy-paste. Specifically:

   ```bash
   # macOS
   brew install awscli packer opentofu terragrunt ansible ansible-lint jq \
                gitleaks pre-commit shellcheck
   brew install --cask session-manager-plugin
   # Optional (CI is authoritative; install locally for `pre-commit run --hook-stage pre-push`):
   brew install checkov

   # Fedora/RHEL
   dnf install -y awscli packer opentofu terragrunt ansible-core jq \
                  gitleaks ShellCheck
   pip install ansible-lint==26.4.0 pre-commit==4.6.0 checkov==3.2.528
   # session-manager-plugin: download .rpm from AWS docs
   ```

3. **Section 4 — Per-operator setup:** include BOTH the SSH key import (Phase 1 SEC-04) AND the CIDR allowlist setup (Phase 2 NET-02/03). RESEARCH covered both but separated them; merge under "Step 1" / "Step 2" for clearer onboarding.

4. **Section 5 — Daily Flow:** Use Makefile target names verbatim. Include `make secrets-show` (RESEARCH line 811) — currently the only operator-facing secret-retrieval surface (Phase 1).

5. **Section 7 — Troubleshooting:** the firewalld bullet MUST link to `ansible/firewalld-docker-fix.yml` by path (relative from repo root). This is the cross-reference required by must_haves.key_links.

6. **Section 8 — Invariants:** Verbatim 5-bullet list from RESEARCH lines 830-835:
   - hardening must remain the last role in ansible/playbook.yml
   - terraform/.terraform.lock.hcl must stay committed and NEVER re-added to .gitignore (REP-01)
   - action SHA-pin policy: every `uses:` in .github/workflows/* MUST be 40-char hex
   - Packer source AMI must stay pinned via SSM parameterstore (REP-04 — CI greps `most_recent = true`)
   - `changeme` literal MUST NOT appear in any tracked code file (SEC-01/02 — pre-commit `no-changeme` hook enforces)

7. **Section 9 — Known Follow-Ups:** SSM `:NN` pin (verbatim text from <interfaces>).

8. **Markdown styling:**
   - H1 `# devbox` at the top
   - H2 for each numbered section (`## 1. What this is`, `## 2. Prerequisites`, ...)
   - Fenced code blocks with language hints (` ```bash `, ` ```yaml `)
   - No emoji (CLAUDE.md is read by both humans and Claude; emoji is noise — per system prompt "use emojis only if explicitly requested")
   - Tables for env-var summary if it helps readability; otherwise definition-list-style is fine

9. **Header note** (above H1): "_Operator quickstart for the devbox IaC repo. Last updated: 2026-05-14 (Phase 4 DOC-01). For project history and architecture, see `.planning/PROJECT.md`._"

**Constraints from coding-style.md:**
- File ≤ 400 lines (per RESEARCH line 744 budget) but ≥ 180 (per must_have artifact min_lines)
- No deep nesting (markdown sections at H2 only, occasionally H3)
- DRY: do not duplicate the Makefile target docstrings — link by reference ("see `make help`")
- KISS: prefer plain prose + fenced code over diagrams (RESEARCH already has the ASCII diagram; do not copy it — link)
- Single source of truth: invariants list lives ONLY here (Section 8) and in the corresponding gate location (ci.yml grep-gates + pre-commit grep-gates). Both gates reference this section by ID/comment.

DO NOT include marketing prose, "what's coming next", or roadmap teasers — RESEARCH line 744 budgets ~400 lines max; spend them on actionable operator content.
  </action>
  <verify>
    <automated>
test -f CLAUDE.md && \
  test "$(wc -l < CLAUDE.md)" -ge 180 && \
  test "$(wc -l < CLAUDE.md)" -le 400 && \
  grep -q '^# devbox' CLAUDE.md && \
  grep -q -i 'prerequisites' CLAUDE.md && \
  grep -q 'DEVBOX_USER' CLAUDE.md && \
  grep -q 'AWS_REGION' CLAUDE.md && \
  grep -q 'make packer-bake' CLAUDE.md && \
  grep -q 'make tg-apply' CLAUDE.md && \
  grep -q 'make start' CLAUDE.md && \
  grep -q 'make stop' CLAUDE.md && \
  grep -q 'make devbox-ssm' CLAUDE.md && \
  grep -q 'make devbox-port-forward' CLAUDE.md && \
  grep -q 'make devbox-allowlist-me' CLAUDE.md && \
  grep -q 'make secrets-show' CLAUDE.md && \
  grep -q 'pre-commit install' CLAUDE.md && \
  grep -q 'pre-commit install --hook-type pre-push' CLAUDE.md && \
  grep -q 'pre-commit install --install-hooks' CLAUDE.md && \
  grep -q 'session-manager-plugin' CLAUDE.md && \
  grep -q 'ssh-keygen' CLAUDE.md && \
  grep -q 'aws ec2 import-key-pair' CLAUDE.md && \
  grep -q 'ansible/firewalld-docker-fix.yml' CLAUDE.md && \
  grep -q 'terraform/.terraform.lock.hcl' CLAUDE.md && \
  grep -q -i 'hardening' CLAUDE.md && \
  grep -q -i 'invariant' CLAUDE.md && \
  grep -q 'SSM' CLAUDE.md && \
  grep -qi 'follow-up\|follow_up\|known issue' CLAUDE.md && \
  ! grep -q ':emoji:\|🎉\|🚀\|✨' CLAUDE.md
    </automated>
  </verify>
  <done>
- CLAUDE.md exists at repo root, 180-400 lines, single H1 `# devbox`
- All 9 sections present in order (What this is, Prerequisites, Env Vars, Per-Operator Setup, Daily Flow, Rotations, Troubleshooting, Invariants, Known Follow-Ups)
- Every Makefile target in the documented daily flow is grep-able in CLAUDE.md
- Three `pre-commit install` invocations documented (Phase 4 plan 04-02 requirement)
- SSH key import + CIDR allowlist (Phase 1 SEC-04 + Phase 2 NET-02/03) covered
- Firewalld troubleshooting bullet links to `ansible/firewalld-docker-fix.yml`
- 5 invariants verbatim from RESEARCH lines 830-835
- SSM `:NN` follow-up captured (Phase 3 REP-04 deferred)
- No emoji
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand ansible/firewalld-docker-fix.yml header with retirement criteria (DOC-02)</name>
  <files>ansible/firewalld-docker-fix.yml</files>
  <action>
**APPEND, not restructure.** The existing header (lines 1-26 of `ansible/firewalld-docker-fix.yml`) already covers WHAT and WHY (Read at plan time — verified). DOC-02's gap is the WHEN-TO-RETIRE criteria.

**Step 1 — Read current state:** the file currently ends its header at line 26 ("Skip it otherwise (no docker → no `docker` zone → set-default-zone fails).") and begins the play at line 27 (`- name: firewalld default zone = docker (workaround)`).

**Step 2 — Insert retirement-criteria block BETWEEN line 26 and line 27** (preserve every byte before and after; do not touch any task body, do not reformat existing comments).

The inserted block (per RESEARCH lines 846-869, verbatim with minor formatting normalization to match existing comment style — same `#` prefix, same indent, same column width):

```yaml
# Retirement criteria — this play SHOULD be deleted when ANY of:
#   (1) The CIS scan requirement that demands `firewalld present and running`
#       is lifted from this project's hardening posture. Today, the AL2023 CIS
#       role's defaults (ansible/roles/AMAZON2023-CIS/defaults/) require firewalld
#       to be installed. If a future CIS profile relaxes this, OR we switch to a
#       different hardening baseline (e.g., DISA STIG), this workaround is moot —
#       remove firewalld entirely and rely on the EC2 SG. This matches the comment
#       at ansible/roles/hardening/defaults/main.yml lines 4-13.
#
#   (2) The `containers` layer is removed from `ansible/layer_config.yml`. Without
#       Docker, no `docker` zone is registered, the default-zone-to-docker rewrite
#       is moot, and the host can stay on the stock `public` zone. Remove this play.
#
#   (3) Per-port allowances are added to the `public` zone (option (a) from line
#       14 above) inside the `roles/hardening` role itself — e.g., a
#       firewalld_port: list iterated over with the firewalld module. Once those
#       are in place, this workaround is redundant; delete it.
#
# Verification of retirement: after removing this play, `make packer-bake` must
# succeed and the resulting AMI's `firewall-cmd --get-default-zone` must be
# either `public` (option a) or the firewalld package must be absent (option b).
# Either is acceptable; the workaround state (default zone = docker) is NOT.
#
# Cross-reference: CLAUDE.md Troubleshooting → "firewalld blocking ports inside
# the AMI" mentions this header as the canonical explanation.
```

The closing cross-reference comment is a new addition (not in RESEARCH) — it documents the bidirectional link required by must_haves.key_links.

**Step 3 — Verify the inserted block does not break Ansible parsing.** The block lives in the YAML document's leading comments (before the first `- name:` line) which Ansible's YAML parser treats as a no-op. Run `ansible-playbook --syntax-check ansible/firewalld-docker-fix.yml -i localhost,` if `ansible-playbook` is on PATH; otherwise rely on the smoke test in Task 3.

**Constraints from coding-style.md:**
- File ≤ 200 lines (currently 61 lines; +25 inserted = ~86 lines projected)
- No changes to the actual playbook tasks (immutability of working code per coding-style.md)
- Single source of truth: retirement criteria live ONLY here; CLAUDE.md cross-references this file rather than restating

**Do NOT:**
- Change indentation of existing tasks
- Reorder existing comment lines
- Add `tags:` or other YAML metadata not already present
- Touch the `meta: end_play` early-exit task at line 31-33
  </action>
  <verify>
    <automated>
test -f ansible/firewalld-docker-fix.yml && \
  grep -q '^# Retirement criteria' ansible/firewalld-docker-fix.yml && \
  grep -q 'CIS scan requirement' ansible/firewalld-docker-fix.yml && \
  grep -q '`containers` layer is removed' ansible/firewalld-docker-fix.yml && \
  grep -q 'Per-port allowances' ansible/firewalld-docker-fix.yml && \
  grep -q 'firewall-cmd --get-default-zone' ansible/firewalld-docker-fix.yml && \
  grep -q 'CLAUDE.md' ansible/firewalld-docker-fix.yml && \
  grep -q '^- name: firewalld default zone = docker (workaround)$' ansible/firewalld-docker-fix.yml && \
  grep -q 'meta: end_play' ansible/firewalld-docker-fix.yml && \
  test "$(wc -l < ansible/firewalld-docker-fix.yml)" -le 200 && \
  python -c "import yaml; list(yaml.safe_load_all(open('ansible/firewalld-docker-fix.yml')))"
    </automated>
  </verify>
  <done>
- ansible/firewalld-docker-fix.yml retains every original task body byte-identical
- New Retirement-criteria block inserted between the existing header and the play opener
- All 3 retirement options documented (CIS lifted, containers removed, per-port allowances added)
- Verification command (`firewall-cmd --get-default-zone`) present
- Cross-reference to CLAUDE.md present (bidirectional link with Task 1)
- File still parses as valid YAML
- No task-body changes (immutability of working code)
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-link sanity + commits</name>
  <files>CLAUDE.md, ansible/firewalld-docker-fix.yml</files>
  <action>
**Smoke 1 — Bidirectional cross-link:**
```
grep -q 'ansible/firewalld-docker-fix.yml' CLAUDE.md && \
  grep -q 'CLAUDE.md' ansible/firewalld-docker-fix.yml && \
  echo "Bidirectional link OK"
```
Both must echo OK.

**Smoke 2 — Makefile-target reality check:**
For every `make <target>` mentioned in CLAUDE.md, verify the target exists:
```
for t in packer-bake tg-apply start stop status devbox-ssm devbox-port-forward devbox-allowlist-me secrets-show; do
  grep -qE "^${t}:" Makefile || echo "MISSING TARGET: $t"
done
```
Output should be empty (no MISSING TARGET lines). If any target is missing from the actual Makefile, the doc is documenting non-existent UX; STOP and report to orchestrator — do NOT delete the doc bullet (would be scope reduction per planner_authority_limits); fix the Makefile or escalate.

**Smoke 3 — Plan 04-01 + 04-02 file references:**
CLAUDE.md mentions `.checkov.yaml` and `.ansible-lint`? It should mention them at most as "CI configuration" — not as files operators need to edit. Verify the doc does NOT promise an operator-side checkov config file that doesn't exist in the operator's workflow.
```
grep -q '\.checkov\.yaml' CLAUDE.md && echo "Doc references .checkov.yaml — is this for operators or for CI? Verify context."
```

**Smoke 4 — gitleaks against new docs** (planning-doc allowlist applies — RESEARCH line 14 / .gitleaks.toml lines 10-13):
```
gitleaks detect --no-git --source CLAUDE.md --config .gitleaks.toml --no-banner
gitleaks detect --no-git --source ansible/firewalld-docker-fix.yml --config .gitleaks.toml --no-banner
```
Both must exit 0. If a planted-looking string in CLAUDE.md ("AKIA-style key example") matches the upstream allowlist (per 01-03-SUMMARY note that gitleaks v8.30.1 has built-in AWS-docs allowlist for `AKIAIOSFODNN7EXAMPLE`), the file passes.

**Smoke 5 — ansible-playbook syntax check on firewalld-docker-fix.yml** (if ansible on PATH):
```
ansible-playbook --syntax-check ansible/firewalld-docker-fix.yml -i localhost,
```
Must exit 0. If `ansible-playbook` not on PATH, fall back to YAML-only check:
```
python -c "import yaml; list(yaml.safe_load_all(open('ansible/firewalld-docker-fix.yml')))" && echo OK
```

**Smoke 6 — markdown linting** (lightweight; if `markdownlint` is on PATH):
```
markdownlint CLAUDE.md 2>&1 | tee /tmp/md.log
```
Non-fatal. Capture warnings in SUMMARY; only fix structural issues (broken code blocks, missing headers); cosmetic warnings (line-length, etc.) are accepted.

**Commits** (two atomic commits per coding-style.md and git-workflow.md):
```
gsd-sdk query commit "docs(phase-04-03): populate CLAUDE.md operator quickstart (DOC-01)" CLAUDE.md
gsd-sdk query commit "docs(phase-04-03): expand firewalld-docker-fix.yml retirement criteria (DOC-02)" ansible/firewalld-docker-fix.yml
```

**Final self-check:**
- `git log -2 --oneline` shows both commits
- `git status` clean
- `git diff HEAD~2 -- .github/workflows/` empty (plan 04-01 boundary)
- `git diff HEAD~2 -- .pre-commit-config.yaml` empty (plan 04-02 boundary)
- `git diff HEAD~2 -- .checkov.yaml .ansible-lint` empty (plan 04-01 boundary)
- `git diff HEAD~2 -- Makefile` empty (no Makefile changes from this plan)
- `git diff HEAD~2 -- ansible/` shows only the firewalld-docker-fix.yml header expansion (no role changes)
  </action>
  <verify>
    <automated>
git log -2 --pretty=format:'%s' | grep -q 'docs(phase-04-03): populate CLAUDE.md' && \
  git log -2 --pretty=format:'%s' | grep -q 'docs(phase-04-03): expand firewalld' && \
  test -z "$(git status --porcelain CLAUDE.md ansible/firewalld-docker-fix.yml)" && \
  test -z "$(git diff HEAD~2 -- .github/workflows/)" && \
  test -z "$(git diff HEAD~2 -- .pre-commit-config.yaml)" && \
  test -z "$(git diff HEAD~2 -- Makefile)" && \
  for t in packer-bake tg-apply start stop status devbox-ssm devbox-port-forward devbox-allowlist-me secrets-show; do \
    grep -qE "^${t}:" Makefile || { echo "MISSING $t"; exit 1; }; \
  done
    </automated>
  </verify>
  <done>
- All 6 smokes recorded in SUMMARY (pass/skip-with-reason)
- Bidirectional cross-link between CLAUDE.md and ansible/firewalld-docker-fix.yml verified
- Every documented Makefile target exists in the actual Makefile (no doc-vs-reality drift)
- gitleaks clean against new docs
- Two atomic docs commits (one per requirement: DOC-01, DOC-02)
- `git status` clean; plan boundary respected (no diff against ci.yml, .pre-commit-config.yaml, .checkov.yaml, .ansible-lint, Makefile, or any ansible role)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation content -> operator action | a wrong command in CLAUDE.md becomes a productized error every fresh operator hits |
| CLAUDE.md cross-ref -> ansible/firewalld-docker-fix.yml | a broken link sends operators down the wrong debugging path |
| copy-pasted secrets-example in CLAUDE.md -> gitleaks pre-commit | a poorly-chosen example credential could trigger the gate |

## STRIDE Threat Register

| Threat ID  | Category    | Component                                  | Disposition | Mitigation Plan |
|------------|-------------|--------------------------------------------|-------------|-----------------|
| T-04-18    | Information disclosure | CLAUDE.md example credentials   | mitigate    | Use placeholder strings (`<your-region>`, `${USER}`, `ami-XXX`); NO actual key values, real ARNs, real account IDs. gitleaks Smoke 4 catches any AKIA-pattern slip. Existing .gitleaks.toml allowlist (Phase 1) covers canonical AWS-docs example creds in `.md` files only. |
| T-04-19    | Tampering   | doc drift from Makefile                    | mitigate    | Task 3 Smoke 2 enforces: every documented `make <target>` MUST exist in Makefile. CI plan 04-01 does not enforce this (no doc-drift gate); follow-up could add a `grep -E '^[a-z-]+:' Makefile` cross-check, but for now Smoke 2 + the in-plan reality check is the gate. |
| T-04-20    | Tampering   | doc drift from .pre-commit-config.yaml     | mitigate    | The 3 `pre-commit install` invocations are documented in BOTH CLAUDE.md and the .pre-commit-config.yaml header comment (per plan 04-02). Duplication is intentional defense-in-depth for first-clone discovery. |
| T-04-21    | Repudiation | "I didn't know about the invariants"       | mitigate    | Section 8 lists 5 invariants verbatim; pre-commit `grep-gates` hook (plan 04-02) and ci.yml `grep-gates` job (plan 04-01) enforce them mechanically. Documentation is informational; the gates are authoritative. |
| T-04-22    | EoP         | retirement-criteria misinterpretation      | mitigate    | DOC-02 makes the 3 criteria explicitly OR-joined ("ANY of") and each is testable (CIS posture, layer_config.yml content, role contents). Verification command (`firewall-cmd --get-default-zone`) makes the "did retirement land" check binary. |
| T-04-23    | Information disclosure | leaked SSM parameter paths in docs | accept | `/devbox/${DEVBOX_USER}/*` is the parameter prefix — knowing the path is not sensitive; the SecureString contents (Phase 1 SEC-03) and the IAM scoping enforce access control. Documenting the path is necessary for operator UX. |
</threat_model>

<verification>
**Coverage:**
- DOC-01 satisfied by Task 1: CLAUDE.md populated with 9 sections covering quickstart, env vars, prerequisites, daily flow, rotations, troubleshooting, invariants, follow-ups
- DOC-02 satisfied by Task 2: ansible/firewalld-docker-fix.yml header expanded with 3 retirement criteria + verification command

**Boundary checks:**
- This plan does NOT modify `.github/workflows/*` (plan 04-01 territory)
- This plan does NOT modify `.pre-commit-config.yaml` (plan 04-02 territory)
- This plan does NOT modify `.checkov.yaml` or `.ansible-lint` (plan 04-01 territory)
- This plan does NOT modify any Ansible role tasks (only the firewalld workaround YAML's header comment)
- This plan does NOT modify the Makefile (just documents what's already there)

**Plan ordering:**
- Wave 1 sibling with 04-01, 04-02 — disjoint files
- No dependency on 04-01 or 04-02 landing first; CLAUDE.md can reference `.pre-commit-config.yaml` setup independently of whether plan 04-02 has landed
</verification>

<success_criteria>
- All `must_haves.truths` measurable via the Task 3 automated verify
- CLAUDE.md is the single source of truth for operator quickstart; no `.planning/`-archeology required
- Firewalld retirement criteria are testable (not aspirational) — each option has a measurable trigger
- Bidirectional cross-link survives (`grep CLAUDE.md` in firewalld-docker-fix.yml AND `grep firewalld-docker-fix.yml` in CLAUDE.md)
- Two atomic docs commits; no scope creep into other plan boundaries
- File line counts within bounds (CLAUDE.md 180-400 lines; firewalld-docker-fix.yml ≤ 200 lines)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-precommit-documentation/04-03-SUMMARY.md`. Capture:
- Smoke 2 result — full list of `make <target>` mentions in CLAUDE.md vs Makefile presence (must be 100% match)
- Smoke 6 markdownlint output — list of warnings, with rationale for each accepted-as-cosmetic
- Final CLAUDE.md line count (target 180-400)
- Final ansible/firewalld-docker-fix.yml line count (target ≤ 200; baseline 61)
- Whether Smoke 5 ran `ansible-playbook --syntax-check` (executor has ansible) or fell back to YAML-only check
- Any deviations from RESEARCH lines 742-839 template (each deviation requires a sentence of rationale per Rule 4 of `~/.claude/get-shit-done/references/`)
</output>
</content>
</invoke>