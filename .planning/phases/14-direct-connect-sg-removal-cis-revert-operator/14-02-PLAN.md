---
phase: 14-direct-connect-sg-removal-cis-revert-operator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - run
  - scripts/devbox-start.sh
  - scripts/devbox-status.sh
  - ansible/firewalld-docker-fix.yml
  - ansible/roles/secrets/templates/devbox-secrets-bootstrap.service.j2
  - ansible/roles/secrets/templates/devbox-secrets-bootstrap.sh.j2
  - CLAUDE.md
autonomous: true
requirements: [DCV-10, DCV-07]
must_haves:
  truths:
    - "secrets-show labels the desktop credential 'DCV login (ec2-user @ <host>:8443)' and still resolves /devbox/<user>/vnc-password"
    - "Operator docs/scripts point to direct https://<host>:8443 DCV connect with NO ./run port-forward step for DCV"
    - "The secrets-bootstrap unit orders Before=...dcvserver.service (not xrdp) and the restart loop names dcvserver.service (not xrdp)"
    - "The :8080 code-server port-forward guidance is retained; only the :3389/RDP port-forward guidance is removed"
    - "shellcheck/ansible-lint stay clean on the edited files"
  artifacts:
    - path: "run"
      provides: "DCV-relabelled secrets-show + help; :3389 port-forward example removed; vnc-password fetch path kept"
      contains: "DCV login"
    - path: "ansible/roles/secrets/templates/devbox-secrets-bootstrap.service.j2"
      provides: "Before=code-server.service dcvserver.service"
      contains: "dcvserver.service"
    - path: "ansible/roles/secrets/templates/devbox-secrets-bootstrap.sh.j2"
      provides: "restart loop 'for svc in code-server.service dcvserver.service'"
      contains: "dcvserver.service"
  key_links:
    - from: "devbox-secrets-bootstrap.sh.j2 restart loop"
      to: "dcvserver.service"
      via: "for svc in code-server.service dcvserver.service"
      pattern: "for svc in code-server.service dcvserver.service"
---

<objective>
Relabel the entire operator-facing surface from RDP/noVNC→DCV with **direct `:8443` connect** (DCV-10), and fix the carried CRITICAL-1 secrets-bootstrap unit so it orders/restarts `dcvserver.service` instead of the dead `xrdp.service`/`xrdp-sesman.service` (part of DCV-07). This is additive — it makes the human-facing surface DCV-correct while the xrdp Ansible role files still exist (removed in Wave 2). The `vnc-password` SSM credential path is KEPT everywhere (relabel-only).

Purpose: DCV-10 mandates direct connect (browser web client or native DCV client at `https://<host>:8443`, within the allowed CIDR) with NO `./run` port-forward step for DCV. CRITICAL-1 (carried from Phase 13 verification) leaves the bootstrap unit ordering `Before=...xrdp` and restarting xrdp unit names — both dead after removal; this swaps them to `dcvserver.service` (KISS: `dcvserver.service` alone is sufficient — `authentication=system` reads the live PAM password at connect, so the virtual-session unit need not be restarted on rotation — YAGNI).
Output: edited `run`, `scripts/devbox-{start,status}.sh`, `ansible/firewalld-docker-fix.yml` (comment only), the two `devbox-secrets-bootstrap.*.j2` templates, and `CLAUDE.md`.

NOTE — CLAUDE.md is git-ignored: edit it on disk, do NOT stage/commit it (the executor's commit must not include CLAUDE.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-RESEARCH.md

<interfaces>
<!-- run:391-395 port-forward inline examples (3389/RDP); run:441-473 cmd_secrets_show (vnc fetch run:458-460 KEEP, label run:470, err run:462); run:516-522 help (devbox-port-forward example :518, secrets-show desc :522). -->
<!-- The cmd_devbox_port_forward FUNCTION stays (serves :8080 / jupyter); only the 3389/RDP EXAMPLE comment lines are removed. Do NOT add an :8443 port-forward example. -->
<!-- scripts/devbox-start.sh:70,72 ; scripts/devbox-status.sh:54,61 — RDP :3389 + port-forward-3389 hints (relabel DCV :8443 direct, drop the 3389 hint, KEEP the :8080 hint). -->
<!-- ansible/firewalld-docker-fix.yml:6 comment '(code-server :8080, RDP :3389)' — comment only, body unchanged. -->
<!-- secrets-bootstrap.service.j2:2 Description, :5 Before= ; secrets-bootstrap.sh.j2:52-53 comment, :65 comment, :67 restart loop. KEEP chpasswd + the /devbox/<u>/vnc-password fetch. -->
<!-- CLAUDE.md (git-ignored, on-disk edit only): :9, §4 step2 :87/:89, §5 daily-flow :120-122 (DELETE the devbox-port-forward 3389 block, keep :8080 line), :125-126, :129-130, §7 troubleshooting :189-193 (rewrite the RDP-can't-connect entry → DCV :8443), §8/§7 invariant note :220-227 (CIS 2.2.1 now re-enabled; DCV uses Xdcv; post-hardening Xdcv assert). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: secrets-bootstrap unit — swap xrdp→dcvserver (CRITICAL-1)</name>
  <files>ansible/roles/secrets/templates/devbox-secrets-bootstrap.service.j2, ansible/roles/secrets/templates/devbox-secrets-bootstrap.sh.j2</files>
  <action>In `devbox-secrets-bootstrap.service.j2`: line 2 Description `...code-server / RDP` → `...code-server / DCV`; line 5 `Before=code-server.service xrdp.service xrdp-sesman.service` → `Before=code-server.service dcvserver.service`. In `devbox-secrets-bootstrap.sh.j2`: (a) comment lines 52-53 — reword "Apply the credential as the ec2-user RDP/PAM login password. xrdp authenticates the session via /etc/pam.d/xrdp-sesman -> password-auth..." → "Apply the credential as the ec2-user PAM login password. DCV authenticates the session via authentication=system (PAM -> system-auth), so this sets the password the DCV client logs in with." KEEP the `printf ... | chpasswd` line unchanged. (b) comment line 65 — "...the RDP units are only present when the desktop/xrdp layers were enabled..." → "...dcvserver is only present when the desktop/dcv layers were enabled...". (c) restart loop line 67 — `for svc in code-server.service xrdp.service xrdp-sesman.service; do` → `for svc in code-server.service dcvserver.service; do` (KISS — do NOT speculatively add dcv-virtual-session.service; YAGNI). KEEP the `/devbox/$DEVBOX_USER/vnc-password` SSM fetch (sh.j2:32-34) and all chpasswd/guard logic UNCHANGED.</action>
  <verify>
    <automated>cd ansible/roles/secrets/templates && ! grep -qi 'xrdp' devbox-secrets-bootstrap.service.j2 devbox-secrets-bootstrap.sh.j2 && grep -q 'Before=code-server.service dcvserver.service' devbox-secrets-bootstrap.service.j2 && grep -q 'for svc in code-server.service dcvserver.service' devbox-secrets-bootstrap.sh.j2 && grep -q 'vnc-password' devbox-secrets-bootstrap.sh.j2 && echo PASS</automated>
  </verify>
  <done>Neither template contains `xrdp`; the service unit orders `Before=code-server.service dcvserver.service`; the restart loop is `code-server.service dcvserver.service`; the `vnc-password` SSM fetch + chpasswd logic survive intact.</done>
</task>

<task type="auto">
  <name>Task 2: run — relabel secrets-show + help to DCV :8443, drop the :3389 port-forward example</name>
  <files>run</files>
  <action>In `cmd_secrets_show`: line 462 error `RDP login password not found at ...` → `DCV login password not found at /devbox/${DEVBOX_USER}/vnc-password`; line 470 echo `RDP login (ec2-user @ <host>:3389) password:` → `DCV login (ec2-user @ <host>:8443) password:`. KEEP the `vnc-password` SSM fetch at run:458-460 UNCHANGED. In `cmd_devbox_port_forward`: remove the `3389 -> RDP` example comment lines (run:393-394) and the `8080 3389` combined example (run:395) — keep the function body and the `:8080`/`8888:18888` examples (the function still serves code-server + jupyter). Do NOT add an :8443 port-forward example (DCV is direct connect). In `cmd_help`: the `devbox-port-forward` example line (run:518) `./run devbox-port-forward 8080 3389 8888:18888` → drop the `3389` token (e.g. `./run devbox-port-forward 8080 8888:18888`); the secrets-show description (run:522) `code-server and RDP login passwords` → `code-server and DCV login passwords`. No `make`-target tokens introduced (CLAUDE.md §8 grep-gate).</action>
  <verify>
    <automated>! grep -qiE 'RDP|3389' run && grep -q 'DCV login (ec2-user @ <host>:8443)' run && grep -q '/devbox/${DEVBOX_USER}/vnc-password' run && bash -n run && (command -v shellcheck >/dev/null && shellcheck -S error run || true) && echo PASS</automated>
  </verify>
  <done>No `RDP`/`3389` text in `run`; secrets-show prints the DCV :8443 label; the vnc-password fetch path is intact; `bash -n run` parses; shellcheck (if present) reports no errors.</done>
</task>

<task type="auto">
  <name>Task 3: scripts + firewalld comment + CLAUDE.md — direct :8443, keep :8080 port-forward</name>
  <files>scripts/devbox-start.sh, scripts/devbox-status.sh, ansible/firewalld-docker-fix.yml, CLAUDE.md</files>
  <action>scripts/devbox-start.sh: line 70 `RDP desktop: ${PRIVATE_IP}:3389 (native RDP client...)` → `DCV desktop: https://${PRIVATE_IP}:8443 (browser web client or native DCV client; reachable from VPC; requires your CIDR in allowed_web_cidrs)`; line 72 — drop the `'./run devbox-port-forward 3389' for RDP` clause, KEEP the `:8080` code-server port-forward hint. scripts/devbox-status.sh: line 54 same RDP→DCV :8443 direct relabel; line 61 — drop the `'./run devbox-port-forward 3389' -> an RDP client` clause, KEEP `https://localhost:8080`. ansible/firewalld-docker-fix.yml: line 6 comment `(code-server :8080, RDP :3389)` → `(code-server :8080, DCV :8443)` — comment ONLY, the play body (sets zone=docker) is unchanged (workaround-layout MEMORY: this stays its own named playbook). CLAUDE.md (git-ignored — edit on disk, DO NOT git add): line 9 `RDP desktop on :3389` → `Amazon DCV remote desktop on :8443`; §4 Step 2 (:87,:89) `code-server / RDP` + `:3389 (RDP)` → `code-server / DCV` + `:8443 (DCV, TCP+UDP)`; §5 daily-flow (:120-122) DELETE the `./run devbox-port-forward 3389 # tunnel RDP` block, KEEP the :8080 port-forward line above it; (:125-126) `native RDP client at <host>:3389` → `Amazon DCV web client (browser) or native DCV client at https://<host>:8443`; (:129-130) `vnc-password param is the RDP/PAM login password` → `...the DCV/PAM login password`; §7 troubleshooting (:189-193) rewrite the `RDP client can't connect to :3389` entry as `DCV client can't connect to :8443`: confirm CIDR in var.allowed_web_cidrs, the :8443 TCP AND UDP ingress applied (`./run tf-apply`), browse `https://<host>:8443` or use a native DCV client, log in as ec2-user with `./run secrets-show` (no SSM port-forward — direct connect); §8/§7 invariant note (:220-227) rewrite: CIS 2.2.1 is now RE-ENABLED (the exception is reverted in v4.0); DCV uses its own /usr/bin/Xdcv virtual X server, not the system Xorg, so the exception is no longer needed; a post-hardening assert stats /usr/bin/Xdcv to make a regression loud. Replace any `/etc/pam.d/xrdp-sesman` doc reference with DCV `authentication=system` (system-auth/password-auth).</action>
  <verify>
    <automated>! grep -qiE 'RDP|3389|xrdp' scripts/devbox-start.sh scripts/devbox-status.sh ansible/firewalld-docker-fix.yml && grep -q ':8443' scripts/devbox-start.sh && grep -q ':8443' scripts/devbox-status.sh && grep -q ':8080' scripts/devbox-status.sh && bash -n scripts/devbox-start.sh && bash -n scripts/devbox-status.sh && ! grep -qiE ':3389|RDP|xrdp' CLAUDE.md && echo PASS</automated>
  </verify>
  <done>No `RDP`/`3389`/`xrdp` in the two scripts, the firewalld-fix comment, or CLAUDE.md; both scripts surface DCV :8443 direct and retain the :8080 code-server port-forward hint; both scripts pass `bash -n`; CLAUDE.md left unstaged (git-ignored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot bootstrap → SSM → instance credential | The oneshot fetches the ec2-user PAM password from SSM SecureString and applies it via chpasswd; service ordering governs first-boot races |
| operator → secrets-show → SSM | Operator-facing credential read (unchanged; relabel-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Denial of Service / Repudiation | secrets-bootstrap Before=/restart loop naming dead xrdp units | mitigate | Swap to `dcvserver.service` so the oneshot orders/restarts the real DCV service — removes the negligible first-boot race and the dead unit names (the `systemctl list-unit-files` guard already tolerates absent units) |
| T-14-05 | Information Disclosure | vnc-password SSM credential path | accept | KEPT unchanged (relabel-only); SecureString + IMDSv2 + instance-profile creds; `printf|chpasswd` keeps cleartext out of the process tree (unchanged) |
| T-14-06 | Tampering | direct-connect operator docs (no SSM tunnel for DCV) | accept | Docs direct operators to the CIDR-gated :8443 perimeter; no port-forward step removes a tunnel surface; consistent with the SG (Plan 01) |
</threat_model>

<verification>
- `! git grep -nE 'xrdp|3389|RDP' -- run scripts/ ansible/firewalld-docker-fix.yml ansible/roles/secrets/templates/` → empty (operator-surface + bootstrap clean; full repo-wide gate is Plan 04).
- secrets-bootstrap: `grep -q 'Before=code-server.service dcvserver.service'` and `grep -q 'for svc in code-server.service dcvserver.service'`.
- Surviving-credential checks: `vnc-password` present in `run`, `devbox-secrets-bootstrap.sh.j2`.
- `bash -n run scripts/devbox-start.sh scripts/devbox-status.sh` all parse; `ansible-lint ansible/roles/secrets/` clean.
- CLAUDE.md edited on disk but NOT staged (git-ignored).
</verification>

<success_criteria>
Operator surface (run/scripts/firewalld-comment/CLAUDE.md) targets direct DCV :8443 with the :8080 port-forward retained and no DCV port-forward step; secrets-bootstrap unit orders/restarts dcvserver.service with the vnc-password credential intact; shellcheck/ansible-lint/bash -n clean.
</success_criteria>

<output>
Create `.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-02-SUMMARY.md` when done.
</output>
