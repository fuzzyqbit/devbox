---
context: phase
phase: 14-direct-connect-sg-removal-cis-revert-operator
status: code-executed-awaiting-adversarial-review-then-phase-15-uat
last_updated: 2026-06-19
branch: feat/dcv
---

# Resume point — v4.0 DCV (Phase 14 code DONE, on feat/dcv, pushed through 1773e06)

## Done
- **Credential rename:** vnc-password → desktop-password (purpose-based; tech-agnostic). Commit 47f68f4. Verified zero vnc residue.
- **Phase 13 (dcv role):** COMPLETE + verified PASSED (bake-config). dcv role built (51c5f1f port + QUIC-on dcv.conf, FIPS cert /etc/dcv/dcv.{pem,key} 0600 dcv:dcv, oneshot VIRTUAL session w/ GNOME-on-Xorg --init, SELinux relabel, bake assert, before hardening). 13-VERIFICATION.md written.
- **Phase 14 (SG + xrdp removal + CIS revert + operator + docs):** CODE EXECUTED (commits caacb14, 7575ba0, 2dcbe60, e4d2ab2, 1773e06). xrdp COMPLETELY removed — independently verified: repo-wide grep (excl .planning, ansible/roles/dcv/**, .gitlab-ci.yml) for `xrdp|xorgxrdp|:3389|\bRDP\b|tigervnc|vncserver|novnc` = EMPTY. role dir + test-xrdp.yml gone; CIS 2.2.1 override reverted; post-hardening guard RETARGETED /usr/libexec/Xorg→/usr/bin/Xdcv (gated layers.dcv+desktop); secrets-bootstrap xrdp→dcvserver; SG :8443 TCP+UDP gated, :3389 dropped, rdp_endpoint→dcv_endpoint, tofu validate ok; docs renamed HOWTO-...-RDP.md→-DCV.md + rewritten; hardening last=1; dcv role + desktop-password credential intact. STATE/ROADMAP/REQUIREMENTS mark DCV-06..10 complete, Phase 14 Complete 5/5.

## Pending (resume here)
1. **Adversarial review of Phase 14** (NOT yet run — skipped under context pressure; the user accepted "execute now"). This is the one discipline that's caught a green-but-broken issue in EVERY prior phase. Run an opus adversarial review of the committed removal/SG/CIS-revert hunting: does reverting CIS 2.2.1 actually hold (the retargeted guard stats /usr/bin/Xdcv — confirm that's the real nice-xdcv binary path; `repoquery --requires nice-xdcv` for a hidden xorg-x11-server-common dep); does dropping :3389 + opening :8443 TCP+UDP leave the SG correct; any over-removal (gnome-session/mesa/code-server/jupyter/credential). Then author 14-VERIFICATION.md.
2. **Phase 15 (DCV-11) live UAT — human/AWS, can't automate:** ./run build → apply → DIRECT :8443 TCP+UDP connect (in-CIDR) as ec2-user → GNOME virtual session renders + license resolves + SELinux AVC-clean enforcing + FIPS TLS handshake + QUIC path + CIS-2.2.1-revert safe. Plus `repoquery --requires nice-xdcv` to confirm the CIS-revert safety.

## State
- feat/dcv pushed through 1773e06 (origin synced). NOT merged to main.
- Milestone v4.0: phases 13 (done) + 14 (code done, adversarial-review pending) + 15 (live UAT) remain to close.
</content>
