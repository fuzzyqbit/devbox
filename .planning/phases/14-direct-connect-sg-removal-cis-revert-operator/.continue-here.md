---
context: phase
phase: 14-direct-connect-sg-removal-cis-revert-operator
status: plans-revised-awaiting-recheck-then-execute
last_updated: 2026-06-19
branch: feat/dcv
---

# Resume point — v4.0 DCV milestone (YOLO-the-rest in progress)

Paused at a clean checkpoint due to context limit. Tree clean, all on branch `feat/dcv`.

## Done
- **Milestone v4.0 defined** (Amazon DCV; non-GPU; QUIC on; **direct TCP+UDP :8443**, no SSM tunnel; license assumed reachable externally — documented residual risk). PROJECT/REQUIREMENTS(DCV-01..11)/ROADMAP(phases 13-15)/research all committed.
- **Phase 13 (dcv role) — COMPLETE + verified PASSED** (bake-config). dcv role built (port of git 51c5f1f + QUIC-on dcv.conf, FIPS cert at /etc/dcv/dcv.{pem,key} dcv:dcv 0600, oneshot virtual session with GNOME-on-Xorg `--init`, SELinux relabel, bake assert, wired before hardening). Opus adversarial review CLEAR after the CRITICAL-2 `--init` fix. 13-VERIFICATION.md written. Commits 17b3a6b/f98dd7a/b907908/2b98793/e0f698a/c13ad6d/e64c164/e5b9e23.
- **Phase 14 plans — REVISED + committed (`1a0e5c3`), passed opus plan-check once, then revised to close 3 blockers + 2 warnings** (docs/ uncovered, secrets/defaults comment, un-passable DCV-09 gate, run:404, CLAUDE.md grep). 5 plans, 3 waves: W1 additive [14-01 SG :8443 TCP+UDP, 14-02 operator+secrets-bootstrap, 14-05 docs/], W2 destructive [14-03 xrdp removal + CIS-2.2.1 revert + guard RETARGET→/usr/bin/Xdcv], W3 verify [14-04 repo-wide completeness gate].

## Next (resume here)
1. **Re-check the revised Phase 14 plans** (opus gsd-plan-checker) — confirm B1/B2/B3/W1/W2 closed, no regression. (Was about to spawn this when paused.)
2. **Execute Phase 14** (`/gsd:execute-phase 14`): sequential (use_worktrees=false), wave by wave. After execution, run an **opus adversarial review** of the committed removal+SG+CIS-revert (the lesson — every phase has had a green-but-broken catch) + my own gate battery (hardening-last=1, repo-wide completeness gate empty, SG :8443 TCP+UDP, dcv role intact, CIS 2.2.1 reverted + guard retargeted). Then author 14-VERIFICATION.md.
3. **Phase 15 (DCV-11) = LIVE UAT — cannot be done autonomously** (needs `./run build` + a live instance + AWS). HAND OFF the checklist: direct :8443 TCP+UDP connect (in-CIDR) as ec2-user → GNOME virtual session renders + license resolves + SELinux AVC-clean under enforcing + FIPS TLS handshake + QUIC path + CIS-2.2.1-revert safe. Also at UAT: `repoquery --requires nice-xdcv` to confirm it doesn't pull xorg-x11-server-common (the CIS-revert safety).

## Carried risks (must hold through execution)
- nice-xdcv vs CIS 2.2.1: the retargeted post-hardening guard (stat /usr/bin/Xdcv, gated layers.dcv+desktop) makes a wrong revert a LOUD bake failure — keep it.
- Over-removal: removing the xrdp role dir must NOT touch the dcv role (it ships its own colord .rules), gnome-session/mesa, code-server, jupyter, or the kept vnc-password SSM credential (relabel only).
- Push: branch feat/dcv is local — NOT pushed. Operator decision.
</content>
