---
phase: 14-direct-connect-sg-removal-cis-revert-operator
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03", "14-05"]
files_modified: []
autonomous: true
requirements: [DCV-09]
must_haves:
  truths:
    - "No xrdp / xorgxrdp / :3389 functional artifact survives anywhere outside .planning/ and the kept dcv role's provenance comments (allowlisting only the vnc-password credential + the path-excluded dcv role)"
    - "No VNC / noVNC / :6080 functional artifact survives (allowlisting the kept vnc-password SSM identifiers; port tokens are delimiter-bound so the .gitlab-ci.yml checkov digest containing the substring 6080 does NOT false-positive)"
    - "The full Phase-14 grep battery (research §6) passes: SG :8443 TCP+UDP gated, :3389 gone, CIS 2.2.1 reverted, guard targets /usr/bin/Xdcv, hardening last"
    - "The gate still flags the B1 (docs/), B2 (secrets/defaults) and removed-xrdp residue if any of it were left unfixed"
    - "ansible-lint + terraform validate + pre-commit run --all-files are clean"
  artifacts: []
  key_links:
    - from: "repo-wide git grep gate"
      to: "the kept vnc-password credential identifiers + the path-excluded dcv role provenance"
      via: "delimiter-bound port tokens + credential allowlist + ':!ansible/roles/dcv/**' pathspec"
      pattern: "git grep -nIE .* -- ':!.planning/\\*\\*' ':!ansible/roles/dcv/\\*\\*'"
---

<objective>
The VERIFICATION wave (DCV-09): run the repo-wide completeness gate proving that NO dead remote-desktop config (xrdp / xorgxrdp / :3389 / VNC / noVNC / :6080) survives anywhere outside `.planning/` history and the intentionally-kept `dcv` role — plus the Phase-14 structural grep battery (research §6) and the project lint/format gates. This is a pure verification plan: it modifies no source files (the removals/edits are Waves 1-2); it adds a SUMMARY recording the gate results, and creates source-file repairs ONLY if the gate surfaces a residual that Waves 1-2 missed.

Purpose: DCV-09 is a repo-wide completeness requirement. Run it LAST (after every additive + destructive edit) so it is authoritative. `nyquist_validation: false` in config → there is no automated test-framework to scaffold; the grep battery + ansible-lint + terraform validate + pre-commit IS the validation here (mirrors the Phase-12 RDP-11 completeness-gate pattern).

BLOCKER 3 — the original §6 gate was un-passable because of two UNAVOIDABLE false-positives, and is redesigned here to be GREEN-able after a perfect Wave 1-3 WHILE still catching real residue:
- (a) The KEPT Phase-13 `dcv` role legitimately contains the string `xrdp` in ~8 provenance comments (e.g. `ansible/roles/dcv/tasks/main.yml`: "ported from the proven xrdp...", "mirrors the xrdp RDP-13 block"; `files/45-allow-colord.rules`: "Ported verbatim from the xrdp role"; `files/dcv.pam`: "Modeled on the xrdp-sesman PAM file"; `files/dcv-gnome-session.sh`: "Ported from ansible/roles/xrdp/templates/startwm.sh.j2"). These document provenance and MUST NOT be removed (removing them is over-removal of the just-shipped, verified Phase-13 role). DECISION: **path-exclude the dcv role** (`-- ':!ansible/roles/dcv/**'`) rather than scrub the comments. Justification: path-exclusion is zero-churn on the verified Phase-13 role (no risk of touching working files), it is auditable (one pathspec, documented inline), and the dcv role is the ONE place `xrdp` is legitimately allowed — anywhere else is real residue. (Scrubbing the 8 comments was the alternative; rejected as higher-churn on a freshly-verified role for no completeness gain — the path-exclusion already guarantees no NEW xrdp residue can hide there because the role is fully Phase-13-owned and reviewed.)
- (b) `.gitlab-ci.yml:239` checkov image digest `sha256:22b308dd96e158b446c6080d19ea41ffb609ebd46962c9ace43b9b6c8aaff5cf` contains the SUBSTRING `6080`. DECISION: **bound port tokens to delimiters** — match `:6080` / `port 6080` / `\b6080/(tcp|udp)\b`, NOT the bare digit run `6080`. Same treatment for `3389` (match `:?3389` as a port, i.e. delimiter-bound, not a bare 4-digit substring). This kills the digest false-positive while still catching every real port reference (which always carries a `:` or `port ` prefix).

This phase closes at "grep-clean + lint-clean + validate-clean".
Output: gate results recorded in the SUMMARY; if a residual is found, the minimal source repair + a re-run (note any repaired file in the SUMMARY's files-modified).

NOTE — the live bake (GNOME-renders, AVC-clean-under-enforcing, FIPS handshake, QUIC path, license resolves, `repoquery --requires nice-xdcv`) is Phase-15 (DCV-11), OUT of scope here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide completeness grep gate — xrdp/VNC/noVNC functional residue = ZERO (DCV-09)</name>
  <files>(verification only — repair source files only if a residual is found)</files>
  <action>Run the redesigned completeness battery from the repo root. Each command must produce the expected (empty / OK) result; if ANY surfaces a non-allowlisted hit, fix the offending source file (minimal edit consistent with Waves 1-3) and re-run until clean, recording the repaired file in the SUMMARY.

  TWO documented exemptions (BLOCKER 3), applied to EVERY grep below:
  - PATHSPEC EXCLUSIONS: `-- ':!.planning/**' ':!ansible/roles/dcv/**'`. The `.planning/**` exclusion drops historical planning docs; the `ansible/roles/dcv/**` exclusion drops the KEPT Phase-13 dcv role whose ~8 provenance comments legitimately contain `xrdp` (documented in this plan's objective — do NOT remove them; this is the ONLY place `xrdp` is allowed).
  - DELIMITER-BOUND PORT TOKENS: ports are matched WITH their delimiter — `:3389`, `:6080`, `port 6080`, `6080/(tcp|udp)` — NEVER the bare digit runs `3389`/`6080` (the bare-`6080` form false-positives on the `.gitlab-ci.yml:239` checkov digest `...446c6080d...`; the bare-`3389` form is avoided for symmetry/robustness).

  (1) xrdp / xorgxrdp / :3389 — `git grep -nIE 'xrdp|xorgxrdp|:3389|3389/(tcp|udp)' -- ':!.planning/**' ':!ansible/roles/dcv/**'` filtered through `grep -vE 'vnc-password|!= "changeme"'` must be EMPTY.
  (2) VNC / noVNC / :6080 — `git grep -nIE 'tigervnc|vncserver|novnc|noVNC|/etc/pam\.d/vnc|:6080|port 6080|6080/(tcp|udp)' -- ':!.planning/**' ':!ansible/roles/dcv/**'` filtered through `grep -vE 'vnc-password|ssm_vnc_password_param|VNC_PWD|vnc_pwd'` must be EMPTY (the ONLY VNC-string survivors are the credential-path identifiers; everything functional was removed in v3.2 Phase 12).

  COVERAGE PROOF (the gate must still CATCH real residue — confirm by reasoning in the SUMMARY, not by leaving residue): the pathspec covers `docs/` (B1 residue: `:3389`, `port-forward 3389`, `native RDP` — `:3389` is caught; also run the docs-scoped supplement below), `ansible/roles/secrets/defaults/main.yml` (B2 residue: the word `xrdp` in the comment — caught by token `xrdp`, NOT path-excluded), `run`, `scripts/`, `terraform/`, and the removed xrdp role/wiring. Because the dcv-role exclusion is the ONLY path exemption, B1/B2 residue (which lives in docs/ and secrets/defaults, neither excluded) WOULD be flagged if Waves 1-2 had not fixed it.
  (3) docs-scoped RDP-prose supplement (catches the docs residue that is not a bare port token) — `git grep -nIE 'xrdp|:3389|port-forward 3389|native RDP|RDP desktop|RDP client' -- 'docs/**'` must be EMPTY (the kept code-server/:8080 + SSM + Jupyter content is fine; only RDP/:3389 desktop residue is forbidden).

  Tune the allowlist to the kept credential identifiers ONLY and the dcv-role pathspec ONLY — do NOT broaden either to mask a real residual. Record the exact final commands + their (empty) output in the SUMMARY as the DCV-09 evidence, and record the inline justification for each of the two exemptions.</action>
  <verify>
    <automated>test -z "$(git grep -nIE 'xrdp|xorgxrdp|:3389|3389/(tcp|udp)' -- ':!.planning/**' ':!ansible/roles/dcv/**' | grep -vE 'vnc-password|!= \"changeme\"')" && test -z "$(git grep -nIE 'tigervnc|vncserver|novnc|noVNC|/etc/pam\.d/vnc|:6080|port 6080|6080/(tcp|udp)' -- ':!.planning/**' ':!ansible/roles/dcv/**' | grep -vE 'vnc-password|ssm_vnc_password_param|VNC_PWD|vnc_pwd')" && test -z "$(git grep -nIE 'xrdp|:3389|port-forward 3389|native RDP|RDP desktop|RDP client' -- 'docs/**')" && echo PASS</automated>
  </verify>
  <done>All three greps (xrdp/:3389; VNC/noVNC/:6080; docs RDP-prose), with `.planning/` AND the kept `ansible/roles/dcv/**` path-excluded, delimiter-bound port tokens, and the credential-identifier allowlist, return NOTHING — no functional remote-desktop residue survives outside the deliberately-kept dcv role provenance. The `.gitlab-ci.yml` checkov digest does NOT trip the gate (delimiter-bound tokens); the dcv-role provenance comments are NOT removed (path-excluded).</done>
</task>

<task type="auto">
  <name>Task 2: Structural grep battery + lint/validate gates (research §6 + invariants)</name>
  <files>(verification only)</files>
  <action>Run the remaining research §6 structural checks + the project gates, recording results in the SUMMARY: (a) `test ! -d ansible/roles/xrdp` and `test ! -f ansible/test-xrdp.yml` → both removed. (b) hardening-last invariant: `[ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'hardening')" -eq 1 ]`. (c) SG: `grep -A5 'from_port   = 8443' terraform/main.tf | grep -q 'var.allowed_web_cidrs'` (gated), `[ "$(grep -cE 'protocol    = "(tcp|udp)"' terraform/main.tf)" -ge 3 ]` (8080 tcp + 8443 tcp + 8443 udp), `! grep -qE ':?3389' terraform/main.tf`. (d) CIS revert: `! grep -q 'amzn2023cis_rule_2_2_1' ansible/roles/hardening/defaults/main.yml`. (e) guard retargeted: `grep -q '/usr/bin/Xdcv' ansible/playbook.yml`. (f) surviving-feature checks: `grep -q 'role: dcv' ansible/playbook.yml`, `grep -q 'dcv: true' ansible/layer_config.yml`, `test -f ansible/roles/dcv/files/45-allow-colord.rules`, `grep -q 'dcvserver.service' ansible/roles/secrets/templates/devbox-secrets-bootstrap.service.j2`, `grep -q 'output "dcv_endpoint"' terraform/outputs.tf`, `grep -q 'vnc-password' run`, `test -f docs/HOWTO-ACCESS-CODE-SERVER-DCV.md` and `test ! -f docs/HOWTO-ACCESS-CODE-SERVER-RDP.md` (B1 rename landed), `grep -q 'secrets_vnc_password_length: 20' ansible/roles/secrets/defaults/main.yml` (B2 length kept). (g) project gates: `ansible-lint` (whole repo or at least playbook.yml + roles/secrets + roles/hardening), `cd terraform && (tofu validate || terraform validate)`, and `pre-commit run --all-files` (the grep-gates / no-changeme / hardening-last hooks are authoritative — the kept `!= "changeme"` asserts are a known acceptable false-positive carried from the secrets role; the no-changeme hook scopes to the literal `changeme`, which is absent). Fix any failure at its source and re-run.</action>
  <verify>
    <automated>test ! -d ansible/roles/xrdp && test ! -f ansible/test-xrdp.yml && [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'hardening')" -eq 1 ] && grep -q '/usr/bin/Xdcv' ansible/playbook.yml && ! grep -q 'amzn2023cis_rule_2_2_1' ansible/roles/hardening/defaults/main.yml && grep -q 'role: dcv' ansible/playbook.yml && grep -q 'output "dcv_endpoint"' terraform/outputs.tf && test -f docs/HOWTO-ACCESS-CODE-SERVER-DCV.md && test ! -f docs/HOWTO-ACCESS-CODE-SERVER-RDP.md && (cd terraform && (tofu validate >/dev/null 2>&1 || terraform validate >/dev/null 2>&1)) && echo PASS</automated>
  </verify>
  <done>All structural greps pass (xrdp dir/test gone, hardening last, :8443 TCP+UDP gated, :3389 gone, 2.2.1 reverted, guard targets Xdcv); surviving-feature checks pass (dcv role/toggle/colord/dcvserver-unit/dcv_endpoint/vnc-password/secrets-length all present, docs HOWTO renamed to DCV); ansible-lint + terraform validate + pre-commit run --all-files are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked source tree → baked AMI | A dead/residual remote-desktop config string left in tracked source could ship into the image or mislead future operators; the gate is the boundary check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Tampering / Information Disclosure | residual dead remote-desktop config in tracked files | mitigate | Repo-wide git-grep battery (allowlisting only the vnc-password credential identifiers + the kept dcv-role pathspec) proves zero functional xrdp/VNC/noVNC residue before the phase closes |
| T-14-11 | Repudiation | invariant drift (hardening-last, no-changeme, SHA-pin) | mitigate | `pre-commit run --all-files` runs the authoritative grep-gates + no-changeme + hardening-last hooks; the kept `!= "changeme"` asserts are a documented acceptable false-positive (literal `changeme` absent) |
| T-14-14 | Repudiation | gate exemptions masking real residue | mitigate | Both exemptions are MINIMAL and documented inline: the dcv-role pathspec is the only place `xrdp` is legitimate (Phase-13-owned, reviewed); the delimiter-bound port tokens still catch every real `:`/`port `-prefixed reference. Coverage re-confirmed: B1 (docs/) and B2 (secrets/defaults) live OUTSIDE the exempted path and WOULD flag if unfixed |
</threat_model>

<verification>
- All three repo-wide greps (Task 1: xrdp/:3389; VNC/noVNC/:6080; docs RDP-prose) return empty, with the two documented exemptions applied.
- The `.gitlab-ci.yml:239` checkov digest does NOT trip the gate (delimiter-bound tokens); the dcv-role provenance comments are NOT removed (path-excluded).
- The full §6 structural battery (Task 2) passes incl. the hardening-last invariant, every surviving-feature check, and the B1/B2 landing checks (docs HOWTO renamed, secrets-length kept).
- `ansible-lint` clean; `tofu validate` (or `terraform validate`) clean; `pre-commit run --all-files` clean.
- SUMMARY records the exact gate commands + outputs + the two exemption justifications as DCV-09 evidence.
</verification>

<success_criteria>
DCV-09 satisfied: the redesigned repo-wide completeness gate returns zero functional xrdp/VNC/noVNC residue (the kept vnc-password credential identifiers + the path-excluded dcv role provenance are the only allowlisted survivors; port tokens are delimiter-bound so the .gitlab-ci.yml checkov digest does not false-positive); the structural grep battery + ansible-lint + terraform validate + pre-commit all pass; hardening-stays-last re-asserted; B1/B2 residue would still be flagged if unfixed; the phase closes grep-clean + lint-clean + validate-clean. Live bake checks (render/AVC/FIPS/QUIC/license/repoquery nice-xdcv) carried to Phase-15 DCV-11.
</success_criteria>

<output>
Create `.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-04-SUMMARY.md` when done.
</output>
</content>
