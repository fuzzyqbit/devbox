---
phase: 14-direct-connect-sg-removal-cis-revert-operator
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified: []
autonomous: true
requirements: [DCV-09]
must_haves:
  truths:
    - "No xrdp / xorgxrdp / :3389 functional artifact survives anywhere outside .planning/ (allowlisting only the kept vnc-password credential identifiers)"
    - "No VNC / noVNC / :6080 functional artifact survives (allowlisting the kept vnc-password SSM identifiers)"
    - "The full Phase-14 grep battery (research §6) passes: SG :8443 TCP+UDP gated, :3389 gone, CIS 2.2.1 reverted, guard targets /usr/bin/Xdcv, hardening last"
    - "ansible-lint + terraform validate + pre-commit run --all-files are clean"
  artifacts: []
  key_links:
    - from: "repo-wide git grep gate"
      to: "the kept vnc-password credential identifiers"
      via: "allowlist filter (vnc-password / ssm_vnc_password_param / VNC_PWD / vnc_pwd / != \"changeme\")"
      pattern: "git grep -nIE .* | grep -vE 'vnc-password"
---

<objective>
The VERIFICATION wave (DCV-09): run the repo-wide completeness gate proving that NO dead remote-desktop config (xrdp / xorgxrdp / :3389 / VNC / noVNC / :6080) survives anywhere outside `.planning/` history — allowlisting ONLY the intentionally-kept `vnc-password` credential identifiers — plus the Phase-14 structural grep battery (research §6) and the project lint/format gates. This is a pure verification plan: it modifies no source files (the removals/edits are Waves 1-2); it adds a SUMMARY recording the gate results, and creates source-file repairs ONLY if the gate surfaces a residual that Waves 1-2 missed.

Purpose: DCV-09 is a repo-wide completeness requirement. Run it LAST (after every additive + destructive edit) so it is authoritative. `nyquist_validation: false` in config → there is no automated test-framework to scaffold; the grep battery + ansible-lint + terraform validate + pre-commit IS the validation here (mirrors the Phase-12 RDP-11 completeness-gate pattern).
Output: gate results recorded in the SUMMARY; if a residual is found, the minimal source repair + a re-run (note any repaired file in the SUMMARY's files-modified).

NOTE — the live bake (GNOME-renders, AVC-clean-under-enforcing, FIPS handshake, QUIC path, license resolves, `repoquery --requires nice-xdcv`) is Phase-15 (DCV-11), OUT of scope here. This phase closes at "grep-clean + lint-clean + validate-clean".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide completeness grep gate — xrdp/VNC/noVNC functional residue = ZERO (DCV-09)</name>
  <files>(verification only — repair source files only if a residual is found)</files>
  <action>Run the research §6 completeness battery from the repo root. Each command must produce the expected (empty / OK) result; if ANY surfaces a non-allowlisted hit, fix the offending source file (minimal edit consistent with Waves 1-3) and re-run until clean, recording the repaired file in the SUMMARY. (1) xrdp/xorgxrdp/:3389 — `git grep -nIE 'xrdp|xorgxrdp|3389' -- ':!.planning/**'` filtered through `grep -vE 'vnc-password|!= "changeme"'` must be EMPTY. (2) VNC/noVNC — `git grep -nIE 'tigervnc|vncserver|novnc|noVNC|/etc/pam\.d/vnc|6080' -- ':!.planning/**'` filtered through `grep -vE 'vnc-password|ssm_vnc_password_param|VNC_PWD|vnc_pwd'` must be EMPTY (the ONLY VNC-string survivors are the credential-path identifiers; everything functional was removed in v3.2 Phase 12). Tune the allowlist to the kept credential identifiers ONLY — do NOT broaden it to mask a real residual. Record the exact final commands + their (empty) output in the SUMMARY as the DCV-09 evidence.</action>
  <verify>
    <automated>test -z "$(git grep -nIE 'xrdp|xorgxrdp|3389' -- ':!.planning/**' | grep -vE 'vnc-password|!= \"changeme\"')" && test -z "$(git grep -nIE 'tigervnc|vncserver|novnc|noVNC|/etc/pam\.d/vnc|6080' -- ':!.planning/**' | grep -vE 'vnc-password|ssm_vnc_password_param|VNC_PWD|vnc_pwd')" && echo PASS</automated>
  </verify>
  <done>Both repo-wide greps (xrdp/:3389 and VNC/noVNC/:6080), filtered through the credential-identifier allowlist and excluding `.planning/`, return NOTHING — no functional remote-desktop residue survives.</done>
</task>

<task type="auto">
  <name>Task 2: Structural grep battery + lint/validate gates (research §6 + invariants)</name>
  <files>(verification only)</files>
  <action>Run the remaining research §6 structural checks + the project gates, recording results in the SUMMARY: (a) `test ! -d ansible/roles/xrdp` and `test ! -f ansible/test-xrdp.yml` → both removed. (b) hardening-last invariant: `[ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'hardening')" -eq 1 ]`. (c) SG: `grep -A5 'from_port   = 8443' terraform/main.tf | grep -q 'var.allowed_web_cidrs'` (gated), `[ "$(grep -cE 'protocol    = "(tcp|udp)"' terraform/main.tf)" -ge 3 ]` (8080 tcp + 8443 tcp + 8443 udp), `! grep -q '3389' terraform/main.tf`. (d) CIS revert: `! grep -q 'amzn2023cis_rule_2_2_1' ansible/roles/hardening/defaults/main.yml`. (e) guard retargeted: `grep -q '/usr/bin/Xdcv' ansible/playbook.yml`. (f) surviving-feature checks: `grep -q 'role: dcv' ansible/playbook.yml`, `grep -q 'dcv: true' ansible/layer_config.yml`, `test -f ansible/roles/dcv/files/45-allow-colord.rules`, `grep -q 'dcvserver.service' ansible/roles/secrets/templates/devbox-secrets-bootstrap.service.j2`, `grep -q 'output "dcv_endpoint"' terraform/outputs.tf`, `grep -q 'vnc-password' run`. (g) project gates: `ansible-lint` (whole repo or at least playbook.yml + roles/secrets + roles/hardening), `cd terraform && (tofu validate || terraform validate)`, and `pre-commit run --all-files` (the grep-gates / no-changeme / hardening-last hooks are authoritative — the kept `!= "changeme"` asserts are a known acceptable false-positive carried from the secrets role; the no-changeme hook scopes to the literal `changeme`, which is absent). Fix any failure at its source and re-run.</action>
  <verify>
    <automated>test ! -d ansible/roles/xrdp && test ! -f ansible/test-xrdp.yml && [ "$(grep -E '^[[:space:]]*-[[:space:]]*role:' ansible/playbook.yml | tail -1 | grep -c 'hardening')" -eq 1 ] && grep -q '/usr/bin/Xdcv' ansible/playbook.yml && ! grep -q 'amzn2023cis_rule_2_2_1' ansible/roles/hardening/defaults/main.yml && grep -q 'role: dcv' ansible/playbook.yml && grep -q 'output "dcv_endpoint"' terraform/outputs.tf && (cd terraform && (tofu validate >/dev/null 2>&1 || terraform validate >/dev/null 2>&1)) && echo PASS</automated>
  </verify>
  <done>All structural greps pass (xrdp dir/test gone, hardening last, :8443 TCP+UDP gated, :3389 gone, 2.2.1 reverted, guard targets Xdcv); surviving-feature checks pass (dcv role/toggle/colord/dcvserver-unit/dcv_endpoint/vnc-password all present); ansible-lint + terraform validate + pre-commit run --all-files are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked source tree → baked AMI | A dead/residual remote-desktop config string left in tracked source could ship into the image or mislead future operators; the gate is the boundary check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Tampering / Information Disclosure | residual dead remote-desktop config in tracked files | mitigate | Repo-wide git-grep battery (allowlisting only the vnc-password credential identifiers) proves zero functional xrdp/VNC/noVNC residue before the phase closes |
| T-14-11 | Repudiation | invariant drift (hardening-last, no-changeme, SHA-pin) | mitigate | `pre-commit run --all-files` runs the authoritative grep-gates + no-changeme + hardening-last hooks; the kept `!= "changeme"` asserts are a documented acceptable false-positive (literal `changeme` absent) |
</threat_model>

<verification>
- Both repo-wide greps (Task 1) return empty.
- The full §6 structural battery (Task 2) passes incl. the hardening-last invariant and every surviving-feature check.
- `ansible-lint` clean; `tofu validate` (or `terraform validate`) clean; `pre-commit run --all-files` clean.
- SUMMARY records the exact gate commands + outputs as DCV-09 evidence.
</verification>

<success_criteria>
DCV-09 satisfied: repo-wide completeness gate returns zero functional xrdp/VNC/noVNC residue (only the kept vnc-password credential identifiers allowlisted); the structural grep battery + ansible-lint + terraform validate + pre-commit all pass; hardening-stays-last re-asserted; the phase closes grep-clean + lint-clean + validate-clean. Live bake checks (render/AVC/FIPS/QUIC/license/repoquery nice-xdcv) carried to Phase-15 DCV-11.
</success_criteria>

<output>
Create `.planning/phases/14-direct-connect-sg-removal-cis-revert-operator/14-04-SUMMARY.md` when done.
</output>
